// AUTO-GENERATED by scripts/gen-changelog.mjs — DO NOT EDIT.
// 数据源：根目录 CHANGELOG.md（Keep a Changelog），唯一手写源；predev/prebuild 自动重新生成。
import type { ChangelogEntry } from "./about";

export const CHANGELOG: ChangelogEntry[] = [
  {
    version: "2.7.4",
    date: "2026-09-03",
    items: [
      { category: "feat", text: "同一台机器可以开多个终端。标签栏右侧的「＋ 新终端」，或侧栏连接项上的「＋」。以前想一边 `tail -f` 一边敲命令就得开两个窗口。同一连接多个标签时会标上 `#1 #2`。" },
      { category: "improve", text: "折叠侧栏的快捷键 Ctrl+B 改为 Ctrl+Shift+B，与已有的 Ctrl+Shift+C / A / F 一致，Ctrl+B 还给远端。" },
      { category: "improve", text: "字号在多个终端间实时同步。以前字号存在一个全局键里，却只在当前终端生效，重启后其它终端才突然跳过去。" },
      { category: "fix", text: "终端里的中文输出会随机烂字。PTY 每次最多读 4096 字节，而这个边界与字符边界无关——一个 3 字节的中文字只要跨了边界就会被拆成两半、各自变成 `�`。实测：纯中文输出每约 4KB 就烂一个字。" },
      { category: "fix", text: "SFTP 列目录时中文文件名可能乱码，同一个解码 bug 的另一条路径。" },
      { category: "fix", text: "自动填密码偶发静默失效。同上：一句中文的「密码：」提示只要被读取边界劈开，就匹配不上了。" },
      { category: "fix", text: "Ctrl+B 被终端页抢走。它是 tmux 的默认前缀键、也是命令行的光标左移。更难受的是两件事同时发生：按键已经发给了远端，侧栏又跟着折叠一下。而且它没判断当前是不是在终端页，在连接页、日志页按也会生效。" },
      { category: "fix", text: "刷大量输出时界面会卡死。以前每读到 4KB 就往前端推一次，`cat` 一个大文件能把这个频率推到每秒上万次。现在按一帧合批，但交互时不加任何延迟（只在真洪水时限流）。" },
      { category: "fix", text: "「复制选中」可能悄悄复制整个 5000 行历史。按过一次全选之后，那份选区会被永久缓存；之后只要当下没选中任何东西，点复制就会拿到那份旧的。现在缓存 10 秒过期。" },
      { category: "fix", text: "重新连接会清掉全部历史输出。断开后保留标签的意义就是让之前的输出还能看、能复制，结果点一下重连就全没了。现在只在旧输出下面加一行「[已重新连接]」。" },
      { category: "fix", text: "折叠侧栏把已断开的连接显示成绿点（展开态是对的，两处口径不一致）。" },
      { category: "fix", text: "SSH 总开关关掉后，每敲一个键终端里就多一行红字，几下就把最后那屏有用的输出刷没了。现在同一个原因只写一次。" },
      { category: "fix", text: "标签多了不会自动滚到当前标签，新连的会话可能落在可视区外，看着像没反应。" },
      { category: "fix", text: "搜索框聚焦时再按 Ctrl+Shift+F 没反应（提示里写的「再按一次可以直接换关键词」实际做不到）。" },
      { category: "fix", text: "传输中拖文件进来完全没反馈。现在会出一层灰色提示说清楚原因（仍然不接新的一批）。" },
    ],
  },
  {
    version: "2.7.3",
    date: "2026-09-02",
    items: [
      { category: "improve", text: "docs: 为 updater 端点修复补充 CHANGELOG 条目" },
      { category: "fix", text: "updater 首选端点换掉证书已过期的 ghproxy.net" },
      { category: "fix", text: "检查更新会先撞一个已失效的加速地址。updater 的首选镜像 `ghproxy.net` 的 HTTPS 证书已过期，每次检查更新都必然失败一次，然后才回落到 GitHub 官方地址；网络再差一点就会直接检查不到新版本。已换成实测可用的 `gh-proxy.com`（GitHub 官方地址保留为兜底）。" },
      { category: "fix", text: "附带说明：更新包本身用签名校验，经由哪个镜像都不会影响完整性，所以这次只换地址、不涉及安全策略变化。" },
    ],
  },
  {
    version: "2.7.2",
    date: "2026-09-02",
    items: [
      { category: "feat", text: "SSH 终端——跳板机中转、拖拽上传、终端内搜索、全屏与配色跟随主题" },
      { category: "feat", text: "终端全屏。工具栏新增全屏按钮，或按 F11；终端铺满整个窗口，顶部导航与左侧连接栏让位。" },
      { category: "feat", text: "终端配色跟随亮暗主题。亮色主题下终端改为浅底配色，标题栏、边框、滚动条一并适配；切主题不断连接、不丢历史输出。" },
      { category: "feat", text: "断开后终端不再消失。会话断开或连接失败时，标签会保留为「已断开」，历史输出仍可滚动、选中、复制，并可一键重连。" },
      { category: "feat", text: "多行粘贴先确认。粘贴超过一行时先弹框展示行数与内容预览——多行内容会被远端逐行执行，不会等你再按回车。" },
      { category: "feat", text: "终端内搜索（Ctrl+Shift+F 或工具栏放大镜）。支持区分大小写与正则，显示「第几个 / 共几个」，Enter / Shift+Enter 切下一个上一个。" },
      { category: "feat", text: "Ctrl+滚轮 缩放字号（9–24px），字号全局生效、重开保持。" },
      { category: "feat", text: "滚动历史从 1000 行提到 5000 行。" },
      { category: "feat", text: "终端里的链接可点开（仅 http/https）。" },
      { category: "feat", text: "连接超过 6 个时侧栏出现搜索框，按名称/主机/用户名过滤。" },
      { category: "feat", text: "拖拽上传。从资源管理器直接把文件拖进来：拖到文件面板就传到当前目录（不弹框），拖到终端则弹一个可编辑的目标目录确认框（预填上次用过的目录，确认后记住）。支持多文件（顺序排队，进度条显示 1/N）；文件夹会当场提示「请先打包」。" },
      { category: "feat", text: "文件面板记住上次的目录，不再每次重开都从根目录 `/` 开始。" },
      { category: "feat", text: "跳板机（堡垒机）。目标机在内网、只能从堡垒机进时，在连接里选一条已保存的连接作跳板，终端、文件浏览、上传下载全部自动经它中转；文件传输是端到端的，不会在跳板机上落盘。连接列表里会标出「经 xxx」。" },
      { category: "improve", text: "终端标题栏重排。去掉「断开」按钮（它紧挨着「清屏」，是最容易手滑误点的位置）；复制整屏 / 选择模式 / 清屏收进「更多」菜单并带上文字说明。" },
      { category: "improve", text: "标签栏的 × 分两档：已连接 → 断开（标签保留、可重连）；已断开 → 关闭标签。误点不再丢历史输出。" },
      { category: "improve", text: "去掉工具栏的「已回传 N 字符」徽标（当初排查输入问题时加的调试件）。" },
      { category: "fix", text: "终端底部最后一行被切掉半截（尤其在全屏/最大化之后、开着 Claude Code 这类带输入框的 TUI 时最明显）。" },
      { category: "fix", text: "部分电脑上终端滚动条几乎看不见。滑块颜色改为显式指定并跟随主题。" },
      { category: "fix", text: "连上终端后面板圆角消失。" },
      { category: "fix", text: "亮色主题下终端标题栏的文字看不清（深色文字压在近黑背景上）；折叠侧栏里写死的选中色与状态点颜色也改为跟随主题。" },
      { category: "fix", text: "切会话 / 切页时会把隐藏的终端压成 1 行并发给远端，导致切回来时 TUI 排版错乱。" },
      { category: "fix", text: "断开 / 连接失败的原因看不到。以前只写进一个马上要销毁的终端，那行字从未渲染；用户只看到标签凭空消失，而之前还弹过一个「已连接」的成功提示。" },
      { category: "fix", text: "右键菜单标的快捷键是假的，而且按下去有害。菜单写着 `Ctrl+Shift+C` 复制、`Ctrl+A` 全选，但两个都没实现，实际会发出 \\x03（SIGINT，中断正在跑的命令）与 \\x01（跳行首 / tmux 前缀键）。现已真正实现 `Ctrl+Shift+C` / `Ctrl+Shift+A`。" },
      { category: "fix", text: "关掉当前标签后掉进空面板（标签栏上明明还剩其它会话），且会连带掉出全屏。现在会激活相邻会话。" },
      { category: "fix", text: "文件管理的列目录/建目录/删除会阻塞界面。这三个命令把同步阻塞调用直接放在异步命令里，每次钉住一个 tokio worker 最长 30 秒；worker 被占满时其它命令（包括每敲一个字符都要走的 `ssh_input`）会一起排队。" },
      { category: "improve", text: "文件管理快了一个量级。列目录/建目录/删除不再每次重新登录服务器，而是复用一条常驻会话。" },
      { category: "improve", text: "大量输出时不再掉帧（`tail -f`、构建日志），改用 GPU 渲染。" },
      { category: "improve", text: "中文/emoji/框线字符的宽度排版修正，减少 TUI 串位。" },
    ],
  },
  {
    version: "2.7.1",
    date: "2026-08-31",
    items: [
      { category: "feat", text: "SSH 终端支持粘贴。Ctrl+V、Ctrl+Shift+V、右键菜单（复制 / 粘贴 / 全选）、工具栏按钮四条路径。此前终端里没有任何粘贴通路：xterm 把 Ctrl+V 当作 SYN 控制字符发给远端并 `preventDefault`，浏览器因此不会产生 `paste` 事件，xterm 自带的两个 paste 监听器永远收不到。改用官方钩子 `attachCustomKeyEventHandler` 在 xterm 之前截获。代价是远端不再收到 Ctrl+V（readline 的 quoted-insert 失效）。另：`Shift+Insert` 在旧版本上本来就能粘贴，xterm 对它不设按键结果因而不拦截原生事件。" },
      { category: "feat", text: "文件传输进度条 + 取消。传输期间在文件头下方显示百分比与剩余时间，可随时取消（后端置标志，monitor 线程 ≤200ms 内终止进程）。此前传输期间界面只有一个 busy 态，看不出在传还是卡死，也停不下来。" },
      { category: "feat", text: "覆盖前二次确认。下载与上传遇到同名文件都会先弹确认框——此前删除有二次确认、覆盖却静默执行，而两者丢数据的后果是一样的。下载改为先写 `.ccbpart` 临时文件、传完才替换，中途取消或失败不会碰到原文件。" },
      { category: "fix", text: "带空格 / 特殊字符的文件名无法上传下载。scp 的远程路径没加 shell 引号，而同一文件里的 `ls`/`mkdir`/`rm` 都加了，于是这类文件在列表里看得见、点下载却失败。遗留 SCP 协议下远程路径由远端 shell 展开，`a b.txt` 会被拆成两个参数。" },
      { category: "fix", text: "传输失败时错误信息几乎是空的。scp 的 `-q` 会把进度条和 ssh 的警告/诊断信息一起关掉，失败只剩一句「退出码 1」。已去掉 `-q`，噪声交给前端本就写好的清理函数。" },
      { category: "fix", text: "远端是 macOS/BSD 时列目录直接报错。BSD 的 `ls` 拒绝 `--time-style` 时以非零码退出，而降级判断写在 `spawn_capture(..)?` 之后，永远走不到——降级路径是死代码。" },
      { category: "fix", text: "每次上传/下载都白跑一次握手。强制 SFTP 协议的 `-s` 在本机 OpenSSH 8.1p1 上不存在，原实现每次传输都先试一次（一次完整 TCP + 认证）才回退。改为探测一次、进程内缓存；探测只读 scp 的 usage 输出，不建立网络连接。" },
      { category: "improve", text: "`clipboard-manager:default` 自述是“No features are enabled by default … Clipboard interaction needs to be explicitly enabled”，即什么都不授予——这解释了为何全项目都在用 `navigator.clipboard.writeText` 绕开插件。已补 `clipboard-manager:allow-read-text`（只加读文本，不加读图片 / HTML）。" },
      { category: "improve", text: "取消标志用 `DashMap<String, Arc<AtomicBool>>` 注册，不放 `Child`：`spawn_capture` 的 monitor 线程本就独占 Child，共享出去要处理 `kill(&mut self)` 的可变借用、多套一层锁。" },
      { category: "improve", text: "传输改用 `spawn_blocking`：原先最长 600s 阻塞在 tokio worker 上，加了取消后会导致 `ssh_sftp_cancel` 自己排不上队。" },
      { category: "improve", text: "「取消 / 超时 / 失败」用错误前缀常量区分，不靠匹配中文文案——文案会改，前缀不会。" },
      { category: "improve", text: "进度只解析 `(\\d+)%`，不解析速率与 ETA 的列位：各版本 scp 的进度行格式不一致，百分比是唯一稳定项；剩余时间由前端按变化率算并做 EMA 平滑。去掉 `-q` 后进度条会涌进捕获缓冲，故只折叠「当前未完成行」，已换行的真实错误一律不动。" },
      { category: "improve", text: "留档：旧的 `xterm` / `xterm-addon-fit` 已被官方废弃（npm 标注 “Move to @xterm/xterm instead”、冻结在 5.3.0），已迁移到 `@xterm/xterm@6` + `@xterm/addon-fit@0.11`。这步实际随 v2.7.0 一同发出，但当时未记入发版说明，在此补记。" },
    ],
  },
  {
    version: "2.7.0",
    date: "2026-08-25",
    items: [
      { category: "feat", text: "SSH 终端——侧边栏可折叠、选区/拖选复制、拖选开关、配色对比度对齐" },
      { category: "feat", text: "SSH 终端连接侧边栏可折叠（图标 rail）。终端 Tab 头部「收起」按钮或 `Ctrl/Cmd+B` 切换：侧栏收成 48px 窄条（首字母圆 + 状态点 + 展开按钮 + hover tooltip），终端释放约 240px；展开时宽度 300ms expo 过渡、列表 stagger 淡入。折叠态用 localStorage 记忆，刷新/重开保持，零后端改动。" },
      { category: "feat", text: "「拖拽即选」开关（默认关）。设置页「高级 → 终端拖拽即选」开启后，在终端里直接拖选一段文字即自动进选择态、松手自动复制；关闭则恢复为必须按住 Shift 或点「选择模式」按钮（两条老路径始终可用）。对应新增后端配置项 `ssh_drag_select_enabled`（默认 false，向后兼容）。" },
      { category: "feat", text: "按住 Shift 拖选松手自动复制。选择态下鼠标松开即把选区写入剪贴板（抵御 Claude Code 持续重绘冲掉 live selection），并加 500ms 去重窗避免一次松手弹出多条提示。" },
      { category: "fix", text: "SSH 终端登录后无法输入（根因：portable-pty 0.9.0 的 Windows ConPTY 输入 bug）。现象是「输出正常、敲键无反应」——前端 `onData`→`ssh_input` 链路实测是通的（dev 后台 `[SSH] input` 能收到键入），但 0.9.0 重写的 ConPTY backend 会静默丢弃写入（返回 Ok），远端 shell 收不到输入。已将 `portable-pty` 从 `\"0.9\"` 钉死到 `\"=0.8.1\"`（回到稳定的旧 ConPTY backend，crates.io 已发布、API 兼容，多家终端项目已验证此回退可解 wezterm#1396 一类输入故障）。这是依赖层回归，非业务逻辑 bug，前端聚焦加固代码（见下）属冗余防护、保留无害。" },
      { category: "fix", text: "SSH 连接状态可观测性 + 终端聚焦加固。`ssh_connect` 在连接成功/早期失败/关闭三处增加 `#[cfg(debug_assertions)] eprintln!(\"[SSH] ...\")` 日志（dev 后台可见连接结果）；`ssh_input` 增加写后 `input-ok` / 失败 `input-FAIL` 诊断日志。`SshTerminal.tsx` 在打开、`mousedown`/`pointerdown`、切回该终端、`focusin` 时显式 `term.focus()` 夺回隐藏 textarea 焦点，并用「已回传 N 字符 / 输入失败」徽标前台提示输入回路状态。注意：HMR 不会重建 xterm 实例，已存在的旧会话需断开重连才会走新代码。" },
      { category: "fix", text: "SSH 终端切换应用级 Tab 需重新连接（根因：`TabsContent` 卸载 `TerminalTab`）。项目自研 `TabsContent`（`src/components/ui/tabs.tsx:130`）在 `active !== value` 时直接 `return null`，导致切到连接/日志/设置等 tab 时整个 `TerminalTab` 被卸载、前端 `sessions` 活跃连接 state 清零，切回需手动重连（后端 SSH session 其实未断，但前端无 re-attach 机制）。修复：`App.tsx` 将 `TerminalTab` 移出 `TabsContent`，改为常驻挂载、用 `display: activeTab==='terminal'?'block':'none'` 控制显隐，复用 `TerminalTab` 内部已验证的 `display:none` 多会话常驻模式，SSH 会话全程保活、切回即见历史输出。" },
      { category: "fix", text: "SSH 终端在 TUI（如 Claude Code）下无法选区/复制（根因：鼠标报告模式 + 关鼠标报告的转义发错了方向）。TUI 开启鼠标报告（`ESC[?1006h/?1002h/?1003h`）后，xterm 将鼠标拖拽编码为终端鼠标事件发给远端、本地选区建不起来，复制按钮读 `term.getSelection()` 恒为空并提示「没有选中内容」。真正的修复关键是关鼠标报告必须写到【本地 xterm】（`term.write`）：xterm 的 `mouseTrackingMode` 只由它从【输出流】解析的转义控制，而早先错误地把 `ESC[?1006l` 经 `ssh_input` 发给【远端 PTY(stdin)】，xterm 从不解析、Claude Code 又反复在输出里重设 `?1006h`，于是鼠标报告一直开着、拖选永远被吃（dev 日志实锤：`?1006h` 与重置序列先后出现、选区恒空）。修复（纯前端、零 Rust）：① 状态栏新增「选择模式」按钮，开启时把 `ESC[?1006l/?1003l/?1002l/?1000l` 直接 `term.write` 到本地 xterm 关闭鼠标报告使拖选建立本地选区；并在 `ssh_output` 监听每帧输出后再关一次，抵御远端重绘重设 `?1006h`（开启期 Claude Code 鼠标滚轮/点击在本地失效，复制完切回即可）；xterm v6 非 Mac 下 `shouldForceSelection` 本就返回 `shiftKey`，故「按住 Shift 拖选」是原生支持、无需额外发序列；② 新增「复制整屏」按钮，从 xterm 缓冲区（`term.buffer.active` 遍历行 `translateToString(true)`）直接导出可视区域纯文本，鼠标报告模式下也必成；③ 新增「按住 Shift 自动选择」：窗口监听 Shift 的 keydown/keyup，按住期间把同样的关鼠标报告序列 `term.write` 到本地 xterm、松开停止重关（只响应聚焦的会话、多会话互不误触），与手动选择模式共用 `selectActiveRef` 协调——任一为真即本地关鼠标报告；xterm v6 原生 `shouldForceSelection` 在 Windows 下也对 Shift 强制选区，双重保险；④ 复制按钮优先 `term.getSelection()`，空则明确提示「请先拖选或点复制整屏」。标题栏在选择模式/按住 Shift 下显示「拖选复制」徽标（区分「选择模式·拖选复制」与「按住 Shift·拖选复制」来源）。" },
      { category: "fix", text: "SSH 连接列表「编辑」按钮图标上下错位。该按钮漏了 `flex items-center gap-1`，svg 与文字未被 flex 约束导致换行堆叠；已与同组其他按钮对齐。" },
      { category: "fix", text: "收起按钮与设计稿不一致（样式 / 字形 / 位置）。改为带边框 ghost 按钮 + chevron 箭头（新增 `chevronLeft` 图标，展开按钮旋转 180°），并调整头部按钮顺序为「新建 → 收起」与设计稿一致。" },
      { category: "fix", text: "终端区域「雾蒙蒙」对比度不足。xterm 主题对齐设计稿调色板：主文本 `#d4d4d4`→`#e6e6e6`、green `#6a9955`→`#22c55e`、blue `#569cd6`→`#60a5fa`、magenta `#c586c0`→`#c4b5fd`，背景保持 `#1e1e1e`（已确认非蒙版/拉伸所致，ResizeObserver 早已处理画布重算）。" },
    ],
  },
  {
    version: "2.6.4",
    date: "2026-08-20",
    items: [
      { category: "feat", text: "关于页新增赞助开发者入口 + mcp_proxy 描述带上已挂载外挂工具名" },
      { category: "feat", text: "`mcp_proxy` 的描述会带上当前已挂载外挂 server 的工具名（如 `codegraph: codegraph_explore, codegraph_callers, ...`）。这不是给人读的文案，是喂给客户端工具检索的领域词：Claude Code v2.1.7+ 默认开启 Tool Search，工具描述不再常驻上下文而是按需检索，而 `mcp_proxy` 原描述里一个领域词都没有，模型查代码结构时根本检索不到它，外挂 server 等于隐身。没挂任何 server 时描述与改动前逐字相同" },
    ],
  },
  {
    version: "2.6.3",
    date: "2026-08-12",
    items: [
      { category: "fix", text: "成功的编辑不再顶着一句矛盾的提醒。上版让带 Windows 换行的替换能匹配了，但成功响应里还会带一句“含 N 个回车符……重试可避免匹配失败”，容易让人（和模型）以为没成功而白跑一轮" },
      { category: "improve", text: "补上真正的端到端回归测试。上版那条标着“回归”的单测只调了两次 `normalize_newlines` 断言字符串包含关系，把 `needle` 全改回 `f.old_string` 它照样全绿——等于零防护。现在在 `http.rs` 里真起服务、真读写文件，一次钉住三件事：CRLF 文件能匹配、写回仍是 CRLF、LF 文件不会被升成 CRLF" },
      { category: "improve", text: "归一化规则真正收口。上版声称“两边不可能再脱钩”，实际四个使用点只改了两个；`encode_text` 的 round-trip 守卫与 `write_files` 还各自内联着一份拷贝，而 `encoding.rs` 里那句“不要在别处再抄一遍”的注释在同一个文件往下 93 行就被自己违反了。现已全部改调 `normalize_newlines`" },
      { category: "improve", text: "纯 CR（经典 Mac）换行的行为从隐含前提变成明写契约：它会被转成 LF 且不还原（`crlf` 标志只看 `\\r\\n`），对这类文件改一个词会把全文行尾换掉。行为未变（该格式已基本绝迹，不值得为它多开一条还原路径），但现在有注释和测试钉着，日后想改必须先面对那条断言" },
      { category: "improve", text: "空白告警改为算在归一化后的 `oldString` 上（上一条修复的实现）" },
      { category: "improve", text: "更正 v2.6.2 的一条发版说明。原文把“空操作不再报成功”写成了修复旧缺陷，事实不是：老代码按原串比较 `oldString`/`newString`，`\"gamma\\r\\n\"` 与 `\"gamma\\n\"` 不相等会被放过，然后拿未归一化的原串去匹配、报 `oldString not found`——从不会假报成功。那道检查实际是为了堵住归一化自身新开出的口子。已将 2.6.2 那条从「修复」移到「技术细节」并改写；已发布的 GitHub Release 说明改不了，故在此留档 ---" },
    ],
  },
  {
    version: "2.6.2",
    date: "2026-08-12",
    items: [
      { category: "fix", text: "编辑 Windows 换行（CRLF）的文件时可能莫名报“找不到要替换的内容”。内容就在那里，只是行尾的表示方式对不上。现在两种行尾都能正常匹配，不用再猜" },
      { category: "fix", text: "一条会误导人的报错。替换内容里带回车符时，以前报的是“字符在当前编码下无法表示”——这个诊断是错的，会把人引到编码问题上去查。现在直接说清楚真实原因与怎么改" },
      { category: "improve", text: "根因：文件读进来就被归一化成 LF（`read_text`），而 `read_files` 又在内容旁边报 `newline: \"CRLF\"`——看到 CRLF 就在 `oldString` 里写 `\\r\\n`，而被匹配的文本里一个 `\\r` 都没有，于是必然失败" },
      { category: "improve", text: "`oldString` 现在按与文件内容同一套规则归一化再匹配。这不是“宽容”：匹配是查询，归一化只是把针和草垛放进同一个坐标系，零信息损失（“只匹配 CRLF 不匹配 LF”这个能力本来就不存在）" },
      { category: "improve", text: "`newString` 里的 `\\r` 则仍然拒绝，只是提前到碰文件之前并给真实原因：写入是改写，静默把调用方要求的 `\\r\\n` 换成别的东西，与本模块“宁可拒写也不静默损坏”的原则相冲" },
      { category: "improve", text: "归一化规则抽成 `encoding::normalize_newlines` 单一定义，`read_text` 与 `edit_files` 共用——两边规则一旦脱钩就会重现“看着一模一样却匹配不上”，现在物理上不可能" },
      { category: "improve", text: "写入行为完全未变：CRLF 文件改完仍是 CRLF（回写时按探测到的行尾还原）" },
      { category: "improve", text: "同时加了一道防回归检查：归一化后的 `oldString` 若与 `newString` 相同，在碰文件之前就拦下。这不是修旧缺陷，而是归一化本身会新开出的一个口子：两边归一化后变得相同时，替换会“成功”但文件一字未变 ---" },
    ],
  },
  {
    version: "2.6.1",
    date: "2026-08-08",
    items: [
      { category: "fix", text: "mac 版发不出来。v2.6.0 的 mac 构建挂在一条测试上：导入时给项目取名的那段逻辑跟运行平台绑死了，在 mac / Linux 上会把一整条 Windows 路径当成项目名。现已改成与平台无关（Windows 用户不受影响，v2.6.0 的 Windows 包是完整可用的）" },
      { category: "feat", text: "导入时能看清每个工具到底是干什么的。导入向导里每条候选多了一个「运行一下」按钮，就地列出它提供哪些工具（名字 + 一句话）；设置页的服务行也可以点开看同一份清单。跑一次就够：导入后不需要对同一个工具再探一次" },
      { category: "improve", text: "启用前的风险确认框会列出它要交给远程的工具。原先只写着“远程将获得这个 server 的全部能力”，却从不说那些能力具体是什么——而启用才是真正交出执行权限的那一步" },
      { category: "improve", text: "`project_label` 不再用 `Path::file_name()`：那个方法描述的是「本机文件系统」的语义，而这里处理的是「别的机器写进 JSON 的字符串」。Unix 上 `\\` 不是分隔符，`D:\\work\\myapp` 会被整串当成一个组件（解出 `d--work-myapp`）。Windows 两种分隔符都认，所以本机永远测不出来" },
      { category: "improve", text: "新增 `mcp_bridge_inspect`：与 `mcp_bridge_probe` 并列为仅有的两个会启动子进程的命令，扫描本身仍是零进程。它没有二次确认框（列表行里已逐字展示完整命令），因此两处补偿不能省：按钮文案直说是“运行”、本次执行进审计" },
      { category: "improve", text: "inspect 结果直接写 `mcp_manifest`，所以导入后设置页天然命中；没导入就成孤儿行，由扫描时顺手跑的 `purge_orphans` 兜底（7 天、零定时器，与 `sweep_idle` 同思路）" },
      { category: "improve", text: "工具清单抽成共用的 `ToolList`，四处 import 同一个；`ImportWizard` 拆出 `CandidateRow`（规则 7）" },
      { category: "improve", text: "以工具清单而不是 `instructions` 为主：后者是 MCP 的可选字段，很多 server 不提供，而工具描述是必填项 ---" },
    ],
  },
  {
    version: "2.6.0",
    date: "2026-08-07",
    items: [
      { category: "feat", text: "外挂 MCP 桥：把本机已装的任意 MCP 工具转给远程用。设置页新增一张卡，可从 Claude Code / Cursor / Claude Desktop 的现有配置一键导入，逐个启用。新接一个工具不需要改任何代码，也不为任何具体工具写适配" },
      { category: "feat", text: "支持多个项目。给某个工具开启「允许远程指定工作目录」后，远程每次调用可以指定一个目录，多个项目同时用与轮流用都支持；闲置一段时间后自动回收" },
      { category: "feat", text: "导入向导会跳过已经导入过的。设置页里已有一模一样的一条时置灰标注，不再重复列出；同名但配置不同的仍然可以导入，不会被当成重复项吃掉" },
      { category: "sec", text: "总开关默认关闭，每个工具还得单独启用，且启用前弹风险确认。确认框里逐字列出它实际要启动的完整命令与参数——比如某个工具末尾那个 `D:`，就是你要交出去的范围。导入不会自动启用任何一条" },
      { category: "sec", text: "外挂工具不受 cc-bridge 路径白名单约束，这件事写在了界面上。它们是独立进程，cc-bridge 能记下转发了什么，但对方进程内部读写了哪些文件是黑盒——弹框里直说了，没用“已安全防护”这种说法粉饰。开启「允许远程指定工作目录」同样需要二次确认，白名单在那里只能管住“从哪个目录启动”，管不住它起来后能碰什么" },
      { category: "sec", text: "密钥只进不出。界面、审计日志、导入预览都只出现环境变量的键名；外挂配置也不参与配置的导出与导入（导出一份能四处传的文件不应携带执行通道，导入也不能绕开二次确认）" },
      { category: "fix", text: "单次调用超时不再销毁整条连接。大项目上首次调用可能超过一分钟，原先每次超时都重建、于是每次都从冷启动重来、永远收敛不了；现在超时只失败这一次，已加载的东西都留着，直接重试即可" },
      { category: "fix", text: "国内加速镜像失败时不再静默报成功。之前镜像推送失败时发版仍一路绿灯，导致国内下载与更新通道已经不可用却一直没人发现（影响近四个版本）；现在只有真正推送成功、并且回头验证过“客户端能读到本次版本”才算成功" },
      { category: "fix", text: "连接页首次接入指引指错了地方。那句话是卡片还是展开式时写的，照着往下找只能看到几行折叠标题；它只在“远程从未调用过”时出现，恰好只有第一次用的人会撞上" },
      { category: "improve", text: "连接页的传输方式与 Bearer 状态并进了地址行，不再单占一行（现为 `192.168.1.7 : 7823 · HTTP · Bearer`）。那句话在正常态几乎永远不变，不值一整行；而服务状态卡跨两行，它矮一点，右侧六张卡跟着一起矮" },
      { category: "improve", text: "不用 `rmcp`，自写 stdio JSON-RPC 客户端：它的 `TokioChildProcess` 自己管子进程（`kill_on_drop`），拿不回进程树控制权。实测确认：直接杀子进程会留下孤儿，而先关 stdin（让它读到 EOF 自己退）能退干净，JobObject / 进程组只作兜底" },
      { category: "improve", text: "Windows 下命令解析自己实现了 PATHEXT 查找：Rust 的 `Command` 不查 PATHEXT，`codegraph`、`npx` 这种裸名字直接报“找不到程序”。真机测试又发现：npm 会在 `.cmd` 旁边装一个无扩展名的 sh shim，而 Windows 根本执行不了它，所以解析须按 cmd.exe 的语义来（裸名字只拼 PATHEXT，不试原名）" },
      { category: "improve", text: "工具清单持久化到 SQLite（`mcp_manifest` 表），打开设置页与远程列工具都不启动任何子进程。启进程只能由「探测」按钮显式触发，且拓完就关（不进连接池）" },
      { category: "improve", text: "指纹存规范化串而非哈希：`DefaultHasher` 官方明确不保证跨版本稳定，存进库里会在升级 Rust 后集体失效；存串还能直接看出“到底是哪一项变了”" },
      { category: "improve", text: "配置只能经本机面板改（八个 Tauri 命令，一个都不注册为 MCP 工具）：能改它等于能以本机用户身份执行任意程序。也没走 `save_config` 的 patch（那是整数组替换，后端分不清“改了一条”与“塞进来五条新的”，逐次审计写不出来）" },
      { category: "improve", text: "握手失败时不会残留子进程：进程已启动但握手超时 / 拿不到管道时，会亲手杀掉它。我们的 JobObject 是 `kill_on_drop = false` 建的，靠 drop 收不了尾，漏一次就残留一棵进程树" },
      { category: "improve", text: "cc-bridge 不能桥接自己：判定靠可执行文件的真实路径，不靠名字（名字是用户随便取的，既会误判也会漏判）" },
      { category: "improve", text: "只读模式下转发被整体拦住：cc-bridge 无法知道外挂工具到底读还是写，因此一律当写操作处理" },
      { category: "improve", text: "应用退出时会把所有外挂子进程一并关掉；总开关关闭、单条停用、改启动参数也各自断开对应连接" },
      { category: "improve", text: "外挂 MCP 配置不参与配置导出 / 导入，两条回归测试盯着 多项目支持" },
      { category: "improve", text: "连接池的键从 `server` 改成 `(server, cwd)`。跟着变的比看上去多：停用 / 删除 / 改启动参数时必须摸掉全部 cwd 变体（只关默认那一个的后果是：界面写着已停用，它在其它项目上的 N 个进程还在跑）；而连接中毒时只能摸掉出问题的那一个，不能把其它项目健康的会话连坐" },
      { category: "improve", text: "失败退避也改成按 `(server, cwd)` 记：否则 A 项目启动失败（比如那个目录根本没建索引）会把 B 项目一起退避掉 30 秒" },
      { category: "improve", text: "空闲回收用惰性做法：在取会话与设置页列表两个入口顺手扫一遍，零定时器、零常驻线程。已知缺陷：没人调用就不会扫，用完放着不管的话进程会活到下次调用或应用退出——拿几个闲置进程换零后台任务，这个交换是故意的" },
      { category: "improve", text: "远程“好心”把默认目录显式传一遍时不会多起一个进程（两者归一化成同一把键）" },
      { category: "improve", text: "`mcp_list_servers` 会告诉模型哪些 server 接受 `cwd` 及可用的白名单根目录——不告诉它就不会用，不列根目录就只能靠猜路径" },
      { category: "improve", text: "未开子开关却传了 `cwd` 会报错而不是静默忽略：静默忽略会让模型以为切到了 A 项目、实际查的是 B，而两边都返回了看起来正常的结果" },
      { category: "improve", text: "关掉子开关时会把该 server 的所有会话一并断掉：那些用远程指定目录起来的进程，是在一个现已撤销的授权下启动的" },
      { category: "improve", text: "设置页每行显示当前活着几个实例（「运行中：N 个目录」），否则用户不知道自己开了几个进程" },
      { category: "improve", text: "导入去重的判定是同名 + 同配置，不是只看名字；且刻意不比 `enabled` 与“允许远程指定目录”两个开关——比了的话，用户一启用它就会重新冒出来当成可导入项，恰好把这个修复抵消掉 超时处理" },
      { category: "improve", text: "超时后保留连接是安全的：请求 id 单调递增且串行（方法都取 `&mut self`），所以被放弃那条的响应迟到时 id 必定小于下一个期待值，会被按 id 丢掉。原方案担心的“拿到别人的结果”需要 id 撞车，而这里撞不上。真正没救的情形（读线程报错、server 进程退出、写管道失败）仍然销毁连接 Gitee 镜像" },
      { category: "improve", text: "镜像步骤的“可选”改用 `continue-on-error` 实现：原先靠 `try/catch` 里的 `exit 0`，但 PowerShell 的 `catch` 接不到 git 这类原生命令的非零退出码（已实测：native 失败不抛、cmdlet 失败才抛），于是失败要么被 `catch` 吐成绿灯、要么让整个发版变红同时还打印成功日志" },
      { category: "improve", text: "每个 git 调用后显式判 `$LASTEXITCODE`；失败带原因 `exit 1`（步骤标红可见，但不拖累已发布的 GitHub Release）" },
      { category: "improve", text: "新增发布后可用性检测：直接请求客户端实际使用的 raw 地址（跟随跨域 302），并比对 manifest 的 version 是否等于本次 tag —— 光看 200 不够，推失败时旧 manifest 也是 200，而客户端把 Gitee 排在首选，读到旧版本会认为“已是最新”而不回退问 GitHub，造成国内用户静默看不到新版" },
      { category: "improve", text: "新增诊断输出：目标仓库、待传文件清单与总体积、pack 体积、HTTP 层详细日志（`GIT_CURL_VERBOSE`）、每次 push 的耗时/均速/退出码；失败后自动用 `http.postBuffer=500MB` + `--no-thin` 重试一次，关键数字同时写成 annotation" },
      { category: "improve", text: "背景数据（PastePanda 同步实测）：GitHub runner → gitee.com 上行约 35KB/s，7.30MB 用 216 秒推成功、而 7.66MB 在 262 秒被 `curl 55` 重置，8MB 跑 25 分钟未完 —— 体积已卡在临界点；cc-bridge 还要同时推 Windows exe + mac tar.gz，风险更高 连接页" },
      { category: "improve", text: "地址行直接靠原有的 `truncate` 降级，没拆 flex 子项：它的降级方向正好是对的——宽度不够时先吃掉尾部的元信息，地址（用户要复制的那串）保住。元信息不跟 `font-mono`：等宽字体下 CJK 既偏宽也难看（与「最近活动」同一理由），顺带也拉开了层次" },
      { category: "improve", text: "首次接入指引文案抽成 `FIRST_RUN_HINT` 常量：它原本在 `serviceChain.ts` 里写了两遍（含防火墙段 / 不含各一份），日后只改一处就会不一致；常量的注释里钉了一句——文案里那两个标签名必须与 `ConnectTab` 的实际 UI 对得上" },
    ],
  },
];
