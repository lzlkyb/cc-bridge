// AUTO-GENERATED by scripts/gen-changelog.mjs — DO NOT EDIT.
// 数据源：根目录 CHANGELOG.md（Keep a Changelog），唯一手写源；predev/prebuild 自动重新生成。
import type { ChangelogEntry } from "./about";

export const CHANGELOG: ChangelogEntry[] = [
  {
    version: "2.4.2",
    date: "2026-08-05",
    items: [
      { category: "fix", text: "macOS 上 CPU 一直占着一个核心跑满（mac 版自首发以来一直存在）。本机地址变化的监听任务在 mac 上陷入了空转，并且每转一圈都去重新枚举一遍网卡。Windows 不在此影响范围" },
      { category: "fix", text: "顺这个问题一并修好了一件 mac 上本来就不对的事：地址变化时的托盘悬浮提示与桌面通知。它们原本挂在上述那个空转的任务上，在 mac 上“能用”仅仅是因为它在烧 CPU；现已改由应用自带的 5 秒轮询负责，不再依赖系统级事件。界面上的地址变化提醒（顶部徽章 / 横幅 / 连接页提示）从未受此影响" },
      { category: "improve", text: "5s 轮询 task：主力，所有平台都跑全套 → 能力不再依赖 OS 事件。" },
      { category: "improve", text: "事件 task（实际仅 Windows 生效）：降为低延迟加速器，收到通知就立即跑一轮，不用等下个 tick。 `alerting` 从局部 `let mut` 改成两个 task 共享的 `Arc<AtomicBool>`，否则 Windows 上事件 task 弹完通知、轮询 task 5s 后看到自己那份还是 `false`，会把同一次地址变化重复弹一遍。用 `swap()` 取边沿，只在「从未变到已变」时弹。 对 Windows 的行为变化：轮询 task 现在也会每 5s 刷一次托盘。不会引起图标闪烁——`refresh_tray` 本身就带去重（`last_tray` 缓存，同状态直接返回），那本来就是为修图标闪烁加的。 尚未做的：消费循环仍内联在 `main.rs` 的 `setup` 闭包里，无法直接单测“sender 掉了就退出”。抽成独立函数是对的，但那是重构，不应与一个线上 100% CPU 故障的修复混在一起，留作单独一轮。 最后一批搬出剩下的 1119 行，`commands.rs` 只剩 48 行的模块聚合（含 26 行头注释）， 零实现代码。全过程 `main.rs` 的 `invoke_handler!` 42 项一个字未改。 本批新增 5 个文件：`config_cmds.rs`（745）· `running.rs`（166）· `system.rs`（150）· `server.rs`（86）· `audit_cmds.rs`（68）。 config 与 config_io 最终合成一个文件（而非规划里写的两个），理由是测试的归属： 那两条单测一条测 `switch_root_profile`（config）、一条测 `import_config`（config_io）， 且共用同一个 `SEQ` 计数器与临时目录搭建。拆成两个文件就得把测试模块也劈开、 复制那套 setup——收益不抵代价。✅ 测试路径现已变为 `commands::config_cmds::tests::…` 且两条均通过，证明它们真的跟着被测代码走了—— 其中 `switch_root_profile_refreshes_cached_roots` 是安全回归测试（切组后漏刷 `canonicalized_roots` 是安全缺陷，不是显示问题），留在聚合文件里就断了守护。 本批只报了 1 条错，但很典型：`running.rs` 的 `Ordering` unused——`get_command_output` 函数体内部自己就有一份局部 `use`，而我按“扫到 Ordering 就引一下”又在顶部加了一份。 局部 `use` 这种写法靠顶层扫描看不出来，只能靠 `-D warnings`。 四批总计：2700 行 → 9 个子模块 + 48 行聚合。指纹校验全程证明原有 75 项逐字未变 （最终差异列表里没有一条 `<`，新增 18 项正好是 9 个 `mod` + 9 个 `pub use`）。 安全函数 `assert_backup_path_in_scope` 始终私有，暴露面未扩大。 搬出 514 行，`commands.rs` 1675 → 1167 行（累计从 2700 降 57%）。" },
      { category: "improve", text: "`commands/status.rs`（439 行）——`get_status` 与它的四个响应结构体。只有 1 个命令却占 416 行： 结构体占一半，剩下是聚合逻辑（配置快照 / 统计 / 限流 / LAN 地址 / 工具计数 / 防火墙缓存 / 平台标识）。 前端每 5s 轮询它，所以它同时是性能话题（清单 M2 / M14）的主角——单独成文件后再动它不会再摊到大文件。" },
      { category: "improve", text: "`commands/firewall_cmds.rs`（117 行）——刷新缓存 / 一键放行 / 诊断三个命令。命名同样要避开 `crate::firewall` 与 `crate::firewall_diag` 两个已有模块（与第 2 批 `backup_cmds` 同一理由）。 这三个命令是 Windows 专属能力，但函数本身不带 `cfg`——平台差异在 `crate::firewall` 内部处理， 前端则按 `platform` 字段隐藏整张卡片（清单 N4），所以本次无需处理 cfg 分支。 本批编译报了 5 条 unused import，全是随 status 域走后失效的：`Ordering` / `Duration` / `Instant` / `TcpStream` / `timeout` / `crate::backup`——`get_status` 是它们在 `commands.rs` 里的唯一用户 （轮询计时、TCP 探活、备份统计）。两批下来已能看出规律：每拆一个域，`commands.rs` 的 imports 就瘦一层，而 `-D warnings` 会把漏删的那几条逐一点名。 四道校验全过：指纹原有 75 项逐字未变（新增 8 项全是 `mod` 与 `pub use`）· 双口径 clippy 干净 · `cargo test --no-default-features` 160 + 4 passed · `main.rs` 仍零改动。 搬出 737 行（10 个命令），`commands.rs` 2409 → 1675 行（累计从 2700 降 38%）。 这一域在原文件里被 running_commands 割成了两段（原 1052–1314 与 1687–2160），本次合为一处， 两段相对顺序保持不变以便与原文件对照。 文件名叫 `backup_cmds` 而不是 `backup`：`crate::backup`（`src/backup.rs`）已存在，而 `commands.rs` 顶部有 `use crate::backup;`。子模块同名会与它冲突——幸好是编译错而非静默覆盖， 但没必要赌这个。 本批证明了那套校验真能抓东西——编译报了 3 个错，全在规划时列出的风险类别里： 1. `E0599: no method named creation_flags`——`reveal_backup_dir` 的 Windows 分支要用 `Command::creation_flags`（CREATE_NO_WINDOW，不加会闪黑框），它依赖的 `use std::os::windows::process::CommandExt;` 带着 `#[cfg(windows)]`，搬动时漏了。 补上时必须同样 cfg 门控：不门控则本机 clippy 过、mac clippy 红，而 mac 那边本地看不到。 2. `unused import: std::path::PathBuf`——随备份域走了，`commands.rs` 里只剩测试模块在用 （那里自带 `use`）。 3. `unused import: similar::TextDiff`——彻底无人用。 ✅ 安全边界保住：`assert_backup_path_in_scope`（canonicalize 后做前缀校验，杜绝用备份通道 越权读写任意文件）拆后仍是私有的——它的 7 处调用全在本模块内，因此不需也不得改成 `pub` （规则 7：安全模块不得削弱）。 四道校验全过：指纹原有 75 项逐字未变（新增 4 项全是模块声明）· 双口径 clippy 干净 · `cargo test --no-default-features` 160 + 4 passed · `main.rs` 仍零改动。 `commands.rs` 从立项时的 1556 行长到了 2700 行（42 个 IPC 命令）。本批搬出 297 行，降至 2409 行。 为何拿 update 打头阵：它的 4 个私有辅助（`retry_with_backoff` / `resolve_update_endpoints` / `candidate_endpoint_groups` / `build_updater`）全部只被本模块调用，与其它域零耦合。拿它来验证 「`pub use` 重导出 + `main.rs` 的 `invoke_handler!` 零改动」这套机制——机制若不成立，42 项命令 会同时报错，在这里暴露远比在大搬动里暴露好查。结果：机制成立，`main.rs` 一个字未改。 新增 `tools/fingerprint.py`——纯搬动重构的等价性校验器。 为何需要它：`commands.rs` 自己 只有 2 条测试（覆盖 42 个命令中的 2 个），所以「`cargo test` 全绿」根本不能证明命令没坏； 而大搬动的 diff 也无法逐行审。它按「列 0 出现的顶层项」切块，对每块规范化后算 md5： 搬动只改变块的所在文件与顺序，不改内容，所以哈希集合必须逐字相同。 校验器本身先做了三个对照实验（一个抓不到改动的校验器毫无价值）：① 把 update 那 297 行整块 搬到文件开头 → 0 差异；② 偷改退避基数 `1u64 <<` → `2u64 <<`（一个字符）→ 精确只报 `retry_with_backoff`；③ 搬动与篡改同时发生 → 仍只报那一条。 首次实战就抓到了一个校验器自己的缺陷：`ITEM` 正则只把 `///` / `//!` 当块起点，漏了列 0 的 普通 `//` 注释，于是 `// ===== 自动更新 =====` 这类区段分隔注释被并进了前一个函数的块， 搬动后 `import_config` 与 `check_update` 哈希无故变化。已修正正则并在注释里记下这个坑。 本批四道校验全过：① 指纹 75 项逐字一致（差异仅新增 `mod update;` 与 `pub use update::*;` 两项）； ② `clippy --all-targets -D warnings` 与 `--no-default-features`（mac CI 口径）均干净； ③ `cargo test --no-default-features` 160 + 4 passed；④ 私有辅助未被改成 `pub`，暴露面未扩大。" },
    ],
  },
  {
    version: "2.4.1",
    date: "2026-08-05",
    items: [
      { category: "feat", text: "白名单可按项目切换：安全页的「白名单根目录」标题上多了个配置组胶囊，可为不同项目各存一套目录，点开即切；支持新建（可从当前组复制）、重命名、删除。升级后现有目录自动归入一个「默认」组，可访问范围与升级前完全一致" },
      { category: "feat", text: "备份可以手动清理了：备份卡新增清理入口，按时间（早于 7/30/90 天或自定义）、按体积（清到 100MB/300MB/1GB 以下）或全部清空；删前先给预览——将删多少份、释放多少、清理前后各占多少，且默认开启「至少保留每个文件最近一份」这条底线。版本历史里也能单独删某一份快照，或一键删掉某个文件的全部备份" },
      { category: "feat", text: "设置项可以搜了：命令面板（Ctrl / ⌘ + K）新增「设置」组共 20 项，回车直接跳到对应开关并高亮；搜的是关键词而不只是标题——搜「rce」能找到命令执行，搜「GBK」能找到编码自适应，搜端口号能找到监听端口" },
      { category: "improve", text: "设置页重新组织：原先装了 13 个控件的一张「功能开关」拆成安全、备份与审计、通知、高级四张卡，整页按「风险 + 改动频率」重排；关联参数收进对应开关下方作为子项，开关关掉时自动置灰（以前开关在第 4 张卡、它的参数在第 8 张）。网络卡等多处去掉了常驻的警告与占位文字，只在真正需要时才出现" },
      { category: "improve", text: "开窗不再是一片空白：界面加载期间先显示与真实布局同构的骨架屏，并直接画出应用图标（开启「关窗时释放界面内存」后，从托盘重新打开面板需要 1~2 秒重新加载）；界面顶部与关于卡的图标也不再发虚" },
      { category: "improve", text: "应用名与图标统一：以前界面顶部叫「CC Bridge」，托盘悬浮提示、桌面通知标题、任务切换器里的窗口名却是小写写法，同一个应用两个名字，现在凡是给人看的地方一律统一。macOS 的应用图标也补上了新 logo——此前换 logo 时漏了它，mac 用户在 Dock 和访达里看到的一直是完全不同的旧图" },
      { category: "fix", text: "「删除早于 N 天」之前算错了时间：备份文件的修改时间继承的是源文件的，于是三年没动过的文件、今天刚建的备份会被当成三年前的删掉；按体积清理的「最旧优先」同样受影响，会优先删掉那些只有一两份、最珍贵的历史。现已改为按备份的创建时间判定。清理时还会顺手清除绕过面板、直接到文件夹里手删备份所遗留的无效记录" },
      { category: "fix", text: "「关窗时释放界面内存」之前形同虚设：点标题栏的关闭按钮（绝大多数人的关窗方式）实际只是隐藏窗口，界面进程一直挂着、内存根本没降，现已真正生效。同时修掉关窗再开后 Win11 上窗口圆角变直角、以及 macOS 上关窗后点 Dock 图标没反应只能从菜单栏唤回" },
      { category: "fix", text: "托盘图标左键单击会弹出右键菜单：Windows 上现在左键只开/收面板、右键才出菜单，macOS 按菜单栏惯例保持左键开菜单。另外审计日志的「保留天数」现在会持续生效（此前只在启动时清理一次，长期不重启就永远不清），深色主题下开窗不再先闪一下白屏，设置页的「关于」卡也不再每次切回来都自动展开" },
      { category: "improve", text: "白名单可按项目切换：安全页新增「配置组」，可为不同项目各存一套根目录，一下拉切换；支持新建（可从当前组复制）、重命名、删除。升级后现有目录自动归入一个「默认」组，可访问范围与升级前完全一致" },
      { category: "improve", text: "备份可以手动清理了：备份卡新增「清理备份…」，按时间（删除早于 7/30/90 天或自定义）、按体积（清到 100MB/300MB/1GB 以下）或全部清空；删前先给预览（将删多少份、释放多少、清理前后占用），且默认开启「至少保留每个文件最近 1 份」底线。之前面板上没有任何删备份的入口，只能自己打开文件夹手删" },
      { category: "improve", text: "版本历史里可以删备份了：每份快照可单独删，分组头可一键删该文件的全部备份" },
      { category: "improve", text: "设置页审计区新增「清理早于…」：可按临时天数（7 天前 / 30 天前 / 自定义）清一次，不影响上面的保留天数设置" },
      { category: "improve", text: "设置项可搜索了：`Ctrl/⌘ + K` 命令面板新增「设置」组（共 20 项），回车直接跳到对应开关并高亮；支持搜关键词而非只搜标题（搜「rce」找命令执行、搜「GBK」找编码自适应、搜「7823」找端口）" },
      { category: "improve", text: "开窗不再是一片空白：界面加载期间先显示与真实布局同构的骨架屏（开启「关窗时释放界面内存」后，从托盘重新打开面板需要 1~2 秒重新加载） 变更" },
      { category: "improve", text: "设置页重新组织：原来装了 13 个控件的「功能开关」拆成「安全 / 备份与审计 / 通知 / 高级」四张卡；卡片按「风险 + 改动频率」重排（关于 → 网络 → 防火墙 → 安全 → 备份与审计 → 通知 → 高级 → 应用 → 安装 → 配置），风险总览提到页面顶部" },
      { category: "improve", text: "命令执行的「壳层」选择现在紧跟在「命令执行」开关下方作为子项，开关关闭时自动置灰（之前隔了 3 个开关，关掉命令执行后壳层依旧可改）" },
      { category: "improve", text: "审计的「保留天数」与「清理早于…」现在与「审计日志」开关同卡，开关关闭时置灰（之前开关在第 4 张卡、参数在第 8 张）" },
      { category: "improve", text: "「MCP 传输协议」从「功能开关 › 兼容与性能」移到「网络」卡（它决定远程怎么连，与监听端口同类）" },
      { category: "improve", text: "去掉两处多余的常驻提示：设置页「配置」卡里常驻的「导入将覆盖所有当前配置」警告（导入时的确认弹窗说得更全）；安全页「文件管控」卡底部重复的「所有设置修改后自动保存」（同一张卡顶部已说过）" },
      { category: "improve", text: "网络卡瘦了一圈（约 269px → 195px）：「修改端口将重启服务」的警告改为只在真正改了端口时出现（以前常驻，没改时也占一大块）；删掉按钮旁常驻的「无更改」文字（改为悬停提示）；传输协议的说明收成一行" },
      { category: "improve", text: "导入配置时，旧版本导出的配置文件不再报错：缺少的字段自动取默认值" },
      { category: "improve", text: "审计日志的「保留天数」现在真的会持续生效：除启动时清理外，之后每 24 小时自动清理一次（之前只在启动时跑，长期不重启就永远不清）；改完保留天数也会立即清理一次，不必重启" },
      { category: "improve", text: "设置页审计区新增「立即清理」按钮，并会告知清了多少条（原先手动只有「清空全部」这一个选项，没有中间档）" },
      { category: "improve", text: "白名单卡的「配置组」从卡内通栏一条改成标题行上的一个胶囊（`默认 · 3 个目录 ▾`），目录列表上提 60px；点胶囊才展开组列表。原来那个常驻的「管理」按钮去掉了：面板本身就是用户主动点开的，重命名 / 删除直接列在行上，不再套一层开关" },
      { category: "improve", text: "应用名统一：以前界面顶部写「CC Bridge」，而托盘悬浮提示、桌面通知标题、Alt+Tab 里的窗口名写「cc-bridge」，同一个应用两个名字；现在凡是给人看的地方统一为「CC Bridge」。文件名、安装目录、开始菜单条目、防火墙规则名、以及远程 `claude mcp add cc-bridge` 用的名字均不变——改它们会让老版本升级后防火墙规则指向旧路径、远程直接连不上" },
      { category: "improve", text: "macOS 的应用图标终于换成新 logo：2026-07-09 那次换 logo，17 个图标文件里单单漏了 `icon.icns`（它与当时的备份 md5 完全相同，其余 16 个全重新生成过），于是 mac 用户 Dock / 访达里看到的一直是旧 logo——它不只是旧版本，而是完全不同的一张美术（旧的平均色 (91,88,223) 偏紫、四边等边距居中；新的 (58,122,229) 偏蓝、重心偏上）。Windows 链路（exe 内嵌图标 / 安装包 / 任务栏 / 界面 logo）实测本来就全一致，不受影响" },
      { category: "improve", text: "界面图标不再模糊：顶部和关于卡的 logo 一直在用一张 32×32 的图按 40×40 显示（`public/icon.png` 与 `icons/32x32.png` 字节完全相同），缩放 150% 的屏幕上等于用 32 像素铺 60 个物理像素；现换成 128×128" },
      { category: "improve", text: "开窗首帧的骨架屏现在直接画真 logo，不再是一个灰方块（图是静态文件，不用等 JS 加载），看着像「应用已经在了」而不是「还在加载」；图没加载出来时仍回退到灰方块，不会变成破图标" },
      { category: "improve", text: "文档：README 与 GitHub 仓库 About 改成双平台叙述。v2.4.0 已支持 macOS，但开头一句、「这是什么」、两张架构图、技术栈、快速开始都还写的“本地 Windows”，mac 仅以一条脚注存在；现在 mac 与 Windows 同级列出（带下载对照表），About 也不再是直接粘的一段 markdown 原文（那个字段不渲染 markdown，``、反引号会原样显示），同时补上主页链接与 9 个 topics" },
      { category: "improve", text: "文档：README 终于给出 Gitee 镜像的地址：之前只写「国内用户建议走 Gitee 镜像」却从未给链接，读者无处可找；同时说清那里只有 Windows 的 exe 与 mac 的 `.app.tar.gz`（没有 mac 首次安装用的 zip），以及安装后的自动更新本来就是 Gitee 优先、失败才退 ghproxy/GitHub" },
      { category: "improve", text: "文档：修正 README 里四处与代码不符的描述——安装包体积 3.4MB（实测 v2.4.0 为 4.48MB，mac zip 5.74MB）、`stop_command` 写的 taskkill 整树（实际是 Windows TerminateJobObject / mac SIGKILL 进程组，与工具描述早已修正的一致）、已知限制里“不支持 SSE”与“无跨调用 shell 会话”（两者都已实现，后者默认关闭）；工具表补上漏写的 `push_notification`，计数 17 → 18 修复" },
      { category: "improve", text: "托盘图标单击（左键）会弹出右键菜单：Windows 上现在左键只开/收面板、右键才出菜单；macOS 按菜单栏惯例保持左键开菜单（不再同时切窗口）" },
      { category: "improve", text: "深色主题下开窗不再先闪一下白屏" },
      { category: "improve", text: "Win11 上关窗再开后窗口从圆角变成直角（仅开启「关窗时释放界面内存」时出现）" },
      { category: "improve", text: "macOS 上关窗后点 Dock 图标没反应，只能从菜单栏图标唤回" },
      { category: "improve", text: "「删除早于 N 天」之前算的时间是错的：备份文件的修改时间继承的是源文件的，于是「三年没动过的文件」今天刚建的备份会被当成三年前的删掉；按体积清理的「最旧优先」也会变成优先删那些只有一两份、最珍贵的历史。现已改为按备份创建时间判定" },
      { category: "improve", text: "清理备份时会顺手修掉孤儿索引：历史上绕过面板、直接到文件夹里手删的备份会在数据库里遗留指向不存在文件的记录，现已自动清除" },
      { category: "improve", text: "「关窗时释放界面内存」开关之前形同虚设：点标题栏的关闭按钮（绝大多数人的关窗方式）实际只隐藏窗口，界面进程一直挂着，内存根本没降。现已修正，开关真正生效" },
      { category: "improve", text: "设置页的「关于」卡片之前每次从其它页签切回来都会自动展开并滚到更新历史，现在只在真的有新版本红点时才展开 设计稿：`design/白名单配置组入标题行-设计稿.html`（方案 B）。 能搬的前提：`SecurityTab.tsx` 那个 `<div className=\"sec-grid\">` 的 class 全项目无定义（CSS 里搜不到，只此一处使用），像是两列实验回退后留下的死类——也就是说两张卡一直是通栏堆叠，标题行宽度管够。本次顺手删了这个空 div。 不把原来那整条（标签 + 选择器 + 管理按钮，约 213px）直接搬上标题行，是因为那样标题行总宽会到 ~613px（标题 128 + 徽章 71 + 配置组 213 + 搜索 165 + 间距 36），而 `CardHeader` 本身带 `flex-wrap`：窗口缩到最小（minWidth 560 → 卡宽 520）时会折成两行，省下的 60px 又还回去了。胶囊约 110px，总宽降到 ~510px，任何宽度下都是单行。 拆成 `RootProfilePill`（标题行）+ `RootProfilePanel`（CardContent）两个文件，而不是一个组件：两者不在同一 DOM 位置，没法同时渲染到两处。共享的只有「是否展开」这一个状态，提到 `SecurityTab`；面板自己那堆状态（新建 / 重命名 / 删除确认 / busy）仍在面板内。也因此去掉了 `managing` 态：面板已经是用户主动点开的，再套一层「管理」开关没意义；行内切换点的是组名那个 `button`，与重命名 / 删除是并列兄弟节点，不会误触。 面板不给 margin：它是 `CardContent`（`space-y-3`）的首个子节点，间距由 `space-y-3` 给后一个兄弟上 `mt-3` 完成。展开面板仍渲染在流内（不做浮层），与原实现一致，风险最低。 图片隔着终端无法目测，所以解 PNG 的 alpha 通道，算三个跟尺寸无关的指纹：四周留白比例、不透明像素重心、alpha 加权平均色。同一张美术缩放后这三项基本不变，不同美术一眼分开。 靠它确认的事实：从注册表找到已安装位置（`D:\\soft\\cc-bridge\\cc-bridge-desktop.exe`）、抽出 exe 内嵌图标，与界面 logo、`icon.ico` 各层、全部 PNG 逐一对得上（avg≈(58,122,229)）；而 `icon.icns` 内部 12 个条目全指向另一张图（avg=(91,88,223)、重心 0.50 正中、四边留白 0.035）。 重建时不做任何缩放重采样，直接拿仓库里 tauri icon 已生成的 32/64/128/256/512 原图装成 7 个 PNG 型条目（ic11 / ic12 / ic07 / ic13 / ic08 / ic14 / ic09）——比自己临时写个重采样器可靠。故意不带两档：`ic10`（1024）与 `icp4`（16）仓库里没有源图，512→1024 上采样会糊，而让 macOS 从相邻档自己缩放比硬造更好；旧文件里的 `is32/il32/s8mk/l8mk` 是 Mac OS X 10.7 之前的位掩码遗留格式，现代 macOS 不再读，也不带。115832 B → 182705 B。 写完回读校验：magic 为 `icns`、头部声明长度与实际文件长度一致、链式条目正好走到文件尾不多不少、每层重解后指纹均为新美术。 未在真实 mac 上验过：本机是 Windows，只能保证文件结构合规、各层图正确，Dock 里的实际观感需到 mac 上看。 另：`bundle.icon` 列表里没有 `icons/icon.ico`，只有三个 PNG 加 `icon.icns`。Windows 之所以一直正常，是因为 `tauri_build::build()` 默认就去拿 `icons/icon.ico`、绕过了这份列表——也就是说在修之前，这份列表里唯一真被用到的正是那个过期的 icns。本轮只换了 icns 内容，没动这份配置（Windows 发版路径已稳定，不拿它做实验），但它是个值得后续补上的显式声明。 两个各自正确的修改叠加出的缺陷。" },
      { category: "improve", text: "根因：`TitleBarControls.tsx` 的关闭按钮调的是 `getCurrentWindow().hide()`，而「销毁还是仅隐藏」的判断只写在后端 `CloseRequested` 里。`hide()` 不触发那个事件，所以无论开关怎么设 webview 都不会被销毁。当初把托盘左键「收起」改成 `w.close()` 时就是为了「判断只写在一处」，但漏改了标题栏这个主要入口，那行旧注释「关闭 = 隐藏到托盘（与 CloseRequested → hide() 一致）」也没跟着改，反过来掩盖了问题。" },
      { category: "improve", text: "修法：`hide()` → `close()`。必须同时在 `capabilities/default.json` 加 `core:window:allow-close`——原权限列表里只有 `allow-hide`/`allow-show` 等，没有任何 close 权限，只改按钮会被 ACL 拦掉、变成彻底没反应。已验：`tauri-build` 能解析该权限并烤进 `gen/schemas/capabilities.json`（权限名写错会直接编译失败）。" },
      { category: "improve", text: "⚠ 同时推翻一个我在 v2.4.0 里写下的结论：那里写的「关窗释放内存在 mac 上已实测验证，WebContent 进程 121MB → 消失」不成立。那轮 CI 点的正是这个标题栏按钮，走的是 `hide()` 路径——测到的是 WebKit 自己回收隐藏视图进程的行为，跟这个开关无关（Windows 的 WebView2 没有这个行为，所以那边完全不降）。N8 需重新验证。 设计稿：`design/白名单配置组-方案1-设计稿.html`。" },
      { category: "improve", text: "数据模型的关键取舍：`allowed_roots` 仍是「当前生效集合」的唯一事实源，语义完全不变；组只是存档（新增 `root_profiles` / `active_profile` 两个配置键）。切换组 = 把该组 roots 写进 `allowed_roots`，跑与 `save_config` 完全相同的收尾。 为何不反过来让 profiles 当事实源：路径校验、canonicalize 缓存、导入导出、`get_status`、`list_allowed_roots` 全都读 `allowed_roots`——任何一处漏同步就是安全缺陷（放行本该拒给的路径）。存档模型把这个风险降到零：生效永远只有一份，也不新增任何安全关键路径。" },
      { category: "improve", text: "安全边界：四个命令（新建/删除/重命名/切换）只经 Tauri invoke 暴露给本机面板，不注册为 MCP 工具——远程 AI 不能自行更换自己的可访问范围。切换写审计（从哪组到哪组 + 目录数）。" },
      { category: "improve", text: "兼容历史（四个方向逐一核实，不是推测）：" },
      { category: "improve", text: "新版读旧库 → `normalize_profiles()` 隐式迁移出一个「默认」组，不动 `allowed_roots`，升级后可访问范围零变化（变宽是安全事故，变窄是功能事故）；且只在内存补齐、不写库，纯读取无副作用且幂等。" },
      { category: "improve", text: "旧版读新库 → `config.rs` 的 `_ => {}` 静默忽略未知键，旧版照常读 `allowed_roots`。降级安全。" },
      { category: "improve", text: "旧版导入新配置文件 → 全项目无 `deny_unknown_fields`，多余字段被忽略。" },
      { category: "improve", text: "顺带修掉一个既有兼容缺陷：`BridgeConfig` 原本完全没有 serde default，而 `import_config` 直接把 JSON 反序列化成它——于是凡是历史上新增过字段，更早版本导出的配置就再也导不进来（报 `missing field`）。已在容器级加 `#[serde(default)]`。行为变更：导入从「报错」变为「缺字段取默认值」。" },
      { category: "improve", text: "反向同步：在当前组里增删目录时，`save_config` 把生效集合写回该组存档。不同步的后果：“切走再切回来”会用旧存档覆盖掉刚改的目录，看起来像改动丢了。" },
      { category: "improve", text: "前端新增 `RootProfileBar.tsx`（267 行），没塞进 `SecurityTab.tsx`——后者现 194 行，塞进去会逼近规则 7 的 300 行上限。" },
      { category: "improve", text: "测试：5 条新增。其中两条是关键不变量——「切换后必须刷新 canonicalize 缓存」（漏刷就是 UI 显示已切、而校验还用旧根目录，与 `import_config` 历史上那个 bug 同型）与「老配置文件必须能导入」。为使前者可测，按 `import_config_inner` 的既有套路拆出了 `switch_root_profile_inner`。 设计稿：`design/备份与审计-高级清理-设计稿.html`。" },
      { category: "improve", text: "预览与执行共用同一份筛选实现：`backup::plan_cleanup` 是个纯函数，预览只统计它的结果、执行只删它选出的文件。不这么做早晚会出现「预览说删 847、实际删了 900」——而备份是误操作后的最后一道防线，删错代价高于删审计。前端也不自己算数，确认按钮上的数字全部直接来自预览返回值。" },
      { category: "improve", text: "「将失去全部备份的文件列表」是整个功能里最要紧的一项：只告诉用户「删 847 个」，他根本意识不到自己丢了什么（半年前改过一次的文件，唯一那份备份被清掉）。所以预览区必须把具体文件名列出来、可展开。" },
      { category: "improve", text: "底线默认开，危险选项让用户显式选择：`keep_last_one` 默认 true，把「按天清理」从破坏性操作变成安全操作；关掉后若真会删光某些文件，预览区出红字并提供「开启底线重算」退路。「全部清空」也受底线约束（底线是全局开关，要彻底删得显式关掉），已写成测试固定。" },
      { category: "improve", text: "报数报真实发生的：`execute_cleanup` 不直接用 `plan.freed_bytes`，而是重新 stat 实际删成功的文件。预览之后个别文件可能被外部删掉，toast 里的数字必须是真实结果。" },
      { category: "improve", text: "删备份写审计（条件 + 条数）；并顺手 `heal_orphan_index`，清掉历史上绕过面板手删备份遗留的 `backup_index` 行（`prune_backups` 会删索引行，手删不会）。" },
      { category: "improve", text: "删整组时的一个口径不一致（已修）：`list_backups`（版本历史）按备份文件名推导出的原文件名分组，而 `BackupItem.original` 优先取索引里的完整原路径。直接把分组头的名字传给 `delete_backups_of_file` 会一份都匹配不上（凡有索引记录的备份，也就是绝大多数）。现在两种口径都认；按名匹配会把不同目录的同名文件一起删——但那正是用户在那个分组里看到的内容，语义一致。" },
      { category: "improve", text: "逐条删除不承担批量职责：`delete_backup` 只服务「精确删某一份」，且复用已有的 `assert_backup_path_in_scope`，不另写一套路径校验（安全模块不削弱）。" },
      { category: "improve", text: "审计侧只做了无争议的「清理早于 N 天」。设计稿里的「按工具/关键词等筛选条件删除」没做——它与「审计不可篡改」天然冲突，等于给一把完美的抹痕刀，待拍板。审计也不需要「保留最近 1 份」底线：它是流式记录，删旧条不会让某个东西“没有历史”。" },
      { category: "improve", text: "前端拆成四个小文件（`BackupCleanupDialog` / `BackupCleanupPreviewPanel` / `BackupDeleteConfirm` / `ui/OptionChip`），而不是塞进 `FileControlCard`（287 行）或 `VersionHistoryModal`（已 699 行）。顺带把 `AuditGroup` 从 `SettingsTab.tsx` 抽了出来：后者原本 560 行，抽完降到 453 行。" },
      { category: "improve", text: "测试：5 条针对 `plan_cleanup` 的契约——底线开启时必须留最新一份且警告归零、关闭时必须如实报出受影响文件、按体积时最旧优先且受保护那份不计入“已释放”（否则会算出“已达标”而实际没达标）、全部清空仍受底线约束、纯函数确定性。 设计稿：`design/设置项搜索-方案D-设计稿.html`。卡片从 8 张变 10 张后，“靠记位置”已不可行。 这条之所以便宜，是因为两套机制本来就在跑：`CommandPalette` 已有「导航 / 操作 / 外观」三组，只是没有设置项组；`App.handleNavigate(tab, anchor?)` 已经支持第二个参数（Header 安全徽章就是这么跳的），只是 `CommandPalette` 的 prop 类型写成 `(tab: string) => void` 把口封上了。本次主体就是“把清单填进去 + 把口打开”。" },
      { category: "improve", text: "锚点查找法泛化：原先硬编码 `toggle-${anchor}`，只能定位到开关行。现在先试 `toggle-<a>` 再回退到字面 id，于是卡级锚点（`set-network` / `set-config` 这类）也能跳。保留 `toggle-` 尝试是为了不弄坏 Header 安全徽章——它传的是裸名 `whitelist` / `readonly` / `shell`。" },
      { category: "improve", text: "筛选从只比标题改为标题 + `keywords`（keywords 不展示）。用户想到的词往往不是标题，只比标题的搜索等于只能搜你已经知道的名字。仍用简单 `includes`，20 条量级不上模糊匹配库。" },
      { category: "improve", text: "补上 4 个缺失的 `toggle-*` id（审计日志 / 写操作自动备份 / 限流保护 / 读取编码自适应）：搜得到却跳不到比搜不到更让人抓狂。" },
      { category: "improve", text: "只跳转，不代操。开关带二次确认（白名单 / 命令执行）、带上下文描述、带「只读模式下命令执行被强制禁止」这类联动。在搜索框里“打字回车就改了安全设置”是真风险——面板里「清空审计日志」那项当初就因为这个补了 ConfirmDialog。" },
      { category: "improve", text: "设置项索引抽成 `lib/settingsSearch.ts`（纯数据）。写在 `CommandPalette` 里会把它从 343 推到 378 行（又破 300 行上限），而且数据与交互混在一起后没法单测。" },
      { category: "improve", text: "新增 4 条测试锁住会静默坏掉的契约：id 不重复、每条必须有 tab（这些条目没 `run`，缺 tab 就是一个点了没反应的死条目）、设置页条目必须带 anchor（否则只能跳页顶，搜索失去意义）、以及关键词确实能搜到标题里没有的词。 安全页那两条（白名单目录 / 备份管理）暂只跳页不带锚点：安全页本轮未重构，没有卡级 id。写入索引是为了至少不让用户在设置页白找。 设计稿：`design/设置页布局重组-方案A-设计稿.html`。 先纠正一个我自己的误判：最初说「11 个开关平铺在一张卡里」是错的——那张卡已经有 4 个 `GroupTitle` 分组（安全 / 数据保护 / 兼容与性能 / 通知）加顶部风险总览。真正的毛病是两条：① 分组只有小标题、没有视觉边界，一张卡 13 个控件连着滚，滚到中间不知道自己在哪一节；②「兼容与性能」才是那个筐，6 项横跨 4 个领域。所以拆卡不是“新增分组”，是把已有分组做成形。 三个错放项归位（本次真正的功能性修正，不只是摆位置）： | 项 | 原位置 | 现位置 | 为何 | |---|---|---|---| | `ShellTypeRow` 壳层选择 | 兼容与性能（隔 3 个开关） | 安全卡「命令执行」下方，子项缩进 | 它就是命令执行的参数。命令执行关掉后壳层依旧可改，现已置灰 | | `TransportRow` 传输协议 | 兼容与性能 | 网络卡 | 它决定远程怎么连，与端口同类；而网络卡就在同一页上面 | | 审计保留天数 / 清理早于 | 页面最底单独一张卡 | 与「审计日志」开关合并 | 开关在第 4 张、参数在第 8 张，关了开关参数还能改 |" },
      { category: "improve", text: "新增 `SubSetting`：缩进 + 左侧竖线表达「这几项属于上面那个开关」，`disabled` 时整块置灰。置灰不代替后端约束，后端依然自己判断开关状态；前端只是不让用户去调一个不生效的参数。" },
      { category: "improve", text: "风险总览提到页面级：它汇总的是跨卡风险，拆卡后待在某一张卡里就不合适了。" },
      { category: "improve", text: "`highlightAnchor` 定位逻辑也提到页面级：拆卡后 `toggle-*` 分散在安全 / 高级 / 通知三张卡，留在某一张卡里就只能定位到自己那几个。没动任何 `toggle-*` 的 DOM id——Header 安全徽章与命令面板靠它们跳转，改了等于静默弄坏现有功能。" },
      { category: "improve", text: "SSE 确认弹框收进 `TransportRow` 内部（原先散在父组件）：切 SSE 必须到远端换连接命令，这个确认是切换动作固有的一部分，不应该让每个调用方自己接一遍。" },
      { category: "improve", text: "没改任何开关的行为与默认值，唯一新增交互就是“父开关关闭 → 子项置灰”。不跨 tab 搬（那是方案 B），白名单与自动备份开关留在设置页，只加一行指引说明它管的东西在安全页。 顶上了两个长期超规文件：`SettingsToggles.tsx`（698 行）拆成 `tabs/settings/` 下的四张卡 + `useSettingSave` hook（公用 save/savedKey，并顺手给它的 setTimeout 补了卸载清理）；`SettingsTab.tsx` 也把 `NetworkGroup`/`AppGroup`/`InstallGroup`/`ConfigGroup` 抽了出去。 > 中途有一步做反了：只拆 `SettingsToggles` 后，`SettingsTab.tsx` 反而从 453 行涨到 500 行（多了 TransportRow 接线与页面级 effect）——比重组前更超规。把剩下四个 Group 也抽出去后降到 75 行，成为纯组合层。现在 `tabs/settings/` 下 14 个文件全部在 300 行以内（最大 218）。 设计稿：`design/网络卡瘦身-方案A-设计稿.html`。先把高度账算清楚了再动：常态下整张卡 ≈ 269px，却只装了 2 个设置。 | 改动 | 省 | 为何不丢信息 | |---|---|---| | 重启警告改为仅 `dirty` 时显示 | ~56px | 按钮文案本来就是「保存并重启」；真正需要提醒的时刻（刚改完）它会立即出现，比常驻在那里被忽略更醒目 | | 删掉常驻的「无更改」 | 横向变干净 | 按钮 disabled 表达的是同一件事；改为 `title`（悬停仍知道为何不可点，越界时还会提示范围） | | 传输协议描述收成一行 | ~18px | 删的那句「切换后需到远端替换连接命令」在 SSE 确认弹窗里已有完整归宿——那里直接给出迁移命令 + 复制按钮，并要求点「我已复制，确认切换」 | 顺带扫了一遍同型问题（只看，再逐项拍板）。全库共 11 处提示/警告块：" },
      { category: "improve", text: "已经是按需渲染的 5 处：`ConnectTab` 两处（`scope===\"project\"` 且路径为空 / `includeShellTools`）、`TokenManager` 两处（`regenDone` / `confirmingRegen`）、`CommandAllowlistCard`（`allowOn`）——这几处的作者想清楚了，网络卡那条是漏网的。防火墙关闭警告（`firewallEnabled === false`）同属此类，且那是真安全提示，该留。" },
      { category: "improve", text: "误命中 3 处（是内容不是占位，不动）：`RootProfileBar` 的配置组选择条本体、`LogDetailPanel` 的「耗时拆解」数据面板、`PerfCharts` 的「自动结论」。" },
      { category: "improve", text: "同型常驻 4 处，本次做掉两处：" },
      { category: "improve", text: "✅ `ConfigGroup` 的「导入将覆盖所有当前配置」（~56px）——删前先核实了归宿：下面的 `ConfirmDialog` 列出了覆盖哪些设置（白名单 / Token / 命令执行等）、会重启、不可撤销，还带上具体文件名，是严格更全的超集。" },
      { category: "improve", text: "✅ `FileControlCard` 底部重复的「所有设置修改后自动保存」——同一张卡顶部（扩展名那行下）已经说过，纯重复；且每个控件保存后本身有「已保存」行内反馈。" },
      { category: "improve", text: "⏸ `FileControlCard` 的「备份怎么产生的？」4 行教学块（~90px）+「备份仅存于本机该目录」（~30px）——收益最大（~120px，那是全应用最高的卡之一），但需要改成默认折叠，得先出设计稿，留待后续。 另外诚实记一笔：本轮方案 A 我自己在 `SecurityGroup` / `BackupAuditGroup` 加的两条跳转指引（「→ 白名单目录在安全页…」）也是常驻，各约 34px。但它们性质不同——正因为方案 A 不跨 tab 搬，用户才需要知道东西在哪，常态下是有用的导航信息，故保留。 没做两件事：不把端口与协议合并到一行（窗口 `minWidth: 560`，窄窗必须 wrap，wrap 后省下的高度又还回去；且两个语义不同的设置相邻容易误点）；不压 `py-3`/`h-9` 这类尺寸（它们是全页共用节奏，为一张卡改就会与其它九张卡对不齐——省高度要靠去掉多余元素，而不是把行变矮）。 `TrayIconBuilder` 的 `show_menu_on_left_click` 默认就是 `true`（tauri 2.11.3 源码注释原文：\"default is `true`\"），而代码里设了 `.menu(&menu)` 却从未关掉它，同时又在 `on_tray_icon_event` 里自己接了左键——于是左键同时发生两件事：弹菜单（Tauri 默认行为）+ 切换窗口（我们的 handler）。 两个平台的惯例相反，所以按平台分开而不是一律关掉：" },
      { category: "improve", text: "Windows：左键 = 开/收面板，右键 = 菜单 → `show_menu_on_left_click(false)`，保留左键 toggle。" },
      { category: "improve", text: "macOS：菜单栏图标的惯例是左键就开菜单（右键在菜单栏几乎无人用），而菜单里已经有「打开面板」 → 保留默认 `true`，并在 handler 里跳过左键 toggle（否则同样会弹菜单 + 切窗口）。这也顺带回答了清单 N5（mac 托盘左键手感）。 mac 分支下两个闭包参数都用不到，显式 `let _ = (tray_app, &event);` 丢弃——CI 的 clippy 跑在 macOS 且带 `-D warnings`，不丢会直接挂。 上轮修完 `hide()` → `close()` 后又把这个功能的全部关窗/开窗路径重扫了一遍。" },
      { category: "improve", text: "Win11 圆角丢失（本功能引入的回归）：`DWMWA_WINDOW_CORNER_PREFERENCE` 只写在 `setup` 里，只作用于启动时创建的那个窗口；而省内存模式下窗口会被销毁重建，重建路径没补这一步 → 关窗再开变直角，而开关关闭时不会（窗口没被销毁），同一个应用两种外观。已抽成 `apply_window_decorations`，启动与重建共用；且在 `show()` 前打上，否则能看到一帧直角。" },
      { category: "improve", text: "macOS 点 Dock 图标没反应：`RunEvent::Reopen` 之前完全没处理（整个 `run` 回调里只有 `ExitRequested`）。窗口被销毁后 Dock 上图标还在但点下去什么都不发生，用户很可能以为应用已经死了。严格说是既有缺口（清单 N14），但省内存模式把它放大了：以前至少窗口还在（只是隐藏）。只处理 `has_visible_windows: false`：为 true 时系统自己会把窗口提到前面。" },
      { category: "improve", text: "开窗先显示一个空白窗口：`build()` 后立即 `show()`，而前端要 1~2 秒才加载完，期间用户看到的是纯背景色的空面板。启动路径也是同样写法，但启动时用户本来就预期要等；从托盘点开却期待「秒开」，落差全在这里。 修法（设计稿：`design/开窗首帧骨架屏-设计稿.html`）：" },
      { category: "improve", text: "骨架屏写进 `index.html` 的 `#root` 里，不做成 React 组件。这是关键：空白的那 1~2 秒正是「JS bundle 还没加载完」的时间，React 组件要等 bundle 执行完才能渲染，恰好错过了需要它的那段时间。写在 HTML 里则浏览器解析到就渲染；`createRoot().render()` 会自然把它清掉，不需要任何手动拆除代码。样式全内联，不依赖 Tailwind 那份 CSS（它也是外部资源）。" },
      { category: "improve", text: "顺带修掉深色主题的白闪：主题本来是 `main.tsx` 里打的 class，也就是说 JS 加载完之前页面永远是浅色。现在 `<head>` 里一段同步内联 script 直接读 `localStorage.theme` 抢先定调（同步 script 阻塞渲染，没有中间态），与 `main.tsx` 那行幂等不冲突。" },
      { category: "improve", text: "骨架屏与真实布局同构（Header 的 `px-5 py-3.5` + 40×40 图标，TabsList 的 `px-5 pt-4 pb-3`），否则真内容上来时会看到一次布局跳变，比空白更难受。但只画到卡片轮廓为止：真实内容高度随状态变，而这份 HTML 以后没人会跟着界面改，画得越细越快过时。" },
      { category: "improve", text: "流光用 `background-position` 平移渐变（纯合成），不用 `box-shadow`/`filter`——项目里早就因为那个每帧重绘做过一轮优化；并自带 `prefers-reduced-motion` 守卫（全局那份在 Tailwind CSS 里，这段在它之外）。" },
      { category: "improve", text: "接替时一次 180ms 淡入（`.app-boot-fade`）磨掉硬切换。静止态写 `opacity: 1`、淡入只由 keyframe 提供：反过来写的话，一旦动画被禁用或冻住，元素就永久停在透明—— 2026-08-03 那次整页白屏就是这么来的。 有意不做：把 `show()` 推迟到前端 ready。那样点完托盘后 1~2 秒屏幕上什么都没有，比看到骨架屏更像「点了没反应」。也没加 loading 文字/转圈：骨架屏已经传达了「在加载」。 仍未验证（不能只靠读代码下结论）：" },
      { category: "improve", text: "本机 clippy 跑在 Windows，编译不到 macOS 的 `Reopen` 分支（该变体本身 cfg 在 macos）。已对着 tauri 2.11.3 源码核实签名：`Reopen { has_visible_windows: bool }`、`#[non_exhaustive]`（所以必须带 `..`）、`run` 回调为 `FnMut(&AppHandle, RunEvent)`。真正的门禁是 CI 的 macOS clippy。" },
      { category: "improve", text: "N8（关窗真的释放了内存）在 mac 上仍未实测。" },
      { category: "improve", text: "macOS 的 Cmd+Q 会不会被 `prevent_exit` 拦住：取决于 predefined Quit 是走原生 `terminate:` 还是 Tauri 的 `exit(0)`（两种都应该没事，但需真机确认）。本应用没自设 app menu，mac 上走 Tauri 默认菜单；Cmd+W 走 `CloseRequested`，与标题栏按钮一致。 功能写完后做了一轮完整审查（后端 / 前端各一路），全部已修。其中两个 P0 是本次自己引入的。 P0① 版本历史里的删除按钮根本用不了。`ConfirmDialog` 的遮罩层级写死为 `z-50`，而 `VersionHistoryModal` 的 `Modal` 传的是 `zIndex={1000}`；两者都 portal 到 body、是兄弟节点，于是确认框被压在父遮罩下面：看不见、点不到，点击反而落到父遮罩上把版本历史关掉。项目里早踩过这个坑（`RestoreBackupDialog` 用的是 `ConfirmModal ... zIndex={1001}`，`ConfirmModal` 的注释原文就写着这事），选组件时没查层级。修法：给 `ConfirmDialog` 加 `zIndex` prop（默认 50，向后兼容）并在这里传 1001；顺手给它补上之前没有的 Esc 关闭，并阻止冒泡——不拦的话 Esc 关掉的是父弹框。 P0② 「删除早于 N 天」用的时间是错的。备份是 `std::fs::copy` 产生的，Windows 下底层是 `CopyFileExW`，它保留源文件的最后写入时间。已在本机实测：源文件 mtime 设为 2020-01-02，今天 copy 出的 `.bak` 的 mtime 仍是 2020-01-02。于是拿 mtime 做「删除早于 90 天」会把今天刚建的备份删掉。三处受影响：按天判定、按体积的「最旧优先」（实际变成「源文件最久没动的优先」，恰好优先销毁只有一两份、不可再生的历史）、以及底线选「最新一份」。早该发现的旁证：既有的 `list_backups` 就是用 `parse_backup_timestamp` 从文件名时间戳解创建时间、mtime 只作兜底。修法：新增 `created_from_backup_name`，`BackupItem` 改持 `created`，mtime 降为兜底；并给排序加路径作二级 key，并列时不再依赖 `read_dir` 枚举顺序。 P0③ 开着版本历史删备份，列表会变空态。`invalidateBackups` 把缓存置 null，但列表只在点「打开」时懒加载，没人重拉 → `isEmpty = !loading && !result` 成立 → 弹框显示「暂无备份文件」。删了一份却看起来像全没了。现在弹框开着时直接重新拉取。 P1① 「预览 == 执行」之前并没有真正被约束住。共用同一个 `plan_cleanup` 函数不等于共用同一份输入：两个命令各自重扫目录、各自取一次 `now()`，预览到确认之间只要发生一次 MCP 写操作（本应用的主业）新增 `.bak`，实际删除集合就不等于用户确认过的那个——恰好就是注释里反复承诺不会发生的那个偏差。现在预览返回 `victims` 路径清单，前端原样回传，执行只删这份已确认的清单（每条仍过 `assert_backup_path_in_scope`）。后新增的备份不在清单里、不会被删，这是对的：用户没确认过它们。`plan_is_deterministic` 那条测试只能证明幂等性，注释已改正。 P1② 300ms 去抖窗口内确认按钮仍可点。改完条件后 `previewing` 还是 false、`preview` 还是旧的，而 `handleConfirm` 读当前 state。最坏子场景：关掉保留底线后立即点确认，红字警告和「开启底线重算」都还没渲染，用户在没看到任何风险提示的情况下删掉了某些文件的最后一份备份。现在用 argsKey 指纹锁住：预览参数 ≠ 当前参数时按钮置灰、预览区也不再展示旧结论。 P1③ `heal_orphan_index` 可能清空整张索引表。`Path::exists()` 把任何错误都当成“不存在”：网络盘/移动盘掉线、ACL 拒绝、路径过长一律 false。备份目录在 `Z:\\` 且掉线时点一次清理就能把整张表删光，且后果不可逆：所有备份永久失去 original_path 映射，还原 / 看改了什么全部失效，`prune_backups` 退回 basename 匹配。现在三道防护：只处理备份目录下的行、`symlink_metadata` 且只有 NotFound 算孤儿、单事务；且调用前先确认目录真的读得到（`list_backup_items` 改返 `Option`，区分「目录里没备份」与「读不到目录」）。 P1④ db 锁罩住了整轮删除。原先持锁删完所有文件 + 每条一个独立事务 DELETE，上万备份是数十秒级，期间 `write_files`/`edit_files` 等全部 MCP 写操作（它们写备份索引要同一把锁）全部排队，还挂住一个 tokio worker。现在拆成：`delete_files_bulk` 在 `spawn_blocking` 里且不持锁，事后 `purge_index_rows` 单事务批删索引行。 P2① 无索引备份按 basename 归组会静默违反保留底线。`original` 口径混用（有索引=完整路径，无索引=文件名）。我原先只想到「同一文件裂成两组 → 误报红字」（安全方向），漏了反方向：`a/x.rs` 与 `b/x.rs` 都无索引时并成一组，底线只保护其中一个，另一个真实文件的备份被全部删光，而红字只报一条 `x.rs`。现在 `group_key` 分 `p:` / `n:` 两个命名空间，无索引组在预览里显式标注「无索引，按文件名归组」。 其余已修：逐条/整组删除之前不写审计（与批量一致共用 `audit_backup_deletion`）· `delTarget` 不参与关闭重置（重开会自己弹出上次那个确认）· 重开清理弹窗先闪旧预览 · `target_mb` 允许 0（变成伪装成「清到 300MB」的全删，前后端一起改为 min 1）· 删失败被静默吞掉 · 缺 `is_file()` 过滤（名为 `*.bak` 的目录会计入待删且永远删不掉）· 分组头内边距变成点不到的死区 · 按时间视图有删除没还原 · 自定义天数等于预设时两个按钮同时转圈 · 数字框退格删到空立即回填（新增 `ui/NumBox`）· 保留天数 0 时点「立即清理」只得到「已清理 0 条」而不知原因 · 卸载后悬挂的保存 timer · 删整组确认文案未说明「不区分目录」· 备份与审计预设不一致（统一 7/30/90）。 测试从 5 条加到 8 条：新增「创建时间必须来自文件名而非 mtime」「有索引与无索引永不同组」「并列时靠路径而非枚举顺序决定」。其中最后一条第一次跑就失败了——但是测试自己写错：`SystemTime::now()` 写在 `.map()` 里会每项各调一次，三份并不是同一个时间，测的也就不再是「并列」场景。 顺带：`VersionHistoryModal` 抽出 `backup/DiffView.tsx`（769 → 575 行）。仍超规则 7 的 300 行，但不再是本次改动把它推得更高。 起因是排查「这个功能真的有用吗」，结果发现三层问题。" },
      { category: "improve", text: "失败完全静默：原调用点是 `let _ = audit::cleanup_old_entries(...)`，而这个函数会「重写 + rename audit.log」——rename 在 Windows 上确实会因文件被占用而失败（audit.rs 里 E-P0-7 那段注释就是为此而写的）。吞掉的后果：保留天数静默失效，日志无限增长而无人知道。现在三个调用点都写日志（成功记条数、失败 `log::warn!`）。与之前修的桌面通知是同一类问题：`let _ =` 吞掉了唯一的失败信号。" },
      { category: "improve", text: "为此把 `cleanup_old_entries` 的返回值从 `()` 改为实际删除条数，调用方才能写出有信息量的日志 / 提示。" },
      { category: "improve", text: "机制与产品形态不匹配：全项目只有一个调用点，仅启动时跑一次，无任何定时任务。而 cc-bridge 是常驻托盘服务、设计上就是开着几周不关——于是不重启的用户那里保留天数形同虚设。已新增 24 小时定时清理（走现有的 `spawn_supervised`，带 panic 自愈）；每次 tick 重新读配置而不是把 retention 捕进闭包，用户中途改值不需重启。" },
      { category: "improve", text: "改了设置不生效、也不提示：`save_config` 只存值。现在保留天数一变就后台跑一次清理（放后台的理由与启动那次相同：大日志重写可能耗时，不能卡住保存交互）；另新增 `cleanup_audit_now` 命令 + 设置页「立即清理」按钮，会 toast 告知删了多少条。" },
      { category: "improve", text: "顺带优化：无过期条目时直接返回，不再重写文件。原实现无条件 rewrite + rename，每次启动都白写一遍整份日志、并白白失效一次解析缓存（E-P4 那个）。" },
      { category: "improve", text: "文案修正：原先写「会在每次启动时自动清理」（属实但易让人以为改完即生效），现在把三个时机、以及与日志页「清空全部」的区别都写清了。" },
      { category: "improve", text: "测试：新增一条锁两个契约——返回值必须等于实际删除条数，且无过期条目时文件 mtime 不得变化（证明真的没重写）。 > 顺带摸清了与备份清理的关系（之前容易混）：两套完全独立。审计按天数、只在启动/定时/手动时跑；备份按份数（`backup_retention`，默认 10）、在每次写/删/移/复制文件时顺带裁剪（六个调用点）且失败会上抛。互不影响；唯一共同点是 `0 = 无限保留` 这个语义约定。" },
      { category: "improve", text: "根因：`AboutGroup` 里用 `if (changelogOpenToken > 0) openChangelog()` 当一次性信号，而 React effect 在挂载时必然执行一次（不管依赖值有没变）；`SettingsTab` 是 `lazy` + `TabsContent`，切走就卸载。只要 token 停在 >0，每次切回都会展开。" },
      { category: "improve", text: "token 为何永久停在 >0：首次带红点进设置页 → token+1，同时 `markSeenOnEnter()` 写 lastSeen 使 `unreadCount` 立刻变 0；而负责把 token 归零的 `markChangelogSeen` 靠另一个 effect 触发、条件是 `expanded && unreadCount > 0` → 再也不会被调用。这又是两个各自正确的修复（「消费信号防残留」+ 「进页即标记已读防反复红点」）叠加出的第三个 bug。" },
      { category: "improve", text: "修法：改为比对 `handledTokenRef`（初值 = 当前 token），使挂载天然是 no-op。不再依赖「每条消费路径都记得归零」这个易遗漏的约定（已经漏了两次）。" },
    ],
  },
  {
    version: "2.4.0",
    date: "2026-08-04",
    items: [
      { category: "feat", text: "支持 macOS（Apple Silicon）：装好后即可像 Windows 版一样被远程 Claude Code 连接使用；自动更新完整可用——首次安装时放行一次，之后的更新都是无感的" },
      { category: "feat", text: "设置页「兼容与性能」新增「关窗时释放界面内存」开关：开启后关闭窗口即回收界面进程，托盘常驻内存约 88MB → 6MB，再次打开需重新加载（1~2 秒）；不想等可关掉此开关，改为仅隐藏窗口、再开瞬时显示" },
      { category: "improve", text: "macOS 上的界面文案与可用功能按平台适配：命令执行壳层、桌面通知说法、内存数字、危险命令示例都改成了 mac 的实际情况；Windows 专属的防火墙放行与桌面快捷方式在 mac 上自动隐藏" },
      { category: "fix", text: "macOS 上「打开备份目录」点了没反应、「安装位置」显示为包内部路径，两处已修正；菜单栏图标现在能正确跟随系统浅色/深色主题" },
      { category: "sec", text: "强化危险命令拦截：补上了 macOS 上三类之前拦不住的毁灭性目标（系统目录的另一种写法、整个用户家目录、整块外置盘/网络盘），现在与 Windows 同等强度；同时保证不误拦正常的项目目录" },
      { category: "improve", text: "`CloseRequested` 根据新配置 `release_webview_on_close` 决定销毁还是 `hide()`。开关值用 `config.try_read()` 读（同步回调不能 await），拿不到锁时回退默认 true——不用 `AtomicBool` 镜像是因为配置有四个写入点（save_config / 导入配置等），逐处同步迟早会漏，直接读配置才是单一事实源。" },
      { category: "improve", text: "关键风险点：Tauri 默认「所有窗口关闭 → 退出进程」，若不拦截会把 MCP 服务一起带走、远程直接断连。现在 `RunEvent::ExitRequested` 只拦 `code: None`（窗口全关触发）；托盘「退出」走 `exit(0)` 带 `code: Some(0)`，真退出不受影响。" },
      { category: "improve", text: "窗口重建用 `WebviewWindowBuilder::from_config` 从 `tauri.conf.json` 原配置重建，不手写一份参数以免与配置漂移；托盘菜单「显示」、托盘左键、单实例重复启动三个入口统一走 `show_or_create_main_window`（单实例那条尤其要紧：窗口销毁后双击图标必须能唤起来）。" },
      { category: "improve", text: "托盘左键「收起」改为 `w.close()`，让销毁/隐藏的判断只写在 `CloseRequested` 一处。" },
      { category: "improve", text: "已逐条核实不受窗口销毁影响：MCP 服务（tokio runtime）、托盘（挂在 app 上）、桌面通知（IP 变化 / 后台命令完成 / push_notification 全走 `handle.notification()`）、IP 变化检测（整条链路在 Rust 侧，前端横幅靠 `get_status` 的 `ip_changed` 状态兜底）。唯一影响：窗口关闭期间前端的自动更新检查定时器不跑，重开窗口时会立即补一次。" },
      { category: "improve", text: "设置页两处文案在 mac 上是错的，已按平台切分（N16）：" },
      { category: "improve", text: "「命令执行壳层」默认项在 mac 上显示为 `sh`（Unix 下 `cmd` 实际跑的是 `/bin/sh`），说明也不再提「需本机已装 Git for Windows」——改为「macOS 自带 bash 3.2，如需新版可用 Homebrew 安装」。存储值仍是 `cmd`/`bash` 未变（后端按这两个值反序列化）。" },
      { category: "improve", text: "「关窗时释放界面内存」的数字不再照搬 Windows 的「85MB → 6MB」：mac 实测是独立 `WebContent` 进程 121MB → 进程消失、主进程约 22MB 常驻，两平台测量口径本就不同。" },
      { category: "improve", text: "文案收进 `lib/platform.ts` 的 `shellTypeCopy()` / `releaseWebviewHint()`，并补 10 条单测锁住不变量（尤其「mac 文案不得出现 Git for Windows」与「两平台数字不许串」）。`SettingsToggles.tsx` 已 690 行、远超 300 行上限，不再往里堆文案。 把 Rust 侧所有 `#[cfg(windows)]` 是否有配对的非 Windows 分支、所有 `Command::new()` 生成的子进程、以及前端全部平台相关文案逐条过了一遍，修掉如下问题。" },
      { category: "improve", text: "两个按钮在 mac 上必然报错：" },
      { category: "improve", text: "「桌面快捷方式 / 创建到桌面」无条件 spawn `powershell` 写 `.lnk`，mac 上直接 `No such file or directory`。现按平台拆成两份 impl，前端也隐藏了这一行（mac 的入口是 Dock / 启动台，桌面本来不放应用图标）。" },
      { category: "improve", text: "「打开备份目录」用 `cmd /c start`，mac 上同样没有 `cmd`。非 Windows 改走 `tauri-plugin-opener`（与 `reveal_install_dir` 同一条路）；Windows 分支保留原样不动——当年用 `cmd /c start` 正是为了规避 `explorer /select` 的 DDE 转发不弹窗。" },
      { category: "improve", text: "「安装位置」在 mac 上指到 bundle 内部：`current_exe().parent()` 得到的是 `cc-bridge.app/Contents/MacOS`，展示给用户是一串包内路径，「打开目录」更是让访达跳进「显示包内容」。新增 `resolve_install_dir()`：mac 上往上三层取 `.app` 本体，开发模式（裸二进制）自动回退。" },
      { category: "improve", text: "Dock / 访达图标只有 256px：`icons/icon.icns`（含大尺寸）一直在仓库里，但 `bundle.icon` 只列了三个 PNG，tauri-bundler 找不到 `.icns` 就拿 PNG 现生成一个，Retina 下发虚。已把 `icons/icon.icns` 加进列表（不影响 Windows，那边取 `.ico`/PNG）。" },
      { category: "improve", text: "桌面通知失败原先被完全吞掉：三处调用点全是 `let _ = ...show()`。mac 侧走 `mac-notification-sys`（`NSUserNotification`），系统未放行时 `show()` 返回 `Err`；而插件 desktop 侧的 `permission_state()` / `request_permission()` 是硬编码返回 `Granted` 的空实现（`tauri-plugin-notification 2.3.3/src/desktop.rs:61-66`），查不出真实授权状态——这个 `Err` 是唯一信号。现在三处都写 `log::warn!`，且 `push_notification` 失败时回传 `pushed: false` + `reason`（仍返回 `Ok`：推不出去不是调用方的错）。这条要紧的原因：工具描述里强制 AI「每完成一个任务必调 `push_notification`」，mac 上它一旦不工作，此前用户和日志都拿不到任何提示。" },
      { category: "improve", text: "发给远程 AI 的工具描述里的平台错误（同 `run_command` 早已做过的平台分支）：" },
      { category: "improve", text: "`push_notification`：不再对 mac 说「appears as a Windows toast」，并补了一句「若返回 `pushed:false` 就改用文字告知用户」。" },
      { category: "improve", text: "`stop_command`：原文写 `taskkill /T`，这在 Windows 上也是错的——实际走 `child.start_kill()`，Windows 是 `TerminateJobObject`、Unix 是向整个进程组发 `SIGKILL`。现两个平台各自据实描述。" },
      { category: "improve", text: "UI 文案：" },
      { category: "improve", text: "「后台命令完成通知」不再对 mac 说「Windows toast」。" },
      { category: "improve", text: "「关于」弹框的三个数字与两条卖点按平台分：mac 不再照搬「安装包 3.4MB」（mac 压根没有安装程序，发布物是 `.app.tar.gz`）、「启动内存 < 20MB」（mac 主进程实测约 22MB）、「Job Object 进程隔离」（Windows 专属 API，mac 是 POSIX 进程组）。" },
      { category: "improve", text: "命令白名单卡的危险命令示例按平台取（mac 上举 `rm -rf C:\\Windows` 只会让用户困惑）。" },
      { category: "improve", text: "新增文案收在 `lib/platform.ts`（`notifyCommandCompleteSub` / `dangerousCommandExample` / `aboutCopy`），并补 6 条单测锁不变量（mac 文案不得出现 `3.4MB` / `< 20 MB` / `Job Object`；首帧 undefined 必须落 Windows 文案，避免 Windows 用户闪一下「免安装」这种错数字）。共 43 条前端单测全过。" },
      { category: "improve", text: "顺带确认了两件没问题的事，免得日后重复排查：数据目录用的是 `app_data_dir()`（mac 落 `~/Library/Application Support/`，不在 .app 包内，所以自动更新整包替换不会丢配置 / 审计 / 备份）；开机自启走 `tauri-plugin-autostart` 且已传 `MacosLauncher::LaunchAgent`。" },
      { category: "improve", text: "Layer 1 破坏性拦截在 mac 上比 Windows 弱（由「MCP 工具需不需要适配 mac」这个问题顺藤摸瓜查出来的）。`is_catastrophic_target()` 的清单是按 Windows + Linux 写的，漏了三类 macOS 特有目标：" },
      { category: "improve", text: "`/private`——真绕过。mac 上 `/etc` `/var` `/tmp` 全是指向 `/private/*` 的符号链接，拦 `rm -rf /etc` 而不拦 `rm -rf /private/etc`，同一个目标换个写法就过了。" },
      { category: "improve", text: "`/Users/<用户名>`——一整个家目录。此前只精确匹配 `/Users` 本身，而 Windows 的等价写法 `c:/users/foo` 一直靠 depth 规则拦得住——两平台强度不对等。" },
      { category: "improve", text: "`/Volumes/<卷名>`——mac 挂外置盘/网络盘的位置，地位等价于 Windows 的 `D:\\`（盘根本来就拦）；Linux 的 `/mnt` `/media` 已在列表里，就它没有。 修法：`/private` 进前缀匹配清单；把「容器型」目录（`/users` `/home` `/volumes` `/mnt` `/media`）改为与 `c:/users` 同一套 depth ≤ 2 规则，并把那段 depth 判定抽成 `is_shallow()` 共用。新增两组单测：三类目标必须拦，且深层项目路径（`/Users/foo/myproject`、`/Volumes/Backup/build`、`/opt/myapp/build`）绝不能误拦——拦过头同样是缺陷。守规则 7：这是对安全模块的补强，不是放松。" },
      { category: "improve", text: "同时确认其余 MCP 接口（文件类 9 个、`search_files` / `analyze_file` / `notebook_edit` / `list_allowed_roots` / `batch`）无平台专属代码，路径校验走 `canonicalize` + 祖先遍历、扩展名白名单统一转小写，两平台都对；`is_device_path()` 本来就包含 `/dev/disk*`（mac 的命名）。" },
      { category: "improve", text: "`commands.rs` 顶部的 `tauri::Manager` 导入改成在 Windows 版 impl 内局部 `use`：它只被 `app.path()` 用到，留在顶部会让 mac 的 `clippy --all-targets -D warnings`（CI 与 Windows 侧同等严格）因未用导入直接失败。" },
      { category: "improve", text: "清掉两处过期注释：`ConnectHero.tsx` 与 `index.css` 里仍写着「数据雨 canvas」，而 canvas 在 2.3.19 已改为纯 CSS、元素早已移除。" },
      { category: "improve", text: "mac 自动更新之前是完全不通的：`generate-updater-json.mjs` 只生成 `windows-x86_64` 条目、`findNsisArtifact()` 硬编码扫 `bundle/nsis`，而 mac 客户端要找的 key 是 `darwin-aarch64`（tauri-plugin-updater 的 `updater_os()` 返回 `darwin` 而非 `macos`）。找不到自己的平台 key 就永远检测不到更新，且不报错。现已改为双平台探测（`bundle/macos/*.app.tar.gz` → `darwin-aarch64`），并新增 `UPDATER_MERGE_INTO` 跨 job 合并（Windows 与 mac 在不同 job 构建、各自只看得到自己的产物，后跑的若直接覆写会静默抹掉先跑那个平台的条目）。只出 arm64。" },
      { category: "improve", text: "mac 自动更新完整链路已在真实 macOS 上跑通（检查 → 下载 → 验签 → 替换 `.app` → 重启启动）。硬证据：替换后 `Info.plist` 版本号变为测试版本、包内出现标记文件、`codesign --verify` 通过、无 quarantine 属性（这就是「更新后不会被 Gatekeeper 拦」的直接证据）、重启后 `/health` 200。据此 `docs/Mac版本支持方案.md` 第 2.2 节那条「每次更新后要重新绕 Gatekeeper」可以正式判为不成立。" },
      { category: "improve", text: "新增 `build-macos` 发版作业（仅 tag 触发）与 `updater.json` 跨作业合并：两平台在不同作业构建、各自只看得到自己的产物，若两边都自行生成并 attach，后跑的会静默覆盖先跑那个平台的条目。现由 mac 产出平台片段、Windows 侧合并后统一 attach，并加了「片段存在却没合并进去就终止发版」的显式校验。mac 挂了 Windows 仍能发（降级为只发 Windows）。" },
      { category: "improve", text: "仍未验的一个源码层风险：updater 用 `tempfile::tempdir()` 做备份，而 `rename` 跨文件系统返回 `EXDEV` 而非 `PermissionDenied` → 不尝试提权、直接失败。CI 里同卷所以没覆盖到，app 放在外置盘/另一个卷上会更新失败。" },
      { category: "improve", text: "关窗释放内存在 mac 上已实测验证：用辅助功能点自绘标题栏里的「关闭窗口」按钮（即产品真实路径），窗口数 1→0、`WebContent` 进程从 121MB 到完全消失，而主进程 RSS 几乎不变（96880→96944KB）。结论：mac 上关窗释放约 121MB，且必须看 `WebContent` 进程而不是主进程 RSS，否则会得出“webview 不要钱”的错误结论。关窗后进程存活、`/health` 仍 200，服务没被带走。" },
      { category: "improve", text: "纠正一个我自己的错误结论：曾说「webview 内容对辅助功能是一整块、点不到」——实测推翻。WKWebView 会暴露完整网页 AX 树（`AXWebArea`），136 个元素、所有按钮的中文名都读得到（关闭窗口 / 停止服务 / 跳过引导 / …）。之前查不到是因为用了 `button ... of window 1`（只查直接子元素），改用 `entire contents` 深层查找即可。这使得 CI 能真的走完整 UI 流程。" },
      { category: "improve", text: "烟测当场抓出一个真 bug 并已修：mac 菜单栏托盘图标应为模板图标（单色、由系统适配深浅色），但真机截图里它仍是彩色的，而旁边系统的聚光灯 / 控制中心都是单色黑。根因：模板属性挂在 NSImage 上（`setTemplate:`），`refresh_tray` 里的 `set_icon` 会换一个全新 NSImage、连带把它抹掉；而托盘在启动时就会做首次状态刷新，所以建托盘时设的 `icon_as_template` 几乎立刻失效。已在每次 `set_icon` 后补 `set_icon_as_template(true)`。" },
      { category: "improve", text: "烟测又抓出第二个缺陷（已定位根因，修法待定）：mac 上 `Cmd+W` 实际关不了窗。`Window` 菜单里确实有 `Close Window` 项、点它也返回成功，但窗口始终没关（截图像素差仅 6.79%，而窗口真消失时是 41~85%）。根因：AX 查窗口按钮列表返回为空——窗口是 `decorations: false` 自绘标题栏、没有标题栏按钮，而 AppKit 的 `performClose:` 对无关闭按钮的窗口是 no-op，`Close Window` 与 `Cmd+W` 走的正是它。不是阻塞缺陷（自绘标题栏的关闭按钮不受影响），但两个候选修法各有代价，待确认后再动。" },
      { category: "improve", text: "修正一个错误结论：`spctl -a` 对 ad-hoc 签名包永远报 rejected，清掉 quarantine 后也是——因为它判的是能不能过公证策略，而 Gatekeeper 实际只拦带 quarantine 的包。光看 `spctl` 会得出「永远打不开」的假结论，已改为真的 `open` 两次看实际行为。" },
      { category: "improve", text: "新增 `smoke-macos` 作业（仅 `workflow_dispatch` 按需触发）：在真实 macOS 上构建 `.app`（ad-hoc 签名）并验证 codesign / Gatekeeper 评估 / quarantine 隔离属性陷阱与 `xattr -dr` 解法 / 应用启动与 MCP `/health` 探活 / webview 内存代价 / 桌面通知链路 / Apple Event quit（等价 Cmd+Q）退出语义，并上传截图与日志作证据。" },
      { category: "improve", text: "能这么做的前提：之前把「GUI / 打包 / 分发」整块判成「只能真机」，这个判定过宽——GitHub 的 macOS runner 带图形会话。窗口配置是 `visible: false` 且 CI 无辅助权限（TCC）点不了托盘菜单，改用「再启动一次二进制 → single-instance 回调 → `show_or_create_main_window`」把窗口显示出来。" },
      { category: "improve", text: "`tauri.conf.json` 补 `bundle.macOS.signingIdentity: \"-\"`（ad-hoc 签名，内网自用不买开发者账号）。`bundle.targets` 故意仍保持 `[\"nsis\"]` 不动，mac 侧用 CLI `--bundles app` 覆盖，避开影响 Windows 正式发版路径。" },
      { category: "improve", text: "README 新增「macOS 安装与更新说明」：适用范围（仅 Apple Silicon、ad-hoc 签名未公证）、隔离属性的两种解法（`xattr -dr` 或系统设置放行，并注明 macOS 15 起「右键打开」已失效）、安装位置对自动更新的影响（`~/Applications` 无感 / `/Applications` 每次弹管理员密码框 / 外置盘会失败）、自动更新可用且更新后不会再被 Gatekeeper 拦、以及与 Windows 版的已知差异。构建章节补 mac 命令（必须带 `--bundles app`，打 zip 要用 `ditto` 而非 `zip`）。" },
      { category: "improve", text: "`docs/Mac版本支持方案.md` 第 2.2 节加实测更正块：该节原文全部基于推理，其中三条经源码核对与真机实测后为错或不准确，最严重的是「Tauri 自动生成 `darwin-universal` 条目、不用改」——实际 key 是 `darwin-aarch64` 且需自己生成，这正是 mac 自动更新此前彻底不通的原因。原文保留备查。" },
      { category: "improve", text: "`功能优化清单.md` M4 重写：一次内存排查事故的完整记录——前三版数据全错，真正的根因是一个已卸载软件在 Machine 级环境变量里残留的 `WEBVIEW2_USER_DATA_FOLDER`，它让本机所有 WebView2 应用共用同一 profile 与同一 BROWSER 进程（连带共享 localStorage，比内存串台更严重）。同时记下四次误判的根源与六条测量纪律（归属用父进程链、口径用专用工作集、先确认窗口真可见等）。" },
    ],
  },
  {
    version: "2.3.20",
    date: "2026-08-03",
    items: [
      { category: "fix", text: "后台有个线程会长期占满一个 CPU 核心，现已修复；同一个问题还让「IP 变化提示」一直没能生效，也一并修好" },
      { category: "fix", text: "命令超时后不再丢弃已经跑出来的输出，默认超时也从 30 秒放宽到 2 分钟" },
      { category: "fix", text: "修好三个远程工具参数「传了不生效」的问题：命令超时、输出上限、批量操作出错是否中断" },
      { category: "improve", text: "窗口收进托盘或最小化后，各类后台轮询与常驻动画全部暂停，不再空耗 CPU" },
      { category: "improve", text: "数据库日志文件从约 4MB 收回到 0，启动更快" },
      { category: "improve", text: "刷新状态时的界面重绘范围大幅收窄，日志翻页也更省内存" },
      { category: "improve", text: "一个线程常驻跑满一个 CPU 核：`ip_watch.rs` 用 winsock2 `SIO_ADDRESS_LIST_CHANGE` ioctl 监听地址变化，并把 `-1 + WSAEFAULT(10014)` 当成「地址已变化 / 有数据准备好」。实机探针测得该调用在 `cbOutBuffer=0` 时 0.000ms 立即返回 WSAEFAULT —— WSAEFAULT 意为「输出缓冲区参数无效」，即 ioctl 直接失败、通知根本没挂上。于是那个 `loop` 每秒空转数百万次（实测单线程 101.3% 单核，进程合计 125%）。改用 iphlpapi `NotifyAddrChange(NULL, NULL)`（同步阻塞，已用探针验证真的会阻塞）。" },
      { category: "improve", text: "IP 变化检测实际上一直是死的（上一条的连带后果）：假事件洪流让防抖的无限吸收循环 `while timeout(600ms, rx.recv()).is_ok()` 恒真、永不退出，其后的 `refresh_lan_ips` 与托盘提示从来执行不到（只有 5s 轮询那条兜底路径在工作）。现吸收循环加上限（最多 32 条）。" },
      { category: "improve", text: "命令超时就什么都拿不到：`run_command` 超时分支里 `stdout`/`stderr` 是硬编码的空串，已跑出来的输出全部丢弃。按 1281 条真实审计记录，141 次（11%）撞到了 30s 默认超时（而 p50 只有 1125ms，被截断的都是 cargo build 这类长命令）——编译跑了半分钟、报错都刷出来了，调用方却只拿到一个空结果。现默认超时提到 120s，且超时时会等读取线程把缓冲区吐完（最多 100 × 2ms），把真实输出连同 truncated 标记一起带回，并附提示引导长任务用 `background: true`。新增两条回归测试锁住行为。（注：这不等于「超时自动转后台」，那仍待做。）" },
      { category: "improve", text: "三个 MCP 工具参数一直是失效的（传了等于没传）：`run_command` 的 `timeoutMs` / `maxOutputBytes` 与 `batch` 的 `stopOnError`，在工具 schema 里被暴露成 snake_case（`timeout_ms` 等），而服务端反序列化只认 camelCase——于是调用方按 schema 传参会被静默忽略、永远回落默认值（实际踩到：传 `timeout_ms: 180000` 仍按默认超时结束）；另 `search_files` 的 `maxResults` 被误标成必填。根因在 `ToolSchema` 衍生宏：`parse_nested_meta` 的回调未把非目标项的 `= value` 消费掉，解析在第一个不关心的带值项处就报错并被吞，属性里后续项全部读不到（具体丢 `rename` 还是丢 `default` 取决于两者的书写顺序，所以症状看上去毫无规律）。现改为对所有带值项统一先吃下值再取用，并补两条断言 schema 形状的回归测试（这类 bug 不报错、不失败构建）。注：这会使上述参数名在 schema 里改为 camelCase，但它们原本传了也无效，不存在有效依赖。" },
      { category: "improve", text: "新增两道护栏防止同类问题再现：通知 API 返回非预期值时强制退避 2s（绝不空转）；事件频率硬限流 ≤ 5 次/秒。即使将来 API 行为再变，最坏也只退化成每 200ms 一次，不会再烧掉一个核。" },
      { category: "improve", text: "前端常驻脉冲点从动画 `box-shadow`（每帧重绘，且紧邻 `backdrop-filter` 元素会连带大面积背景重新模糊）改为伪元素的 `transform + opacity` 涟漪（纯合成）；`indet-slide` 从动画 `left`（每帧布局）改为 `transform: translateX`；删掉死代码 `uptime-flash` / `.hero-uptime--live`。新增「窗口不可见时暂停全部动画」机制（`data-app-hidden` + `hooks/useAppHiddenFlag.ts`，Rust 侧在托盘 toggle / 托盘菜单显示 / 关窗收托盘 / OS 最小化四处发 `app:visibility`）。暂停范围只限于 7 个常驻 `infinite` 动画，不碰 transition 与一次性入场动画——最初写成 `*` 加 `transition: none !important`，实际跑起来整页白屏（会把正在入场的元素永久冻在 `opacity: 0`），已改窄并在 CSS 里留下警告。另：早前“暂停未生效”的结论已被推翻（那次测量未受控，见清单 M10），信号计算现有单测兜底；但真实 CPU 收益仍待受控复测。" },
      { category: "improve", text: "`ip_watch::spawn` 不再返回 `UdpSocket`（改用 `NotifyAddrChange` 后不再需要用 socket 关闭作为停止信号）；线程以 `tx.send` 失败作为退出条件。" },
      { category: "improve", text: "新增 `desktop/src-tauri/ip_watch_probe.rs`（独立诊断工具，不在 `src/` 下、不参与 Cargo 构建）：一次性验证两个通知 API 的阻塞行为，以后怀疑同类问题直接 `rustc ip_watch_probe.rs -O -o ip_watch_probe.exe` 跑一次即可。" },
      { category: "improve", text: "日志面板取一页不再克隆全量：`audit.rs` 的 `read_page` 原先把缓存里整份 4898 条 / 5.2MB 的解析结果 `clone()` 一份（另有一次 `parsed.clone()`），而调用方只要几十条；前端 10s 轮询一次，即每 10s 白白克隆再丢弃两份 5MB。现改为 `page_slice()` 只克隆本页。（`AUDIT_CACHE` 本身常驻内存的问题仍在，待做。）" },
      { category: "improve", text: "WAL 文件从 3.94MB 降到 0：实测 `cc-bridge.db` 仅 448KB 而 `cc-bridge.db-wal` 高达 4.13MB——SQLite 默认 `wal_autocheckpoint` 是 1000 页（≈4MB），而本应用写入量小、进程常驻，几乎碰不到该阀值。修法分两层：① `wal_autocheckpoint` 设为 256 页（≈1MB 就回写）防它继续长，不设更小是因为太小会把随机写变成频繁 fsync；② 光有①不够——autocheckpoint 只把内容回写主库并重置写指针复用同一个文件，从不缩小文件本身（实测只加①重启后 WAL 依旧 4.13MB），所以又在启动时补一次 `wal_checkpoint(TRUNCATE)` 把残留 WAL 截到 0。实测：WAL 4132392 → 0 bytes、主库 +16KB（内容回写），净省 3.94MB，并减少每次启动的 WAL replay。" },
      { category: "improve", text: "5s 状态轮询不再连带重渲染整个 Tab：有 8 个组件直接吃 `status`，而 `uptimeSeconds` / `stats` 每 5s 必变 → 顶层引用必然是新的，包 `memo` 也挡不住（react-query 的 structuralSharing 只保得住未变字段的引用，救不了顶层对象）。现按变化频率拆订阅：新增 `StaticStatus` / `LiveStatus` 两个类型，主查询用 `select` 剔掉高频字段，`ConnectHero` 用同一 `queryKey` 自行订阅那两个字段（共享缓存，不多发请求），秒级变化只重渲染它自己。注：重渲染范围尚未用 React DevTools 实测确认。" },
      { category: "improve", text: "窗口收进托盘后不再空转轮询：以前窗口不可见时，8 处轮询依旧全速跑——状态（5s）、审计日志（10s）、防火墙诊断（30s）、后台命令列表与输出（各 3s）、运行时长跳秒（1s），其中最重的是状态轮询里带的可达性探针（真开一次 TCP 连接，200ms 超时）。现全部随窗口可见性断流，恢复可见那一刻立即补一次而不干等周期。新增 `lib/appVisibility.ts`（模块级 store + `useSyncExternalStore`）把可见性从“只写 DOM 属性”升级为 React 可订阅状态，并配 6 条单测。一次性倒计时（IP 变化提示）与小时级更新检查有意不断。" },
      { category: "improve", text: "状态查询不再用读锁罩住磁盘与网络 I/O：`get_status` 的 `config` 读锁原本从函数开头一路罩到末尾，横跨了首次防火墙查询（netsh，可达数百 ms）、可达性探针（最长 200ms）与备份目录枚举，于是前端每 5s 轮询一次就造出一个最长 200ms+ 的窗口，期间在设置页改任何配置都得排队等锁。现拆为「短锁取字段 → 锁外做慢操作 → 再取锁读剩下」，锁持有时间降到微秒级。（附：同时用探针实测排除了两个怀疑——备份目录统计在 1230 个文件下只 2.3ms，bash 探测已有缓存，两者都不是瓶颈。）" },
    ],
  },
  {
    version: "2.3.19",
    date: "2026-08-02",
    items: [
      { category: "feat", text: "安装时可勾选一键放行 Windows 防火墙，省去首次启动被拦截的麻烦" },
      { category: "sec", text: "命令拦截从子串黑名单升级为语法感知分析，更精准地拦截危险命令" },
      { category: "improve", text: "日志分页改为窗口内滚动，操作时不再被外层滚动带走" },
      { category: "improve", text: "连接页改用纯 CSS 动态表达，体积更小、运行更顺" },
    ],
  },
  {
    version: "2.3.18",
    date: "2026-07-24",
    items: [
      { category: "fix", text: "用 notifications cargo feature 替代 cfg(not(test))，修复集成测试 0xc0000139" },
    ],
  },
  {
    version: "2.3.17",
    date: "2026-07-24",
    items: [
      { category: "fix", text: "修复 test 二进制加载 GUI DLL 导致 0xc0000139 崩溃" },
      { category: "fix", text: "同步 Cargo.lock 版本号至 2.3.16" },
    ],
  },
  {
    version: "2.3.16",
    date: "2026-07-24",
    items: [
      { category: "feat", text: "新增桌面通知能力与命令安全策略" },
      { category: "fix", text: "修复发版脚本防漏纳门禁误判（已暂存文件被判为漏纳而中止发版）" },
    ],
  },
  {
    version: "2.3.15",
    date: "2026-07-23",
    items: [
      { category: "improve", text: "工具写权限清单改为运行时从注册表推导，移除硬编码" },
      { category: "fix", text: "修复关于卡片进设置页无条件展开回归" },
      { category: "fix", text: "修复状态响应缺 project_path 字段导致前端 project 模式回填空值" },
      { category: "fix", text: "修复停止命令未真正终止后台子进程" },
      { category: "fix", text: "修复备份清理按文件名误删其他备份（改为按原始路径精确匹配）" },
    ],
  },
  {
    version: "2.3.14",
    date: "2026-07-23",
    items: [
      { category: "feat", text: "命令会话持久化新增环境变量跨调用重放：run_command 支持 env 参数，可为会话持久化环境变量（如 venv 的 PATH/VIRTUAL_ENV），默认关闭。" },
      { category: "fix", text: "网络变化时连接页 IP 未刷新：后端改为独立 5s 轮询刷新地址，前端监听 online/focus 事件并在连接页提供手动刷新按钮。" },
      { category: "fix", text: "设置小红点每次进入软件都显示：首屏 status 未加载时不再误判为全部未读；进入设置页即标记已读。" },
      { category: "fix", text: "连接页 4 个卡片默认全展开：改为默认折叠、单选聚焦，老用户不再被引导霸屏。" },
    ],
  },
];
