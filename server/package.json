{
  "name": "cc-bridge-server",
  "version": "1.0.0",
  "description": "cc-bridge: local MCP file bridge server for remote Claude Code",
  "type": "commonjs",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node cc-bridge.js",
    "setup": "node cc-bridge.js --setup",
    "build": "node build.js",
    "test": "node test/mcp-client-test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "postject": "^1.0.0-alpha.6"
  }
}
