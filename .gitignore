# Node
node_modules/
npm-debug.log*

# Runtime state / secrets (per-deployment, never committed)
config.json
audit.log
.cc-bridge-backup/
testroot/

# Build output
dist/
*.blob

# Rust / Tauri
desktop/src-tauri/target/
desktop/src-tauri/gen/
desktop/src-tauri/binaries/

# OS cruft
.DS_Store
Thumbs.db

# Local one-time deployment helper (not project source)
/cc-bridge-publish.sh

# Internal dev docs / design drafts (kept local, not published)
/CLAUDE.md
/功能优化清单.md
/功能优化清单.md.bak
/cc-bridge开发*.md
/design/
/generated-images/
/desktop/src-tauri/icons/backup_*/
/.workbuddy/

# 测试用独立 target 目录：dev 运行时会占住 target/debug 的 exe，
# 跑 cargo test 需用 CARGO_TARGET_DIR=target-test 绕开（见 已经做完的.md L10）。
desktop/src-tauri/target-test/

# 排障用的临时脚本（下划线前缀），不入库。
_clean_locks.ps1

# CI 日志临时文件（排查 mac 烟测时用 gh api 拉下来的，曾被 git add -A 误提交）
smoke*.log
s[0-9]*.log
