# cc-bridge 已经做完的功能优化

> 本文件收录从 `功能优化清单.md` 里**关闭掉**的条目，分两种性质，读时务必分清：
> - **已完成（✅）** —— 下面绝大多数分组，都是真做了。
> - **已决定不做 / 已关闭** —— 见文件末尾同名一节（2026-08-05 归档 24 条），
>   那些**没有实现**，只是不再作为待办。别当成已有功能。
> 提取日期：2026-07-11（E 组于 2026-07-16 补充提取；I / J 组于 2026-07-16 新增；
> K 组 2026-07-31、L 组 2026-08-03；**M / N 组于 2026-08-05 从待办清单迁入**）。
> 原始状态图例：⬜ 待做 / 🚧 进行中 / ✅ 已完成。
> 已完成共 104 条：P 组 12 条、O 组 2 条、体验优化组 8 条、D 组 9 条、E 组 33 条、I 组 4 条、
> J 组 20 条、**M 组 5 条、N 组 11 条**（K / L 组未计入原 88 条口径，条目见各节）；
> 另加核实关闭 2 条（D16 / P6-3）与**已决定不做 24 条**（见末节）。

---

## P 组 · 能力对齐（已完成部分）

### P0-1. 新增 `edit_files` 精准字符串替换 ✅ 【性价比最高】
- **现状**：只有 `write_files` 全量覆盖。改一行也要把整份文件跨公网传上来再传回，token 成本高、易截断出错，无 native 那种"外科手术式改几行"的流畅感。
- **方案**：新增 MCP 工具 `edit_files`，入参 `path` + `oldString` + `newString`（+ 可选 `replaceAll`）。后端：读文件 → 校验 `oldString` **唯一匹配**（非唯一/未找到则报错并提示）→ 替换 → 写回。
- **安全**：走同一条 `resolve_safe_path`（白名单 + canonicalize），受只读模式拦截、写前 backup。**安全模型不变**。
- **涉及（工具三件套）**：`src/mcp/tools/edit_files.rs` 新建 handler → `tools/mod.rs` 声明 `pub mod` → `http.rs` `dispatch_tool` 加分支 + `get_tool_definitions` 加定义 + `WRITE_TOOLS` 数组加入 `edit_files`。README「MCP 工具」表数量 +1。
- **收益**：立刻把体验从 ~80% 拉到 ~90%，这是投入产出比最高的一步。

### P0-2. 读取编码自适应（GBK 等非 UTF-8）✅
- **现状**：`read_files` 用 `read_to_string`（仅 UTF-8），非 UTF-8 文件（GBK/GB18030 等）直接读取失败。用户自身的 NC65 项目正是 GBK 文件，会踩坑。
- **方案**：读取时先探测编码（BOM / 启发式，考虑 `encoding_rs` 或 `chardetng`），非 UTF-8 转码为 UTF-8 返回；`analyze_file` 已有编码探测可复用。写回时保留原编码（可选）。
- **涉及**：`read_files.rs`（可能抽 `security/encoding` 或 `lib`），依赖 `encoding_rs`（注意二进制体积影响，评估后决定）。
- **收益**：中文 Windows 环境下真实可用，避免"读不了自家文件"。

### P1-1. 目录操作 `create_directory` / `remove_directory` ✅
- **现状**：`write_files` 会自动建父目录，但没有独立的 mkdir；`delete_files` 明确不删目录，缺 rmdir。
- **方案**：新增 `create_directory`（幂等 mkdir -p）与 `remove_directory`（可选递归，递归需二次确认语义或参数显式声明）。
- **安全**：同白名单 + 只读拦截；`remove_directory` 递归删除风险高，需在描述中警示并纳入 `WRITE_TOOLS`。
- **涉及**：工具三件套 + README。
- **收益**：补齐目录级操作，减少"只能靠写文件间接建目录"的别扭。

### P3-1. Diff 高亮返回（写类工具返回 unified diff）✅ 【已完成 2026-07-10，定位已校正】
- **来源**：`cc-bridge大幅度增强.md`（2026-07-10 规划）3.1 节，经核实后拆出，是该规划里唯一验证通过、可直接采纳的部分。
- **现状**：`edit_files`/`write_files` 改完文件后，远程 Claude Code 只知道"成功"，看不到具体改了哪些行，没有 native Edit 那种增删高亮，排查改动是否符合预期只能重新整份 `read_files`。
- **方案**：写入前读旧内容，写入后用 `similar`（**核实后确认用最新 3.1.1**，非规划原文写的 2.4——该 crate 真实存在、1.57 亿次下载、Apache-2.0、mitsuhiko 维护，是本次规划里唯一选型正确的依赖）生成 unified diff，通过 `structuredContent.diff` 字段随工具返回结果一起回传。
- **涉及**：`Cargo.toml` 加 `similar = "3.1"` 依赖；新建 `diff_utils.rs`（`unified_diff()`）；`edit_files.rs` 把 `edit_single` 返回值改为 `EditOutcome` 结构体带 `diff` 字段（已有旧/新文本在内存里，零额外 I/O）；`write_files.rs` 仅对非 base64 写入尝试读旧内容生成 diff（旧内容读失败/不存在时按新建文件处理，不报错）。
- **安全**：纯读旧内容 + 生成文本对比，不改变 `resolve_safe_path`/只读拦截/审计等既有约束。
- **重要定位校正（2026-07-10）**：实现前注意到，Claude Code 官方文档并未说明自定义 MCP 工具结果会不会像原生 `Edit` 工具那样被渲染成彩色高亮——很可能不会，那属于内置工具的 UI 特例。因此 `diff` 字段的价值重新定位为：**供远程 Claude Code 自己（作为读结果的 LLM）自我核对改动是否符合预期**，而不是保证人眼能看到终端彩色高亮。
- **收益**：结构化改动摘要让远程 Claude 更容易自核改对了没改错，投入成本低。
- **验证**：版本号同步递增至 2.2.10（`Cargo.toml` + `tauri.conf.json`），远程 `cargo check` 通过（exit code 0），用户确认无报错。

### P-候选. run_command 改用 portable-pty（ConPTY）修复真实子进程输出丢失 ✅【已根原因 + 回退 CREATE_NO_WINDOW，2026-07-10】
- **发现的 bug**：`run_command` 无论前台/后台，只要命令是一个真实独立的 .exe（`hostname.exe`/`git.exe`/`cargo.exe`——不是 `echo`/`type` 这类 cmd 内置命令），stdout/stderr 都读不到任何内容（但 exitCode 一直正确）。根因推测：外层 cmd.exe 用 `DETACHED_PROCESS`（完全无控制台）生成，它再往下 spawn 真实 exe（孙进程）时，Windows 对这种无控制台进程的控制台子系统孙进程处理不干净。
- **候选方案**：改用 `portable-pty = "0.9"` 的 ConPTY 代替 `Stdio::piped()+DETACHED_PROCESS`，保留现有 win32job 集成（`Child::as_raw_handle()` 直接嗂给 `process_job::create_and_assign()`）。
- **取舍（用户确认）**：换回 `CREATE_NO_WINDOW` 能修好输出丢失，但会重新暴露已知的 MSVC 并发链接崩溃（`STATUS_CONTROL_C_EXIT`），用户选择不接受这个取舍，改为写 ConPTY 候选方案。
- **已知行为变化**：PTY 天然只有一路输出，stdout/stderr 合并，`stderr` 字段固定为空。
- **进展（2026-07-10 已收口）**：本地实测确认 `portable-pty` 方案**根本性不可用**——`cmd.exe` 启动后会发 DSR 查询 `\x1b[6n` 并等待终端应答，而 portable-pty 0.9 的 `MasterPty`/`SlavePty` **未实现 `Write`**，无法应答，导致 `cmd` 永久挂起（stdout 空、exitCode 超时）。5/5 候选测试全部失败，验证后判定该路线在 Windows 上走不通。
- **最终决策（用户选回退 CREATE_NO_WINDOW）**：彻底回退 `portable-pty`，改用 `CREATE_NO_WINDOW (0x08000000) | CREATE_NEW_PROCESS_GROUP (0x00000200)` + `Stdio::piped()` 直接 spawn `cmd /C`。stdout/stderr 各自独立管道，真实 `.exe` 子进程（hostname/git/cargo）输出**现在能正确捕获**且两路分离；`Cargo.toml` 已移除 `portable-pty` 依赖。实测 `cargo build` 在 `CREATE_NO_WINDOW` 下**未复现**历史 `STATUS_CONTROL_C_EXIT` 崩溃（与 P2 早期笔记里"DETACHED_PROCESS 才不崩"的判断相反——当前 toolchain 下 CREATE_NO_WINDOW 稳定）。
- **验证**：`run_command` 单元覆盖从 0 → 9（含 `foreground_real_exe_returns_stdout` 直接复现原 bug），`cargo test --lib` 全绿（39 passed/3 ignored）、`cargo clippy` 零警告；版本号同步递增至 2.2.14。

### P4-1. 进程树终止改用 `win32job`（替换三处重复的 taskkill 手写调用，共同修 D18）✅ 【已完成 2026-07-10】
- **现状**：`run_command.rs` 和 `stop_command.rs` 各自手写了一份几乎相同的 `taskkill /T /F /PID` 调用（对应清单 D18 记录的重复代码）。taskkill 还有个本质缺陷：它是按调用时看到的 PID 快照去杀，子孙进程刚 fork 出来还没被系统记录全时可能漏杀；cc-bridge 自身意外崩溃/被杀时，后台命令的子孙进程会变成孤儿，永远无人清理。
- **方案**：改用 **`win32job`**（[crates.io](https://crates.io/crates/win32job)，最新 2.0.3，103 万次下载，安全包装 Windows Job Object 的 API，VS Code/Chrome/Docker Desktop 在 Windows 上管理子进程都用这个系统原语）。每次 `run_command` spawn 时把子进程挂进一个 Job Object，设置 `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`：Job 跟踪所有曾挂在它下面的进程（不管嵌套几层），不依赖某一时刻的 PID 快照，天然没有 taskkill 的竞态问题；cc-bridge 主进程自己崩溃/被杀时，挂在 Job 下的后台命令会被系统自动清理，这是 taskkill 方案完全做不到的。
- **涉及**：实际实现时发现不是两处而是**三处**手写 taskkill（`run_command.rs`/`stop_command.rs`/`commands.rs::stop_running_command`），已新建 `process_job.rs` 封装 `create_and_assign()`，三处调用点统一改用：spawn 时立即用 `child.raw_handle()` 挂进 Job，`run_foreground` 超时/结束时 `drop(job)`，`stop_command`/`stop_running_command` 改为移除 `RunningCommand` 注册项后 drop 即可，不再调 taskkill.exe。`RunningCommand` 结构体新增 `job: win32job::Job` 字段持有它直到被移除。
- **行为变化（有意为之）**：前台命令正常结束后也会 drop job，顺带杀掉可能残留的 detached 子孙进程（如 `cmd /C` 里用 `start` 开出去的后台进程）——以前这类进程会在调用返回后继续存活，现在会跟着一起终止。
- **安全**：仍受 `shell_enabled`/`readonly_mode` 约束，不放宽任何现有护栏，只是把进程终止机制换成更可靠的系统原语。
- **验证**：版本号同步递增至 2.2.9（`Cargo.toml` + `tauri.conf.json`），远程 `cargo check` 通过（exit code 0），用户确认无问题。建议后续跟一次 `cargo clippy` + 实际跑一个 `run_command`/`stop_command` 端到端验证进程树真能被杀干净。
- **收益**：消除孤儿进程风险 + 顺带修掉 D18 的代码重复（三处而不是两处）。
- **迁移收尾（2026-07-10, v2.2.16）**：手写 `win32job` 已进一步替换为社区维护的 **`process-wrap`**（`=8.0.2`，watchexec 出品，`command-group` 官方后继，CI 覆盖 Windows/Linux/macOS）。删除 `process_job.rs`（111 行 Win32 FFI）及其 `#[ignore]` 自伤测试。`process-wrap` 的 `JobObject` 内部先以 `CREATE_SUSPENDED` 启动再 resume，**消除了原"先 spawn 再 assign"的孙进程漏杀竞态窗口**（比手写 win32job 更正确）。终止语义变化：`process-wrap` std `JobObject` 默认**不** kill-on-close，故 `stop_command`/超时分支改为显式 `child.start_kill()`（`TerminateJobObject`）；`RunningCommand.job` 改为 `child: Arc<Mutex<Box<dyn StdChildWrapper>>>`。版本锁 8.0.2 理由：对齐 windows 0.56 避免第二份绑定 + 必须开 `tracing` feature（否则 `job_object` 模块无条件调 `debug!` 编译失败）。验证：`cargo test --lib` 41 passed/0 ignored、`clippy` 零警告。

### P4-2. 目录遍历/通配符匹配改用 `ignore` + `globset`（ripgrep 同款）✅ 【已完成 2026-07-10，仅限 search_files】
- **现状**：`search_files.rs`/`list_directory.rs` 是手写递归异步 walker（`tokio::fs::read_dir` + boxed future 递归），**没有任何 `.git`/`node_modules`/`target` 排除逻辑**，远程搜一个真实 Node/Rust 项目会真的递归进 `node_modules` 里几万个文件，又慢又污染搜索结果。同文件里的 `glob_to_regex` 手写通配符转换，**只支持 `*` 和 `?`**，不支持 `**`、`[abc]` 字符类、`{a,b}` 花括号。
- **方案**：改用 **`ignore`**（[crates.io](https://crates.io/crates/ignore)，最新 0.4.28，**1.44 亿次下载**，ripgrep 作者 BurntSushi 维护，自带 `.gitignore` 感知的目录遍历，默认跳过 `.git`）替换两处手写 walker；用 **`globset`**（[crates.io](https://crates.io/crates/globset)，最新 0.4.18，**1.97 亿次下载**，同样是 BurntSushi 维护，`ignore` 内部依赖它）替换 `glob_to_regex`，获得完整 glob 语义。
- **实际范围（比原计划窄）**：只改了 `search_files.rs`。`list_directory.rs` 这次没动——`ignore::Walk` 是扁平迭代器，与 `list_directory` 现有的嵌套树输出契约（`children: Option<Vec<DirEntry>>`）不契合，重建树需要额外复杂度；且它只列名字不读内容，性能代价远低于 `search_files`。留待真有人反馈再单独处理。
- **涉及**：`Cargo.toml` 加 `ignore = "0.4"` + `globset = "0.4"`；`search_files.rs` 手写 `glob_to_regex`（只认 `*`/`?`）换成 `globset::GlobBuilder`（保留原来的大小写不敏感）；手写递归 walker 换成 `ignore::WalkBuilder`。因为 `ignore::Walk` 是同步/阻塞迭代器，整个遍历+匹配逻辑丢进 `tokio::task::spawn_blocking`，内部改用 `std::fs::read_to_string`（不再混用 async fs）。输出 JSON 结构（`matches: [{path, type, lineNumber, ...}]`）保持不变。
- **踩到的坑（已修正）**：本来想用 `hidden(false)` 保留现有"不隐藏点文件"行为，但实测/查证发现 `hidden(false)` + 默认 `git_ignore(true)` 会让 `.git` 目录本身也被遍历进去（ripgrep 自己的已知行为，[BurntSushi/ripgrep#3099](https://github.com/BurntSushi/ripgrep/issues/3099)，官方 wontfix）。所以额外用 `ignore::overrides::OverrideBuilder::new(root).add("!.git")` 强制排除，不依赖 hidden 开关。
- **不加开关**：用户确认不需要 `respectGitignore` 这类可选参数，默认就遵守 `.gitignore`，与 native Claude Code 本地 Grep/Glob 工具的默认行为保持一致。
- **安全**：仍走现有 `resolve_safe_path` 白名单校验，不引入新的路径解析逻辑；被 `.gitignore`/`!.git` 跳过只是这次搜不到，不影响直接 `read_files` 指定路径读取。
- **验证**：版本号同步递增至 2.2.11，远程 `cargo check` 通过（exit code 0）。
- **收益**：`search_files` 内容搜索不再白白读遍 `node_modules`/`target`（前提是目标项目有干净的 `.gitignore`）；`namePattern` 支持完整 glob 语义（`**`、`[abc]` 等），无条件生效。

### P6-1. `search_files` 并行遍历（对标 ripgrep 速度，吃掉服务端 75% 耗时）✅ 【已完成，2026-07-10 真实审计数据驱动】
- **背景（2026-07-10 实测）**：用 `tools/audit_stats.py` 统计本机真实 `audit.log`（路径 `Roaming/com.ccbridge.desktop/audit.log`，315 条有效记录、服务端总墙钟 72.8s），发现 **`search_files` 独占 54.3s = 74.6%** 的服务端耗时，平均 1698ms、p95=9391ms、最坏 19859ms；而 edit_files 仅 2.1%、read_files 0.4%、write_files 0.0%（读改写本身极快，平均 3–21ms）。即"比 claude code 慢"的体感主因不是改文件慢，而是**搜索慢**——本地 Claude Code 用并行 ripgrep 约 50–200ms 返回，这里每次 1–20s。这也**纠正了早期"审计/备份同步写是主因（D7）"的判断**：那两条只影响已占 ~3% 且很快的读写改路径，ROI 极低，其相对优先级已降级（见 D7 末注）。
- **现状（已读 `search_files.rs`）**：P4-2 把手写 walker 换成了 `ignore::WalkBuilder`，但用的是 **`build()` 单线程串行**遍历整棵树（ripgrep 快是因为 `build_parallel()` 多线程）；内容搜索还把每个文件 `read_to_string` 整读、再把所有行 `collect` 成 `Vec`，大文件/大目录开销明显。当搜索根恰好没有干净 `.gitignore`（如本项目 `desktop/` 直接搜、或撞进 `target/` 千级 `rlib`/`rmeta`）时，整棵树串行扫一遍就是秒级。
- **方案**：
  1. 遍历改 `ignore::WalkBuilder::build_parallel()`，交给 `ignore` 内部的 rayon 线程池多线程走树——这是逼近 ripgrep 速度的关键。
  2. **强制排除 `target/` `node_modules/` `.git/`，不依赖搜索根是否恰好有 `.gitignore`**（这是目前最坏 20s 尾巴的直接来源）。
  3. 跨线程共享原子 `AtomicUsize` 计数 `max_results`，任意线程达标即早停（配合 `ignore::WalkState::Quit` 让并行 walker 提前收尾）。
  4. 内容匹配走字节惰性扫描（`regex::bytes::Regex` 或按行 `BufRead` 流式），**不整读、不 `collect` 全行**；每行命中只推一次结构化结果。
- **涉及**：`mcp/tools/search_files.rs`（把 `walk_search_blocking` 重写为并行版；输出 JSON 结构 `matches: [{path,type,lineNumber,...}]` 保持不变）。仍在既有 `spawn_blocking` 内跑（并行 walk 本就吃满 CPU，不占 tokio reactor）。
- **安全**：仍走现有 `resolve_safe_path` 白名单校验，不引入新路径解析逻辑；强制排除只是这次搜不到，不影响 `read_files` 指定路径直读。
- **验证**：改完用 `tools/audit_stats.py` 复跑真实 `audit.log`，重点看 `search_files` 的 avg/p95 是否降到 100–300ms 量级、`search_files` 总占比是否从 74.6% 跌到个位数；可造含 `target/` 假目录的 fixture 单测验证强制排除生效。
- **收益**：直接抹掉约 75% 的服务端墙钟，是"比 claude code 慢很多"最直接的体感修复；预计 5–20×。
- **2026-07-10 实施记录**：`walk_search_blocking` 已重写为并行版——`build_parallel()` + `Mutex<Vec>` 收集 + 原子 `head_limit` 早停（`WalkState::Quit`）；`OverrideBuilder` 强制排除 `!.git/!target/!node_modules`（不依赖 .gitignore）；`files_with_matches`/`count` 两模式改 `BufRead` 惰性流式（命中即停/只数不存），`content` 模式保留 `read_to_string`（受 `max_file_size` + 强制排除约束）；并行结果按 `path`(+行号) 排序保证确定性。新增单测 `force_excludes_build_dirs_without_gitignore` 固化强制排除；原有 7 个单测全部保留（因排序改为确定性，依赖顺序的断言已安全）。`cargo build --lib` 通过，`cargo test --lib` 全绿（8 passed，含新增 `force_excludes_build_dirs_without_gitignore`）；`cargo fmt` 通过。
  - **提速验证（2026-07-10 复测）**：`tools/audit_stats.py` 复跑本机 audit.log 仍为**旧串行代码**产生的数据（search_files p95=9391ms / max=19859ms / 占比 74.6%），因 P6-1 尚未部署运行；**占比跌到个位数需重新部署 + 真实使用采集新日志后再复跑**。为直接证明新代码速度，新增 `#[ignore]` 基准 `bench_walk_real_project_timing`：真实 cc-bridge 根遍历仅 ~34ms（151 文件）；合成最坏场景（100 源码 + 2900 target、无 .gitignore）新并行+强制排除只扫 100 源码/~22ms、旧串行不排除扫 3000 文件。小文件场景倍数仅 1-2x（**I/O 非瓶颈，并行优势显不出**），**P6-1 的核心价值是强制排除的确定性**——无论 target 多大新代码永不扫构建目录，旧版扫进大 target 时即触发日志里 19.8s 级长尾；真实提速倍数（5-20x）需在真实大构建目录（几千个大文件）下才显著。

### P6-2. `search_files` 强制排除 VCS 元数据 + 文件名搜索按修改时间倒序 ✅ 【已完成，2026-07-10】
- **背景**：native Claude Code 底层 ripgrep 默认排除 `.git/.svn/.hg/.bzr/.jj/.sl` 等 VCS 目录；旧版 `search_files` 强制排除只覆盖 `.git/target/node_modules`，导致 SVN/Hg 老工程（如含数千 `.svn-base` 的目录）仍被整盘扫入。
- **方案（仅限 `search_files.rs`，几行零风险）**：
  1. 强制排除名单补 `.svn/.hg/.bzr`（对齐 ripgrep 默认 VCS 名单）。
  2. 文件名（Glob 类）搜索结果改按修改时间倒序（最近修改优先，对齐 native Glob 相关性）；content（Grep 类）维持 path+行号序。
- **验证**：新增单测 `force_excludes_vcs_dirs_without_gitignore` + `name_matches_sorted_by_mtime_desc_and_strip_internal_field`；`cargo test --lib` 全绿（含 P6-2 两测试）；`cargo fmt`/`clippy` 零警告；版本号 2.2.17→2.2.18。

### P6-3. `search_files` 内容搜索换 grep-searcher 引擎（对标 ripgrep 速度 + 修 GBK 漏搜）✅ 【已完成，2026-07-10，用户选方案 A】
- **背景**：P6-1 解决了"遍历"速度，但内容搜索内核仍是逐行 `BufReader.lines()` + `regex`，与 ripgrep 的 mmap+SIMD+字面量预筛差距明显；且 `.lines()` 遇非 UTF-8 字节 `Err` 中断，GBK 老工程源码被整文件漏搜（隐藏 bug）。
- **方案（仅引 `grep-searcher`+`grep-regex`，不引 `grep-cli` 控制体积，规则8）**：
  1. 内容搜索统一交给 `grep_file()`（grep-searcher 封装）：mmap+SIMD 行扫描 + 字面量预筛（grep_regex 默认开启），大文件 2–5x 提速。
  2. 自动二进制检测 `BinaryDetection::quit(0)`（含 NUL 文件跳过，对齐 native Grep `--text` 默认关）。
  3. Lossy 解码 + Content 模式 `from_utf8_lossy` 取上下文，非 UTF-8 鲁棒，修复 GBK 漏搜。
  4. 三模式 files_with_matches / count / content 全部适配（files_with_matches 命中即停、count 计行数、content 带回溯上下文）。
- **验证**：新增回归测试 `binary_file_is_skipped_like_native_grep`（二进制跳过）+ `gbk_file_searchable_not_skipped`（GBK 搜到，固化漏搜修复）；`cargo test --lib` 全绿 62 passed / 0 failed / 1 ignored；`cargo fmt`/`clippy`（`search_files.rs` 零警告）；release 构建实测 exe 体积（规则8，见下）。版本号 2.2.18→2.2.19。
- **体积实测（规则8，2026-07-11）**：release `target/release/cc-bridge-desktop.exe`，基线 v2.2.16（commit 1c5a9b7，无 grep-searcher）= **13,565,440 bytes (12.94 MB)**；当前 v2.2.19（commit 412deb2，含 `grep-searcher`+`grep-regex`）= **14,041,600 bytes (13.39 MB)**；增量 **+476,160 bytes (465 KB, +3.5%)**。`ignore` crate 基线已有，增量主要来自 grep-searcher + grep-regex 两个 crate（未引 grep-cli）。

### P1-2. `list_allowed_roots` 自动内嵌 CLAUDE.md ✅【已落地，2026-07-11 代码核实】
- **现状**：`list_allowed_roots` 只返回白名单路径，远程 Claude Code 拿不到根目录的项目级指引（CLAUDE.md），每次都要重新 `read_files` 读，浪费 token。
- **实现**：`list_allowed_roots` 返回白名单时探测每个根顶层 `CLAUDE.md`（≤20KB 内嵌 `projectInstructions`，超限给 path+提示）；纯只读，不动 `security/path.rs`。
- **涉及**：`mcp/tools/list_allowed_roots.rs`（内嵌）+ `mcp/http.rs`（文案）。
- **验证**：代码已落地（`list_allowed_roots.rs:13-69` 探测+内嵌逻辑完整）；待 `cargo clippy` 收口。
- **收益**：远程 Claude Code 直接拿到项目指引，少走弯路、省 token。

### P2. 命令执行（Shell）— 完整 shell + 醒目风险开关 ✅【后端+前端均已落地，2026-07-11 代码核实】
- **现状**：此前只有 `run_command` 雏形，未接 `shell_enabled` 风险开关、无前端 UI。
- **实现**：后端 `shell_enabled` 配置全链路贯通——`config.rs` 默认 false / `db.rs` 持久 / `commands.rs` 出入 / `run_command.rs` 校验+`spawn_shell`；前端 `SecurityTab` 风险开关卡 + 红色风险确认弹窗 + 持久徽章。
- **重大安全**：开启 = 授予 RCE，路径白名单/只读/扩展名约束可被 `bash -c` 绕过；仅 Bearer+限流 作防线。`run_command` 自带 cwd 限白名单/超时/输出封顶/stdout·stderr 分离/退出码 + 必审计；一期启发式黑名单兜底（见 D8-续）。
- **验证**：后端+前端均已落地（`shell_enabled` grep 全链路命中、`SecurityTab` 开关 UI 存在）；待 `cargo` 验证。
- **收益**：补齐与 native Claude Code 对齐的命令执行能力，且默认关、有强提醒，安全可控。

---

## O 组 · 可观测性 / 性能诊断（已完成部分）

### O1. 审计日志结构化耗时拆解（总/网络/传输/读写/其它）✅【已落地，2026-07-11 代码核实】
- **现状**：审计只记 `durationMs`，无 4-way 拆解，PerfCharts 诊断结论只能看总耗时。
- **实现**：`audit.rs` 加 `serverMs/ioMs/auditMs/netMs/overheadMs`（`overheadMs = serverMs − durationMs − auditMs` 派生）；新建 `timing.rs`（`task_local!` 埋点 `ioMs`）；`http.rs` 包四点计时（`t_recv` / `server_ms_dispatch` / `io_ms` / `audit_ms`）。
- **安全**：纯观测，不改安全语义；字段 `Option` + `skip_serializing_if=None` 向后兼容。
- **验证**：`audit.rs`/`http.rs`/`timing.rs` 均已落地（`http.rs:151/249-382` 计时链完整）；O2 `PerfCharts` 已写好「单次调用耗时拆解」堆叠条条件分支，O1 落地即自动渲染。待 `cargo clippy` 收口。
- **收益**：瓶颈定位从"总耗时"细到 server/io/audit/overhead，前端图表自动多出拆解维度。

### O2. LogTab 内建分析面板（图表 + 一键诊断）✅ 图表部分已完成（2026-07-10）
- **现状**：日志页只列原始条目；性能分析此前靠外部脚本 `tools/audit_stats.py`（需手动跑 python，且只看 `durationMs`，无 4-way 拆解）。
- **已实现（2026-07-10）**：`src/components/tabs/PerfCharts.tsx`，由 `LogTab.tsx` 在日志表格上方渲染。
  - **零依赖、手绘 SVG**（不引入图表库，保持二进制体积；与"为减小体积砍掉 lucide-react"的纪律一致），在 Tauri webview 离线可用，随浅/深色主题自适应。
  - **纯前端计算**：直接复用 `get_audit_log` 已拉取的 `AuditEntry[]`，**无需后端改动**即可出图。
  - 含 4 张图 + 1 结论：① 各工具耗时占比**环形图**；② 各工具 **P50/P95 对比分组柱**；③ 整体**延迟分布直方图**（0–10/10–50/…/>5k）；④ **最慢 10 次调用**横向条；⑤ **自动诊断结论**（据 p95 判 网络RTT/混合/服务端慢 + 主因工具占比 + 推荐清单条目）。
  - **向前兼容 O1**：一旦 `AuditEntry` 写入 `serverMs/ioMs/netMs/auditMs/overheadMs`，面板自动多渲染一张「单次调用耗时拆解」堆叠条（已写好条件分支，O1 落地即生效，无需再改前端）。
- **可选 / 后置**：Rust 端 `analyze_audit` 命令**暂未实现**——前端直接计算已满足当前 500 条窗口的分析；仅当日志量大到需在服务端聚合、或要做跨会话长期统计时再补。算法原型已验证（对真实 `audit.log` 跑通：p95=508ms、search_files 占 74.6%、占比合计=1.0000）。
- **收益**：不离开 app 即知瓶颈在哪，比看表格/跑脚本直观得多；与 O3 联动可一键把结论交给我出方案。

---

## 体验优化（UI/交互，已完成部分）

### A1. Toast 反馈统一 ✅【已落地，2026-07-11 代码核实】
- **现状**：复制/保存/重生 token 的反馈散乱（alert / 内联文字），无统一 toast。
- **实现**：抽 `ui/toast.tsx`（自写、离线可用、不引第三方保持体积），复制/保存/重生 token 统一走右下角 toast（success/warning 等变体）。
- **收益**：反馈一致、不打断操作流。

### A2. Token 一键复制 + 眼睛切换 ✅【已落地，2026-07-11 代码核实】
- **现状**：token 区只有文本框，复制要点选全选，显示无隐藏切换。
- **实现**：`ConnectTab` token 区加复制按钮 + `eye/eyeOff` 显示/隐藏切换（`ConnectTab.tsx:209`）。
- **收益**：复制一次到位，敏感 token 可临时隐藏防窥屏。

### C7. 审计日志搜索 / 过滤 ✅【已落地，2026-07-11 代码核实】
- **现状**：`LogTab` 只列原始条目，海量审计难定位。
- **实现**：`LogTab` 加关键字搜索框 + 按工具名下拉过滤 + 按状态（成功/失败）过滤三件套（`LogTab.tsx:73-173`）。
- **收益**：快速定位某工具/某次失败的审计，配合导出更顺手。

### A3. 端口占用提示 ✅【已落地，2026-07-11】
- **现状**：端口被占用时只有 log 报错，UI 无提示，用户盲目尝试。
- **实现**：`state.rs` AppState 加 `startup_error: Mutex<Option<String>>`；`http.rs` spawn_mcp_server bind 失败写入错误、成功清除；`commands.rs` StatusResponse 加 `startupError` 字段；`Header.tsx` 胶囊优先展示「启动失败」红态（含具体错误 title），点击跳设置页改端口。
- **收益**：端口占用当场可见，引导用户立即去设置页修改，消除盲试。

### B4. 首次启动引导空态 ✅【核实已落地，2026-07-11】
- **现状**：`SecurityTab.tsx:102-116` 已有白名单为空时的空态引导卡（文件夹图标 + 引导文案 +「添加第一个目录」按钮）。
- **说明**：该功能早已实现，清单长期漏标，本次核对代码后补标。
- **收益**：新用户首次打开不迷茫，明确引导添目录。

### B5. 图标线性化 ✅【核实已落地，2026-07-11】
- **现状**：`ui/icon.tsx` 全部为 `stroke="currentColor" strokeWidth={2}` 内联 lucide 风格线性 SVG；`package.json` 无 `lucide-react`（注释写明为守 3.4MB 二进制主动拒绝引入）。图标集已含 24+ 图标且方便扩展。
- **说明**：该策略早已执行，清单长期漏标，本次核对后补标。
- **收益**：二进制体积零增长，深浅主题同步变色，扩展只需加 SVG path。

### B6. 深色主题层次微调 ✅【已落地，2026-07-11】
- **现状**：暗色主题 `--card 10%`/`--muted 16%`/`--border 19%` 对比偏低，弱光环境难辨层级。
- **实现**：`index.css` `.dark` 块调 `--card 10→12%`、`--muted 16→18%`、`--border 19→22%`、`--muted-foreground 55→65%`。不动色相、不动浅色。
- **收益**：暗色下卡片边界/正文可读性明显改善。

### C8. 配置导出 / 导入 ✅【已落地，2026-07-11】
- **现状**：无导出/导入能力，换机或重置后需重新配置白名单+开关+端口等全部手动。
- **实现**：`config.rs` 加 `save_full_config`；`commands.rs` 加 `export_config`（返回 JSON）/ `import_config`（解析→白名单校验→写回→重启服务）；`main.rs` 注册命令；`SettingsTab.tsx` 加「配置」卡片（导出下载/导入选文件+确认弹窗）。导入复用 `security::path::resolve_safe_path` 兜底校验，不可绕过。
- **收益**：一键备份/迁移全部配置，换机零摩擦。

---

## D 组 · 代码质量与安全（已完成部分）

### D1. 限流键改用真实对端 IP ✅【已修，2026-07-10】
- **原问题**：限流键取 `x-forwarded-for` header，客户端可伪造，任意换 IP 无限绕过限流。
- **修复**：`rate_limit_key` 改取 `ConnectInfo<SocketAddr>` 真实对端 IP，显式忽略 `x-forwarded-for`（仅作审计字段）；保留 `trust_proxy_headers` 配置位（默认 false）。3 条回归测试守住。
- **收益**：修掉一个真安全漏洞，限流变有效。

### D2. `path_locks` 加 GC 回收 ✅【已修】
- **原问题**：`path_locks: DashMap` 加锁后永不删，远程扫目录会撑到几十万条且每个持锁。
- **修复**：`state.rs` 新增 `gc_path_locks()`（retain 清 `strong_count==1` 空闲锁）+ `main.rs` 挂 60s 后台回收任务。
- **收益**：守护型服务长期运行内存稳定。

### D3. `move_files` 源路径加锁 ✅【已修】
- **原问题**：只锁 `to_resolved`，没锁 `from_resolved`，并发同名源 rename 竞态、第二个必失败。
- **修复**：`from_resolved` 与 `to_resolved` 都加 `path_locks`，按路径字典序加锁防死锁，源==目标去重；`move_files.rs` 约 5 行改动。
- **收益**：修掉一个真并发竞态。

### D4. 删除 `security/ratelimit.rs` 死代码 ✅【已删】
- **原问题**：`RateLimiter` 结构体 + 测试从未被任何文件 `use`，实际限流在 `http.rs` inline 实现。
- **修复**：整文件（含测试）删除，`security/mod.rs` 移除 `mod ratelimit;`。
- **收益**：减少误导，未来开发者不会把限流往孤儿类上加。

### D5. `db.rs`/`config.rs` 默认值单源化 ✅【已修】
- **原问题**：`db.rs::ensure_defaults` 与 `config.rs::Default` 字段字面值完全相同，改一处忘改另一处会漂移。
- **修复**：`db.rs::ensure_defaults` 16 项默认值改从 `BridgeConfig::default()` 取（仅 token 随机生成），单一来源。
- **收益**：杜绝默认值漂移。

### D6. 启动期 `expect()` 改干净失败 ✅【已修】
- **原问题**：`main.rs` 启动期 `.expect()`，磁盘满/文件被占用/权限丢失 → 进程立即崩，守护型 MCP 服务静默消失。
- **修复**：5 处 `.expect()` 改 `?` + 中文友好错误，`main()` 返回 `Result<(), Box<dyn Error>>`；顺带 `std::io::Error::other` 消除 clippy 警告。
- **收益**：磁盘问题不再导致服务消失。

### D7. 审计写盘改异步 + 失败上报 ✅【已修】
- **原问题**：审计用 `std::fs` 同步写阻塞请求线程，且写失败 `.ok()` 静默吞掉——磁盘满就静默丢审计（合规命根）。
- **修复**：审计写入改 `tauri::async_runtime::spawn_blocking` 异步落盘 + `log::error!` 上报失败，不再阻塞/吞错。
- **收益**：审计不再丢；高并发下 HTTP 响应不再被审计写盘拖慢。

### D8. `run_command` 0 单测 ✅【已补 9 个单测，2026-07-10】
- **2026-07-10 二次核实**：因 P4-1 加了 win32job 相关代码，行数从 266 涨到 **275 行**，但同目录/文件内仍无 `#[cfg(test)]`/`tests.rs`，**0 测试覆盖确认仍成立**。
- **现状（已解决）**：命令执行是 RCE 级能力，原 `mcp/tools/run_command.rs` 没有任何单元测试。本次重写（移除 portable-pty、改用 `CREATE_NO_WINDOW` + `Stdio::piped()`）后在同文件 `#[cfg(test)] mod tests` 补齐 **9 个单测**，覆盖：cwd 白名单校验 / shell 关闭拒执行 / cwd 为文件拒执行 / 前台 echo 捕获 / **真实 .exe 子进程 stdout 捕获（原 bug 回归）** / exit code 透传 / 输出字节封顶截断 / **stdout·stderr 分离** / 后台注册 handle。
- **收益**：RCE 工具有了最低限度回归保护，原"真实 exe 无输出"缺陷被 `foreground_real_exe_returns_stdout` 固化防止复发。
- **连带修复**：`process_job` 两测试曾把 runner 自身挂入 `KillOnJobClose` Job 导致 `cargo test --lib` 静默自毁，已改 `#[ignore]`（见 CHANGELOG 2.2.14）。
- **2026-07-10 续（v2.2.15）**：补危险命令拦截后新增 3 个单测，`run_command` 单测 9 → **12**，全绿；见下条 D8-续。

### D8-续. `run_command` 命令安全升级（二期，④P0-1）✅ 【已落地，2026-07-24】
- **原问题**：一期黑名单为纯 `to_lowercase().contains` 子串匹配，`echo "rm -rf /"` 误拦、`rm -rf /home` 漏拦、`powershell -c "Remove-Item ..."` / `del /f /s /q` 等均可绕过；`shell_enabled` 开启后近乎等价完全 RCE。
- **实现（Layer 1 常开）**：新建 `command_policy.rs`（语法感知分词 + 引号感知切子命令 + 逐子命令破坏性检测），替换旧子串黑名单。引入 `shell-words` crate（极小纯 Rust，不冲击体积红线）。修掉两类硬伤：引号内危险串不误拦、链式命令（`ls && rm -rf /`）任一子命令危险即拦；裸设备 `\\.\PhysicalDrive` 兜底；fork bomb `:(){:|:&};:` 拦截；大小写不敏感检测；`sudo`/`doas` 前缀跳过。11 个单元测试。
- **实现（Layer 2 opt-in 白名单）**：新增 `command_allowlist_enabled`（默认 false）+ `command_allowlist` 配置字段，三处同步（`config.rs` / `commands.rs` ConfigPatch+StatusResponse+get_status / `types.ts`）。fail-closed 设计：开启且空白名单 = 全部拒绝（避免虚假安全感）。`run_command.rs` 在开关开启时把白名单传入 `validate_command_policy`。Layer 1 不被白名单绕过——即使 `rm` 在白名单，`rm -rf C:\Windows` 仍被 Layer 1 拦截。
- **前端 UI**：新建 `CommandAllowlistCard.tsx`（SecurityTab 安全页「文件管控」之后），三态联动（命令执行未开 → 锁定提示；已开+白名单关 → 仅开关；已开+白名单开 → 程序列表增删），大小写不敏感去重、>3 条搜索、FLIP 动效、删除确认框、保存失败 toast。`save_config` 严格包裹 `patch`（规则 9）。
- **验证**：`cargo test command_policy` 11/11、`cargo test run_command` 21/21（含 Layer 2 集成测试 `layer2_allowlist_enforced_through_config` + fail-closed `empty_allowlist_blocks_all`）；`cargo clippy --no-default-features --all-targets` = 0；`cargo fmt --check` = 0；`npx tsc --noEmit` = 0。
- **涉及文件**：新建 `mcp/tools/command_policy.rs`、`components/tabs/CommandAllowlistCard.tsx`；修改 `Cargo.toml`（+`shell-words`）、`mcp/tools/mod.rs`、`run_command.rs`（删旧常量为策略调用）、`config.rs`（+2 字段）、`commands.rs`（StatusResponse+ConfigPatch+save_config+apply_field）、`types.ts`（StatusResponse+ConfigPatch）、`http.rs`（顺手修 clippy `get(0)→first()`）。

---

## E 组 · 性能优化（2026-07-12 全链路审计，已完成部分）

> 来源：`docs/性能审计报告.md`，逐项经代码核实。本组 P3 中仍有已评估决定跳过/待做的条目留在 `功能优化清单.md`，未全部提取。

### 🔴 P0 · 阻塞工作线程 / 无并发保护

#### E-P0-1. DB 缺少 WAL / busy_timeout / synchronous PRAGMA ✅【已落地：4 PRAGMA】
- `db.rs:init_database` 的 `Connection::open` 后加 `PRAGMA journal_mode=WAL; PRAGMA busy_timeout=5000; PRAGMA synchronous=NORMAL;`。默认 DELETE 日志每次写两次 fsync。

#### E-P0-2. notebook_edit 同步 std::fs 阻塞 tokio 工作线程 ✅【已落地：tokio::fs】
- `notebook_edit.rs` 的 `std::fs::read_to_string` / `std::fs::write` 替换为 `tokio::fs`。NAS/HDD 可阻塞数秒。

#### E-P0-3. list_allowed_roots 同步 std::fs 在 filter_map 闭包中 ✅【已落地：async for + tokio::fs】
- `list_allowed_roots.rs` 的 `std::fs::metadata` + `std::fs::read_to_string` 替换为 `tokio::fs` 或 `spawn_blocking`。

#### E-P0-4. axum 缺少 ConcurrencyLimitLayer ✅【已落地：tower::limit::ConcurrencyLimitLayer(256) + Cargo.toml tower】
- `http.rs:build_router` 加 `tower::limit::ConcurrencyLimitLayer::new(256)`。

#### E-P0-5. edit_files .count() 全量扫描 ✅【已落地：.take(2).count()】
- `edit_files.rs` 的 `.count()` 改为 `.take(2).count()`——`replace_all=false` 时只需判断 0/1/>1，不必扫描整文件。

#### E-P0-6. load_config 22 次独立 SELECT ✅【已落地：单次批量查询】
- `config.rs:load_config` 改为单次 `SELECT key, value FROM config` 批量查询 + 一次性解析。

#### E-P0-7. 每次审计写入都重新 open 文件 ✅【~20 行】
- `audit.rs:write_audit_log` 改用进程级共享 `BufWriter<File>`（`OnceLock<Mutex<Option<(PathBuf,BufWriter)>>>`），写后 flush；`cleanup/clear` 前 `close_audit_writer` 释放句柄避免 Windows rename 占用。高频下省 1000 open/s。

#### E-P0-8. read_files 行范围读整文件进内存 ✅【~30 行】
- `read_files.rs` 新增 `read_range_streaming`：编码可确定时（override 或 detect 关闭）逐行 `split(b'\n')` 流式解码，绝不同时载入整文件；全读路径行选择改惰性 `skip/take`。大文件读局部行不再爆内存。

### 🟡 P1 · 锁竞争 / 重复计算 / 事务缺失

#### E-P1-1. auth_middleware 每请求锁 config 两次 ✅【已落地：合并 token+rate_limit→一次 read】
- `http.rs` 合并 token + rate_limit 参数到一次 `config.read().await`。

#### E-P1-2. analyze_file 每次调用编译正则 ✅【已落地：LazyLock<HashMap>预编译】
- `analyze_file.rs` 用 `OnceLock<Regex>` 预编译所有语言函数模式。

#### E-P1-3. batch 审计写入同步阻塞 ✅【已落地：spawn_blocking】
- `batch.rs` 子操作 `audit::write_audit_log` 改 `spawn_blocking` 或批量写入。

#### E-P1-4. ensure_defaults 18 次独立 INSERT OR IGNORE 无事务 ✅【已落地：BEGIN/COMMIT】
- `db.rs` 用 `BEGIN/COMMIT` 包裹。

#### E-P1-5. save_full_config 22 次独立 INSERT 无事务 ✅【已落地：BEGIN/COMMIT】
- `config.rs:save_full_config` 用 `BEGIN/COMMIT` 包裹。

#### E-P1-6. list_directory 每条目双重 stat ✅【已落地：entry.metadata()】
- `list_directory.rs` 用 `entry.metadata().await` 代替 `tokio::fs::metadata(&full_path).await`。

#### E-P1-7. write_audit_for_call 序列化两次 ✅【已落地：单次序列化+补字段】
- `http.rs` O1 的 auditMs 探针改近似测量（一次序列化）。

#### E-P1-8. 前端 staleTime=0，轮询永不缓存 ✅【已落地：main.tsx staleTime: 3000】
- `main.tsx:QueryClient` 加 `staleTime: 3000`。

#### E-P1-9. 前端全无 React.memo ✅【已落地：Header+ConnectTab】
- Header / ConnectTab / SecurityTab / LogTab / SettingsTab / SettingsToggles 等中型组件包裹 `React.memo`。

#### E-P1-10. securityBadges 数组每帧重建 ✅【已落地：useMemo([status])】
- `Header.tsx:35` 提为 `useMemo`（依赖 status）。

#### E-P1-11. CommandPalette.items 每帧重建 ✅【已落地：useMemo】
- `CommandPalette.tsx:107` 提为静态常量或 `useMemo`。

#### E-P1-12. search_files Content 模式双重读文件 ✅【已落地：grep-searcher 原生 context 零二次读】
- `search_files.rs` 内容搜索 Content 模式改为用 grep-searcher 的 `before_context`/`after_context` 在**同一次 mmap 扫描**内由 `ContentSink` 直接收集命中行及其前后上下文，彻底移除 `std::fs::read` 整文件进堆内存的 O(file_size) 峰值；行为对齐 native ripgrep（相邻匹配合并上下文）。已验证：clippy `--no-default-features` 0 warning / `cargo test --no-default-features` 75 passed 0 fail。

### 🟢 P2 · 内存 / 序列化 / 构建 / 渲染微优化

#### E-P2-1. rate_limiter DashMap 无 GC ✅【已落地：空 entry 时 remove】
- `http.rs` 同 pattern 加 `gc_path_locks` 式周期性清理空 entry。

#### E-P2-2. read_recent_entries 整文件读入内存 ✅【已落地：VecDeque 固定容量】
- `audit.rs` 用 `rev_lines` 或 `VecDeque` 固定容量反向读尾。

#### E-P2-3. write_files base64 解码 O(64) 每字符 ✅【已落地：LazyLock<[u8;128]> 查找表】
- `write_files.rs` 用 `LazyLock<[u8; 128]>` 查找表实现 O(1) 解码。

#### E-P2-4. cleanup_old_entries 同步阻塞启动路径 ✅【已落地：spawn 后台任务】
- `main.rs` 移到 `tauri::async_runtime::spawn` 后台任务。

#### E-P2-5. load_config 部分字段反序列化失败静默 ✅【已落地：parse_or_warn 辅助函数 + log::warn】
- `config.rs` 字段 `.unwrap_or_default()` 失败时至少 `log::warn!`。

#### E-P2-6. vite.config.ts 无代码分割 ✅【已落地：manualChunks vendor-react/query/tauri】
- 加 `manualChunks` 拆 vendor-react / vendor-query / vendor-tauri。

#### E-P2-7. AboutGroup 大量内联 style 对象 ✅
- `AboutGroup.tsx` 约 13 处静态内联 style 提取为模块级 `CSSProperties` 常量，避免每次渲染重建对象（动态 `t.bg/h.color` 保留内联）。
- `AboutGroup.tsx` CSS 类替代或 `useMemo`。

#### E-P2-8. ConnectTab 计算值未 memo ✅【已落地：displayHost/baseCommand/connectCommand useMemo】
- `ConnectTab.tsx` displayHost / baseCommand / connectCommand 用 `useMemo`。

#### E-P2-9. Header 胶囊内联三元过长 ✅【已落地：pillClassName useMemo】
- `Header.tsx` 状态胶囊三态 className 提 `useMemo`。

#### E-P2-10. 深色主题 popover 未跟随 card 提亮 ✅【验证已 12%，B6 改动已覆盖】
- `index.css` `--popover` 调至 12% 与 `--card` 一致。

#### E-P2-11. /health 端点无速率限制 ✅【已落地：10 req/s 轻量限流】
- `http.rs` `/health` 加轻度限流（如每 IP 10 req/s）。

#### E-P2-12. search_files grep regex opt-level=s 非最优 ✅【已落地：profile override opt-level=3】
- `Cargo.toml` profile override：`grep-searcher` / `regex` 设 `opt-level = 3`。

### ⚪ P3 · 体积 / 启动 / 外观（仅已完成条目）

#### E-P3-4. CSS 中硬编码颜色 30+ 处 ✅【已满足】
- 复查 `index.css` 早已全面使用 CSS 变量（`--hero-*`/`--badge-*`/`--version-*` 等），审计样本的 `.c-front/.c-back/.s-row` 在当前代码中并不存在；该项实际已满足，无需改动。

---

## I 组 · 本会话实时发现并修复（2026-07-15/16，已完成）

### I2. `CLAUDE.md` 规则 7“三处修改”文案同步✅【已落地：规则 7 改为描述注册表 register_tool! 流程，同步更新 RFC 状态字段】
- 现象：规则 7 还写着旧的“三处修改（mod 声明 + http.rs 手写 match 分支 + json! schema）”，但实际代码早已按 `proposals/handwritten_dispatch_refactor_rfc.md` 里的方案重构为注册表模式，`http.rs` 的 `dispatch_tool`/`get_tool_definitions` 早已改为遍历 `registry::all_tools()`，根本没有 match 分支/json! 块可改了；RFC 自己的状态字段也还写着“草案（待决策）”，与实际已实施不符。
- 修复：CLAUDE.md 规则 7 改为描述实际流程（写 handler + `#[derive(ToolSchema)]` → `tools/mod.rs` 声明 → `registry.rs` 一行 `register_tool!`，`http.rs` 两个函数不用再改）；RFC 状态字段改为“已实施”并注明落地日期。

### I3. `overheadMs` 在真实运行时恒为 None（G6 新增的端到端回归测试实测抛出）✅【已修复：`write_audit_for_call` 补充 server_ms/audit_ms 后同步重算 overhead_ms，新增 `mcp::http::over_wire_tests::e2e_timing_dimensions_conserve` 回归测试守护，`cargo test --lib` 77 passed】
- 现象：为了补 G6（五维耗时链路缺端到端回归测试）新写了一个真实走 HTTP 全链路、回读真实 audit.log 的测试，第一次跑就失败了：`overheadMs` 在真实运行中恒为 `None`。
- 根因：`http.rs::write_audit_for_call` 先用 `audit::new_entry(...)` 构造一次审计条目，此时 `audit_ms` 还未知（传 `None`），`new_entry` 内部据此把 `overhead_ms` 永久算成了 `None`；后面测完序列化耗时后只补了 `entry.server_ms`/`entry.audit_ms` 两个字段，**忘记同步重算 `overhead_ms`**，导致前端时长拆解面板里这一项长期实际上从未真正显示过。单测测不出来，因为单测直接调 `new_entry` 且一次性把 `audit_ms` 传对了。
- 修复：在补 `server_ms`/`audit_ms` 的同一处加上 `entry.overhead_ms = Some((server_ms as f64 - elapsed as f64 - audit_ms).max(0.0))`。

### I4. G4/G7/G8 三项顺手优化 ✅【均已落地，`cargo check`/`clippy` 均通过】
- **G4**：`registry.rs` 单测删除硬编码 `tools.len()==17` 断言，改为纯不变式校验（非空/type=object/名称不重复），新增工具时不再需要同步改这个数字。
- **G7**：新增 `mcp::http::restart_server(state)` 收拢“abort 旧 handle → sleep 300ms → 重新 spawn”这段重复逻辑，`restart_mcp_server`/`start_mcp_server`/`import_config`（`commands.rs`）+ 托盘菜单 "restart"（`main.rs`）四处改为调用它，各自现有的 `mcp_running`/事件发送行为不变。
- **G8**：`batch` 工具描述补一句 non-transactional 说明（`stopOnError` 为 true 时已完成的写操作不回滚），避免远程调用方误判。

### I1. 备份还原目标反查改用索引表 ✅【已落地：新增 `backup_index` 表 + `list_backups` 精确还原，`cargo check`/`cargo clippy` 均通过，用户确认已完成】
- 现象：白名单已开启时，版本历史弹框里“看改了什么”/“还原”仍为灰，对 `C:\work\aier` 这类深层企业级 Java 仓库尤其明显。
- 根因：旧实现靠在白名单根目录下按文件名做有边界目录遍历反查原始路径（`max_depth=6`、`max_scan=8000`），深度/文件数超限就必然查不到。
- 修复：`backup.rs` 创建备份时把原始绝对路径写入新增的 `backup_index` 表（`db.rs`，非致命写入失败仅警告）；`list_backups` 改为直接查表精确还原，删除旧的 `build_targets_map`/`walk_collect_targets`。涉及 6 个调用点（write/edit/delete/move/copy_files.rs + commands.rs 的 restore_file）传入 db 连接。
- 附带修复（同一测试线程中发现）：`RestoreBackupDialog` 层级 `z-50` 被父弹框 `VersionHistoryModal`（`z-[1000]`）压在下面，改为 `z-[1001]`；全局 toast 容器 `z-[999]` 同样被压住导致还原成功后提示不可见，提到 `z-[2000]`。
- 已知限制（用户确认接受，无需兼容处理）：`backup_index` 表上线前创建的旧备份无对应索引记录，仍显示为无法定位，需重新产生备份才能用新机制。

---

## J 组 · 功能优化清单批量落地（2026-07-16，用户批准后一次性完成）

> 用户请把 功能优化清单.md 里标为“可直接做”的项全部实现，“需确认”项保留在清单里。全部经 `cargo check`/`cargo clippy -- -D warnings`/`cargo test --lib`（78 passed, 12 ignored）/`npx tsc --noEmit` 验证。

### 后端（Rust）
- **F1**：`security/path.rs` 新建路径分支新增 `contains_dotdot()` 显式拒绝 remainder 中的 `..`/`.` 组件，不再依赖 Windows `\\?\` 前缀的实现细节副作用；新增单测 `test_contains_dotdot_detects_traversal_components`。
- **F3**：`security/auth.rs::verify_token` 改为固定缓冲区完全常量时间比较，消除长度侧信道。
- **F4**：复查发现已在运行代码里实现（`edit_files`/`delete_files`/`move_files.rs` 均已接 `path_locks`，`move_files.rs` 还有字典序双锁防死锁），清单此条为过时信息，直接标完成。
- **D13**：删 `db.rs` 重复的 `generate_token`，统一走 `auth::generate_token()`。
- **D18**：`encoding.rs::encode_string` 确认仅测试使用，加 `#[cfg(test)]`。
- **G1**：新增 `main.rs::spawn_supervised()`，将 gc_path_locks/cwd_sessions、本机地址变化检测、后台命令定时清理、防火墙缓存刷新 4 个循环全部改为经它启动：内层 panic 会被 JoinHandle 捕获、记错误日志后 1s 自愈重启，不再永久静默停止。
- **G5**：`cleanup_finished_commands`/`evict_finished_commands` 从 `commands.rs`（Tauri 命令层）收拢为 `state.rs` 的 `AppState` 方法，消除 `mcp/tools/run_command.rs` 反向依赖。

### 前端（React/TS）
- **D12**：新增 `ui/ConfirmDialog.tsx` 统一确认弹窗（`variant: default/destructive`，支持 children 冒出额外内容），替换 `SecurityOverview.tsx`/`SettingsToggles.tsx` 各自一份重复的 `ShellRiskModal`/`ConfirmModal`、`SettingsToggles` 的 `confirmReset` 内联弹窗、`LogTab` 的 `confirmClear` 内联弹窗，共 6 处。
- **D11**：复查发现 `buildConnectCommand`/`ipHint` 已在 `lib/utils.ts`，`ConnectTab` 无硬编码重复拼接，直接标完成。
- **D9**：复查发现 `ConnectTab.tsx` 已拆分为 `connect/{AddressPicker,FirewallAlertBlock,IpChangedBanner,widgets,Steps}.tsx`（590→313 行），但 5 个新文件的相对导入路径均少写了一层 `../`（移进子目录后没同步改路径），导致 `tsc --noEmit` 当前就是报错状态；修正路径后恢复编译。
- **D10**：`SecurityTab.tsx` 复查发现已拆分为 `RunningCommandsCard`/`FileControlCard`（降至 164 行）；`LogTab.tsx`（当时 700+ 行）新拆出 `LogDetailPanel.tsx`（`DetailPanel`/`TimingBreakdown`/`DiffModal`/`RestoreConfirmDialog`）。
- **H1**：`CommandPalette` 的 `act-token`/`act-clearlog` 加 `confirmTitle`/`confirmDescription` 字段 + `pendingConfirm` 状态，不再打字+回车即执行，走新增的 ConfirmDialog。
- **H2**：`SettingsTab.tsx` 的 `ConfigGroup` 选中文件后先暂存为 `pendingFile`弹 ConfirmDialog（展示文件名 + 将覆盖的安全开关范围），确认后才真正 `import_config`。
- **H4**：`SecurityTab.tsx` 删白名单目录改为先弹 ConfirmDialog 再 `removeRoot`。
- **H5**：`SecurityTab.tsx`/`StepAddDir.tsx` 的 `addRoot`/`removeRoot` 补 try/catch + toast 错误提示。
- **H6**：新增 `lib/utils.ts::copyText()` 统一剪贴板复制入口（await/catch，成功/失败分别回调），修复 `ConnectTab`（`handleCopy`/`handlePermCopy`）、`CommandPalette`、`StepConnectCmd`、`StepPickAddress`、`TokenManager`、`connect/AddressPicker`、`connect/FirewallAlertBlock`、`connect/IpChangedBanner`、`LogDetailPanel` 共 9 处未 await/catch 的 `navigator.clipboard.writeText`；`IpChangedBanner` 额外修复了复制失败仍会调 onResolved 让 banner 错误消失的问题。
- **H8**：`DirectoryBrowser.tsx` 的 `currentPath`/`entry.name` truncate 处补 `title=`。
- **H9**：`ErrorBoundary.tsx` 新增“完全刷新”按钮（`window.location.reload()`），与现有“重新加载”（仅重置 React 状态）并列。
- **O3**：`LogTab.tsx` 新增“导出诊断报告”按钮，基于当前筛选结果拼版本/性能摘要/按工具耗时表/错误列表的 Markdown，下载为 `.md` 同时复制到剪贴板。
- **F2（部分）**： README「安全机制」节 + `ConnectTab.tsx` 加传输安全提醒文案（默认监听 0.0.0.0 + 明文 HTTP 需配合 VPN/受信任内网）；真正的 TLS/收紧监听技术修复用户明确表示不做。

---

## K 组 · 防火墙可用性（2026-07-31）

> 起因：用户反馈「必须把防火墙关掉才能访问」。实机排查发现不是防火墙「拦得对」，
> 而是旧版写出的规则残缺 + 检测会假绿。实查用户机器上 8 条入站规则，**全部是 Profile: Public**。

### K1. 规则只落到「公用」配置文件导致完全不生效 ✅【根因】
- **现状**：`open_firewall_port` 写 `add rule name=cc-bridge dir=in action=allow protocol=TCP localport={port} program="{exe}"`，**未指定 `profile=`**。实测仅落到 Public。
- **后果**：Windows 规则只在当前活动配置文件下生效。公司内网 / VPN 网卡常被判为 Domain 或 Private，此时这些规则一条都不生效 —— 用户只能关整个防火墙。
- **修复**：显式写 `profile=any enable=yes`；规则名改为带端口（`cc-bridge (7823/TCP)`）以便改端口时精确删旧建新。

### K2. 状态检测假绿（比根因更难发现）✅
- **现状**：`parse_inbound_allow` 只比 方向/操作/协议/本地端口，**不看**配置文件覆盖、**不看** `program=` 是否指向当前 exe、**不看** Block 规则、**不看** 规则是否启用。
- **后果**：只要有一条历史废规则（指向旧安装路径 / 旧 exe 名）就报「已放行」。UI 绿的、实际不通，用户判定「只能关防火墙」。
- **修复**：新增 `firewall_diag.rs`，取结构化事实后在 Rust 侧判定并产出 8 类问题码。事实来源分两路：配置文件状态（含 netsh 拿不到的 `AllowLocalFirewallRules`）+ 当前网络类别走 PowerShell；入站规则走 `netsh … show rule name=all dir=in verbose`。粗粒度 netsh 判定仍作为最后兜底（`source: netsh`，前端提示诊断能力受限）。
- **为何规则源不用 PowerShell（踩过的坑）**：最初用 `Get-NetFirewallPortFilter` 批量枚举 + 按 InstanceID 关联，实机验证发现**该枚举在普通权限下会被 Access denied 截断**：876 条入站规则只拿到 610 个端口过滤器，462 条（53%）关联不上，**而且恰好包含全部 8 条 cc-bridge 规则**（它们是安全警报弹窗创建的每用户规则）。加上 `-ErrorAction SilentlyContinue` 后失败是静默的，会直接得出错结论（明明有规则却报 noRule）。逐规则关联能拿全但 876×2 次 CIM 查询太慢；netsh verbose 一次调用 3.4s 拿全（含程序路径）。输出走 `cmd /c chcp 65001 && netsh …` 强制 UTF-8，避免中文系统 GBK 下键名乱码。
- **解析失败不得下结论**：`parse_netsh_rules` 返回「解出的规则块总数」，为 0 时调用方回退到粗粒度判定——否则编码/格式异常会被当成「没有任何放行规则」而误导用户。
- **回归测试**：15 个单测。判定侧：Public-only 不覆盖 Private、Any 覆盖、Block 优先、旧路径废规则、域策略不可修复、修复脚本幂等性、批处理元字符拒绝、环境变量路径展开；解析侧：英文 locale、中文 locale（键名+值都中文）、UDP 规则被滤掉、端口 Any/列表/区间命中、乱码输入返回 0 块、全角冒号不 panic。

### K3. 阻止规则（Block）从未被识别 ✅
- Windows 安全警报弹窗点「取消」会自动生成 Block 规则，而 **Block 优先于 Allow** —— 不删它则加多少允许规则都不通。现在会识别并在修复时删除，删除范围严格限于指向本程序的规则，不动别人的规则。

### K4. 修复动作不幂等 + 提权写法隐患 ✅
- 旧版每点一次「一键开放」就新增一条同名规则（实机已累积到 8 条）。现改为先 delete 后 add，且一并清理旧版固定名规则与废规则。
- 旧 `elevate_netsh` 用 `@'`n…`n'@` 拼 here-string，而单引号 here-string 要求 `@'` 后紧跟**真正的换行**，字面 `` `n `` 不会被当换行处理，存在解析失败风险。改为 `elevate_cmd_script`：环境变量传脚本路径 + PS 内部拼引号，`-PassThru` 带回真实退出码（所以「修复成功」的判据是规则真的写进去了），`-WindowStyle Hidden` 不闪黑框，全部动作写入单个临时批处理→**只弹一次 UAC**。

### K5. 已安装用户的常驻操作入口 ✅
- 旧版只有「连接」页告警块里才有按钮，而告警块又因 K2 假绿而不现身 —— 等于没入口。
- 新增设置页「防火墙」**常驻卡片**：域/专用/公用 三芯片标出「当前」与「是否覆盖」+ 问题清单 + 一键修复 / 重新检查 / 手动命令。修复按钮始终可点（幂等重建）。
- 连接页告警块改为诊断驱动：标题按最严重的一条问题生成；只为真阻塞问题现身，健康度问题（废/重复规则）交给设置页卡片。
- 共享零件 `firewall/FirewallPanel.tsx`（hook + 问题清单 + 覆盖矩阵 + 动作组），两处复用，避免判断不一致。

### K6. 域策略与探测降级的诚实提示 ✅
- `AllowLocalFirewallRules = False` 时本机加规则永远无效，旧版会让用户反复点「一键开放」而不知原因。现明确标为 `fixable: false` 并提示需 IT 下发域策略，或改用反向连接（见清单 P7-2）。
- 启动探测从「仅 netsh」改为 `probe_available()`：PowerShell 或 netsh 任一可用即可，两者都不可用才停用查询。

### K7. NSIS 安装期自动配规则（尽力而为）✅
- 新增 `installer-hooks.nsh`（POSTINSTALL 写规则 / PREUNINSTALL 清理），接入 `tauri.conf.json` 的 `bundle.windows.nsis.installerHooks`。
- 局限已知且有意保留：Tauri 默认 `installMode = currentUser` 不提权，此时钩子会失败（不中断安装）；不改 installMode 是为了不让自动更新的静默安装也弹 UAC。详见清单 P7-1。


## L 组 · 常驻高 CPU（2026-08-03）

> 起因：用户反馈「运行状态需要占用 25% 的 CPU」。

### L1. 地址变化监听线程空转，常驻跑满一个核 ✅【根因】
- **现状**：`ip_watch.rs` 用 winsock2 `SIO_ADDRESS_LIST_CHANGE` ioctl 等待地址变化，并在代码里断言
  `-1 + WSAEFAULT(10014)` 是「正常返回码（无 output buffer 时 ioctl 用它表示有数据准备好）」，于是 `tx.send(())`。
- **实测推翻这个断言**（探针 `ip_watch_probe.rs`）：该调用在 `cbOutBuffer=0` 时连续 5 次均以
  **0.000ms 立即返回** `ret=-1 wsaerr=10014`。WSAEFAULT 的真实含义是「输出缓冲区参数无效」——
  ioctl **直接失败、通知根本没挂上**，不是有事件。
- **实测开销**：该进程 125.3% 单核（16 核机器上占整机 7.8%）；按线程拆：一个线程 101.3%（Running）
  + 两个 tokio worker 各 ~20%。加上 webview 的 ~0.6 核，共约 1.85 核——这就是用户在 8 核机上看到 ~25% 的来源。
- **修复**：改用 iphlpapi `NotifyAddrChange(NULL, NULL)`（同步阻塞）。同一探针验证它**阻塞满 10s 被外部 timeout 杀掉**，行为正确。

### L2. 同一个 bug 还让 IP 变化检测从未生效 ✅
- 防抖代码是 `let _ = rx.recv().await;` 后接一个**无限** `while timeout(600ms, rx.recv()).await.is_ok() {}`。
  生产端既然在狂刷事件，`is_ok()` 恒真 → 循环永不退出 → 其后的 `refresh_lan_ips()`、
  托盘 tooltip 更新、IP 变化弹窗**一次都没执行过**（一直是 5s 轮询那条兜底路径在干活）。
- **修复**：吸收循环加 `MAX_COALESCE = 32` 上限。防抖不得因生产端异常而把功能拖死。

### L3. 两道防再发护栏 ✅
- 通知 API 返回非预期值 → 强制退避 2s，绝不立即重试（当年那个 bug 的直接防御）。
- 事件频率硬限流 `MIN_EVENT_INTERVAL = 200ms`（不丢事件，只拉间隔）。即使未来 API 行为再变，
  最坏也只退化成 5 次/秒，不会再烧掉一个核。
- 新增 `desktop/src-tauri/ip_watch_probe.rs`：独立诊断工具（不在 `src/` 下，不参与 Cargo 构建），
  同时探旧/新两个 API 的阻塞行为。以后再怀疑类似问题，`rustc` 直接编译跑一次就有结论。

### L4. 前端常驻动画改为纯合成 + 后台暂停 🟡【代码已改，效果未经可靠实测】
- **实测推翻了最初的假设**：最先以为是「窗口可见才渲染」，所以预期收进托盘就会降。
  实测：隐藏前 61.7% 单核→ 隐藏后 **60.4%，几乎没变**。结论：WebView2 **不会**像浏览器
  隐藏标签页那样自动节流宿主窗口隐藏后的动画。（同时排除：那两个占 CPU 的 `tmp.exe`
  是 `D:\soft\TerminalPro` 的 xterminal，不是 cc-bridge。）
- **真因**：两个常驻无限动画 `.status-pill .p-dot` / `.hero-dot` 动的是 **box-shadow spread**，
  无法走合成层 → 每帧重绘；而它们恰好紧邻 `backdrop-filter: blur(4px)` 的
  `.hero-metric` / `.hero-addr`，一次重绘就连带大面积背景重新模糊。
- **修复**：
  1. 脉冲点改为同尺寸伪元素的 `transform: scale()` + `opacity` 涟漪（`pulse-ring-ripple`），纯合成不重绘；
  2. `data-app-hidden` + `animation-play-state: paused !important`：窗口不可见时暂停全部动画/过渡。
     信号两路取“或”——Rust 侧 `app:visibility` 事件（托盘左键 toggle / 托盘菜单显示 / 关窗收托盘
     三处发出）+ `document.visibilityState`（兜底 OS 级最小化，因为最小化时 `is_visible()` 在
     Windows 上仍为 true）。新增 `hooks/useAppHiddenFlag.ts`。
  3. 清死代码：`uptime-flash` / `.hero-uptime--live`（2.3.19 改纯 CSS 后已无 TSX 引用，
     且它动的是 `color`——同样每帧重绘的错写法，留着是个坑）。
  4. `indet-slide` 从动画 `left`（每帧触发布局）改为 `transform: translateX`。
- **局限**：若系统既不发 visibilitychange、又不走那三个路径（罕见），则不会暂停。
  取舍：宁可漏暂停也不误暂停，后者会让可见界面的动画冻住。

⚠ **为何标 🟡 而不是 ✅（别当它已经验证过）**：
- ~~无论「最小化」还是「关窗收托盘」，CPU 都没降 → `data-app-hidden` 属性没被置上~~
  **该结论 2026-08-03 已推翻**：那次实验未受控（当时两个同名进程同时在跑，
  按进程名采样会同时命中，而安装版 exe 里根本没有这些特征串）。详见清单 M10。
  信号计算现已有单测兜底（`appVisibility.test.ts`，见 L9）。
- 🔥 **踩过的坑：暂停规则写成 `*` 会白屏**。最初写的是：
  ```css
  :root[data-app-hidden] * { animation-play-state: paused !important; transition: none !important; }
  ```
  实际跑起来**整页白屏**。机制：`transition: none` 会把正在做入场过渡的元素
  永久冻在初始状态（`opacity: 0`），`paused` 同样会把一次性入场 keyframe 停在第一帧。
  而暂停的初衷只是省 CPU，本条实测也证明烧 CPU 的是**常驻无限动画**；
  一次性动画与 transition 本来跑完即停，暂停它们只有坏处。
  现改为只列举 7 个 `infinite` 选择器（`.p-dot::after` / `.hero-dot::after` / `.indet-fill` /
  `.changelog-dot` / `.update-glow::after` / `.vh-skel` / `.skeleton`），且不再用 `!important`。
  **新增 infinite 动画时要往那份名单补一行；切勿回到 `*`。**
- 变量对比实验发现测量被窗口遮挡状态支配：同一份 60fps 配置两轮里 GPU 占用
  从 6.8% 到 0.4% 波动（WebView2 遮挡时停止合成），因此无法归因，
  也尝试过的 `steps(8/12/16)` 降帧方案同样无法定量，**未采用**。
- 目前可信的只有原理：box-shadow → transform/opacity 确实从「每帧重绘」变成「纯合成」。
  真实收益得等 M10 把测量手法做对后重测。

### L5. 审计日志取一页要先克隆全量（对应清单 M16 前半）✅
- **现状**：`audit.rs` 的 `read_page` 先用 `Some(entries.clone())` 把缓存里**整份** 4898 条 / 5.2MB
  的解析结果克隆一份，另外还有一次 `parsed.clone()`，而调用方只要一页（默认几十条）。
  前端日志面板 10s 轮询一次，即每 10s 白白克隆再丢弃两份 5MB。
- **修复**：抽出 `page_slice(all, start, page_size)`，用 `iter().rev().skip().take().cloned()`
  只克隆本页需要的那几十条；两处全量 `clone()` 全删。
- **已验证**：现有单测 `read_page_paginates_recent_first` /
  `read_page_cache_consistent_and_invalidates` 均通过，证明分页语义与缓存失效行为未变。
- **注意只解了一半**：这里只消除了「每次取页的临时克隆」，`AUDIT_CACHE` 本身把整份
  audit.log 解析后常驻内存的问题仍在，且随使用时长线性增长——见清单 M16。

### L6. WAL 文件长到本体的 10 倍仍不 checkpoint（对应清单 M15）✅已实测验证
- **实测**：`%APPDATA%/com.ccbridge.desktop` 共 35MB，其中 `cc-bridge.db` 仅 421KB，
  而 **`cc-bridge.db-wal` 高达 4.1MB（本体的 10 倍）**。原因：SQLite 默认
  `wal_autocheckpoint = 1000 页 ≈ 4MB`，而本应用写入量小、进程常驻，几乎碰不到这个阀值。
- **影响不只是磁盘**：WAL 还会拖累启动时的 replay 耗时（与 M9 启动打点相关）。
- **修复**：`db.rs` 初始化 pragma 里加 `PRAGMA wal_autocheckpoint=256;`（≈ 1MB 就回写一次）。
  选 256 而不是更小值：太小会把随机写变成频繁 fsync，反而伤写入延迟。
- ⚠ **光加 `wal_autocheckpoint` 不够——我最初写的验证点本身是错的**。
  原本写“验证点：WAL 应从 4.1MB 降到 ~1MB”，实测直接撞翻：重启后 WAL 依旧是
  4132392 bytes。真实语义是——autocheckpoint 只把 WAL 内容**回写主库并重置写指针
  复用同一个文件**，它从不缩小文件本身。所以它只能防止 WAL 继续长，解不了已经
  长上去的 4MB。（清单 M15 原文其实两个手段都提了，我只实现了前一半。）
- **补的修法**：启动时做一次 `conn.pragma_update(None, "wal_checkpoint", "TRUNCATE")`，
  把残留 WAL 回写并**把文件截到 0**。失败只 `log::warn!` 不中断启动：TRUNCATE 需独占，
  若有其他进程持连接会返 SQLITE_BUSY，而那只是没收到空间、不影响数据正确性。
- ✅ **已实测验证（2026-08-03，新进程 28312 重启后）**：
  ```
  cc-bridge.db-wal :  4132392 → 0 bytes        (mtime = 启动那一刻)
  cc-bridge.db     :   458752 → 475136 bytes   (+16KB = WAL 内容回写主库)
  ```
  两边数据自洽（内容进主库 + 文件截零），净省 **3.94MB**，且与窗口/测量手法无关，
  是本批改动里唯一不依赖 UI 状态就能确认的硬验证。

### L7. 5s 状态轮询引发当前 Tab 全量重渲染（对应清单 M14）🟡【代码已改，重渲染范围未实测】
- **现状**：8 个组件直接吃 `status`（ConnectTab / FileControlCard / SecurityOverview /
  SecurityTab / SettingsTab / SettingsToggles / TokenManager / Header），而全工程只有 3 处 `memo`。
- **关键在于包了 `memo` 也挡不住**：`uptimeSeconds` / `stats` 每 5s 必变 → `status` 顶层
  引用必然是新的；react-query 的 structuralSharing 只能保住**未变字段**的引用，救不了顶层对象。
- **修复**（按字段变化频率拆订阅，而不是加 memo）：
  1. `types.ts` 新增 `LiveStatus = Pick<StatusResponse, "uptimeSeconds" | "stats">` 与
     `StaticStatus = Omit<StatusResponse, "uptimeSeconds" | "stats">`；
  2. `App.tsx` 的主查询加 `select` 剔掉两个高频字段 → 配置类字段未变时引用稳定，下游不重渲染；
  3. `ConnectHero` 自己用**同一个 `queryKey: ["status"]`** 再订一次（共享缓存，不多发请求），
     `select` 只取 `uptimeSeconds` + `stats` → 秒级变化只重渲染它自己。
- **代价**：19 个文件的 `status: StatusResponse` 签名改为 `StaticStatus`（级联修改，编译器已全部卡住）。
- ℹ **为何标 🟡**：类型层面已由 `tsc` 保证正确，但「重渲染真的收窄到只剩 ConnectHero」
  需用 React DevTools Profiler 确认，得等新前端跑起来。
- **后端还没动**：`get_status` 每 5s 仍全量重算重传（clone 整个 config + 排序延迟样本），
  这半边见清单 M2。

### L8. `run_command` 11% 的调用撞 30s 默认超时，且超时丢弃已产出输出（对应清单 M12）✅
- **实测**：1281 次调用中 **141 次（11%）serverMs ≥ 29s**，基本都是撞到 30s 默认超时；
  而 p50 只有 1125ms——说明多数命令很快，被截断的是 cargo build 这类长命令。
- **叠加问题更痛**：超时分支里 `stdout` / `stderr` 是**硬编码的空串**，已跑出来的输出全部丢弃。
  对调用方而言，编译跑了 30 秒、报错信息都刷出来了，却什么也拿不到。
- **修复**（两步）：
  1. `default_timeout_ms()` 从 30_000 提到 **120_000**；
  2. 超时时不再返回空串：最多自旋 100 × 2ms 等读取线程把缓冲区吐完，然后把真实的
     `stdout` / `stderr` 连同 truncated 标记一起带回，并附一句提示引导长任务用 `background: true`。
- **已验证（新增 2 条回归测试，实际执行通过）**：
  - `timeout_preserves_partial_output`：跑 `echo partial_before_timeout && ping -n 20 127.0.0.1 > nul`
    并把 `timeout_ms` 设为 2000，断言 `timedOut == true` **且 stdout 必须包含那句 echo**。
    这条在旧实现下必然失败。
  - `default_timeout_is_not_30s`：锁住 120s 默认值，防以后被无意改回 30s。
- **与 P6-1 的区别（别误以为这条把 P6-1 也做了）**：本条只做到「超时仍杀，但把已有输出带回」；
  Claude Code 的做法是「超时不杀，自动转后台继续跑」，那是更彻底的解法，仍待做（见清单 P6-1）。

### L9. 窗口不可见时 8 处轮询一个不停（对应清单 M1）✅代码+单测，🟡运行时待验
- **现状**：窗口收进托盘后，`get_status`（5s）/ `get_audit_log`（10s）/
  `get_firewall_diagnosis`（30s）/ `list_running_commands`（3s）/ `get_command_output`（3s）/
  运行时长跳秒（1s）全部继续跑。其中最重的是 `get_status` 里带的可达性探针（真开
  TCP 连接，200ms 超时）。
- **关键设计变更**：原 `useAppHiddenFlag` 只往 `<html>` 写 DOM 属性，React 侧拿不到这个
  状态，没法给 `refetchInterval` 用。新增 `lib/appVisibility.ts`：模块级 store +
  `useSyncExternalStore`，一份订阅多处消费。**为何不用 Context**：`App` 自身的
  `useQuery` 也要读它，而 Provider 只能包在 `App` 外层。
- **接入点**（全部改为 `refetchInterval: appHidden ? false : N`）：App.tsx、FirewallPanel、
  LogTab 两处、RunningCommandsCard；`setInterval` 两处（ConnectHero 跳秒、
  CommandOutputPanel 输出拉取）改为 `if (appHidden) return;` + 依赖里加 appHidden。
- **恢复不得干等一个周期**：App 层用 ref 做 hidden → visible 跃迁检测，那一刻立即 refetch；
  首次挂载不重复发请求。CommandOutputPanel 的 offset 存在 ref 里，恢复后只拉增量，
  不丢输出也不重复追加。
- **有意不动的两处**：`IpChangedBanner` 是一次性 60s 倒计时（仅 IP 变化后出现，跑完自停）；
  `UpdateContext` 是小时级更新检查（隐藏时本就该继续）。
- **已验证**：`tsc --noEmit` 零错；新增 `appVisibility.test.ts` 6 条单测全过（共 27 passed），
  覆盖两路信号取“或”、同值去重、DOM 属性同步、dispose 复位、init 幂等。
  为不引入 jsdom 依赖，用 `vi.stubGlobal` 造了最小 document stub。
- 🟡 **未验证的部分**：实际 CPU 下降幅度需重新构建后实测。注意上一轮那个“暂停未生效”
  的结论已被推翻，见 M10 修正。

### L10. `get_status` 的 config 读锁罩住了 I/O 与 await（对应清单 M2）✅
- **先说被实测推翻的部分**：清单原本怀疑“胖响应”是瓶颈（每 5s clone 整个 config、
  排序延迟样本、prune rpm 队列）。逐项查后：这些都是小 Vec 与微秒级内存操作；
  又用探针（`backup_stats_probe.rs`）实测了唯一的磁盘 I/O：`backup_stats()` 在
  1230 个文件 / 24.3MB 下是 **2.25–2.37ms**，且与“不调 metadata”对照组同值
  （证实 Windows 上 `DirEntry::metadata()` 复用 `FindNextFile` 数据、不发额外 syscall）；
  `detect_bash_exe()` 也已有 `BASH_INIT`/`BASH_EXE` 缓存，只首次探测。
  → **“胖响应耗 CPU”这个怀疑不成立。**
- **但量的过程里摸到了一个真问题，且性质完全不同**：`config` 读锁从函数开头一路罩到
  末尾，横跨了三处慢操作——首次防火墙查询（netsh，可达数百 ms）、可达性探针
  （TCP connect，最长 200ms）、备份目录枚举（2.3ms）。于是前端每 5s 轮询一次，就造出
  一个最长 200ms+ 的窗口，期间用户在设置页改任何配置都得排队等锁。
  这不是 CPU 开销问题，是**写入延迟**问题。
- **修复**：拆成三段——① 一次短锁取出慢操作需要的少量字段（probe_host / port /
  backup_dir）立即释放；② 锁外做三处慢操作；③ 再取锁读其余字段（全内存，微秒级）。
  锁持有时间从「含 200ms 探针」降到微秒级。
- **探针为何不降频**（清单原建议 15–30s）：不可达时用户正盯着「连接中断」等恢复，
  拉长周期直接拖慢恢复提示；而 L9 已让窗口不可见时完全不请求，常驻开销已归零。
- **已验证**：clippy 0 警告；`cargo test --no-default-features` 142 passed / 0 failed。
  （`get_status` 是 Tauri command、需真 `State`，未加单测。）
- 🗒 新增 `desktop/src-tauri/backup_stats_probe.rs`（独立诊断工具，不参与构建，同
  `ip_watch_probe.rs` 惯例）：带“不调 metadata”对照组，以后再怀疑目录枚举成本直接跑一次。

### L11. `ToolSchema` derive 静默丢掉 serde 属性，导致 3 个工具参数实际不可用 ✅
- **怎么发现的**：排查另一件事时传 `timeout_ms: 180000` 给 `run_command`，它却仍按
  默认超时结束。一开始怀疑是 L8 的修复没生效，查下去发现是更深的问题。
- **根因**：`macros/src/lib.rs` 里 `get_rename` / `has_serde_default` 都这么写：
  ```rust
  let _ = attr.parse_nested_meta(|m| {
      if m.path.is_ident("rename") { /* 只在这里读 m.value() */ }
      Ok(())
  });
  ```
  `parse_nested_meta` 的回调**必须把每一项的 `= value` 完整消费掉**。遇到不关心的项
  （如 `default = "f"`）时不读它的值，解析位置就停在 `=` 前 → 整个 `parse_nested_meta`
  立即返回 `Err` → 被 `let _ =` 吃掉 → **该属性里后续所有项都读不到**。
- **为何症状看上去毫无规律（实则取决于书写顺序）**：
  | 字段声明 | 丢了什么 | schema 结果 |
  |---|---|---|
  | `#[serde(default, rename = "sessionId")]` | —（`default` 无值可消费）| `sessionId` ✓ |
  | `#[serde(default = "f", rename = "timeoutMs")]` | rename | `timeout_ms` ✗ |
  | `#[serde(rename = "maxResults", default = "f")]` | default | 被误标 required ✗ |
- **实际影响**（均为静默，不报错不失败构建）：
  1. `run_command.timeoutMs` / `maxOutputBytes`、`batch.stopOnError` —— schema 告诉调用方传
     snake_case，而 serde 只认 camelCase，**传了等于没传、永远用默认值**。这三个参数
     对所有 MCP 客户端实际上一直是失效的。
  2. `search_files.maxResults` 被误标成必填（功能正常，但调用方被迫每次都传）。
- **修法**：抽公共的 `for_each_serde_meta()`，对**所有**带 `=` 的项都先当`syn::Expr`
  吃下值再取字符串，保证解析位置总能前进；另兼顾了 `rename(...)` 括号形式（整块吃掉）。
  `get_rename` / `has_serde_default` 改为共用它——一处修正，6 个字段全部归位。
- **为何不算破坏兼容**：schema 名字从 `timeout_ms` 变成 `timeoutMs`、`stop_on_error`
  变成 `stopOnError`，但这些参数原本传了也无效，**没有客户端能有效依赖它们**。
  已 grep 确认前端与 Rust 侧均无硬编码旧名（前端走 Tauri command，不调 MCP 工具）。
- **已验证**：新增 `mcp/tools/mod.rs` 的 `schema_tests` 两条回归测试，分别锁住“rename 写在
  default 之后”与“default 写在 rename 之后”两个方向（这类 bug 不报错，只能靠断言
  schema 形状卡住）。


## M 组 · 性能全面排查（2026-08-03，已完成部分）

> M1 / M2 / M12 三条的正文早已记在下方 L 组（分别对应 L9 / L10 / L8），
> 这里保留的是它们在原清单里的条目与编号映射，便于按 M 编号回查。

### ~~M1. 后台时轮询不停，且 `get_status` 每 5s 真连一次 TCP~~ ✅【已修，详见 L9】
- 8 处轮询全部改为 `refetchInterval: appHidden ? false : N`；两处 `setInterval` 也断了流。
- 新增 `lib/appVisibility.ts`（模块级 store + `useSyncExternalStore`），把可见性从“只写 DOM
  属性”升级为 React 可订阅状态；6 条新单测覆盖。
- 可达性探针**没有**按原计划降频，理由见 L10（不可达时降频会拖慢恢复提示，
  而隐藏时已完全不请求）。

### ~~M2. `get_status` 是胖响应，每 5s 全量重算重传~~ ✅【结论已修正 + 真问题已修，详见 L10】
- **原假设被实测推翻**：“胖响应耗 CPU”不成立。clone config 都是小 Vec、排序/prune 是
  微秒级；唯一的磁盘 I/O `backup_stats()` 实测只 2.3ms（1230 文件），
  `detect_bash_exe()` 已有缓存。
- **真问题是锁，不是胖**：`config` 读锁横跨 netsh / TCP 探针 / 扫盘，每 5s 造出一个
  最长 200ms+ 的窗口阻塞配置写入。已拆为「短锁取字段 → 锁外做慢操作 → 再取锁读剩下」。
- 因此清单原提的“拆高频/低频两份响应”**不做**——没有数据支撑其收益，而前端侧已由
  L7 的 Static/Live 拆订阅解决了重渲染问题。

### M4. webview 进程数 / 内存 —— 根因是环境变量污染，已解决 ✅【前三份数据均作废】

> 本条是一次**排查事故的完整记录**：数据错了三版、根因四次误判，最后发现问题不在代码里。
> 保留全部错路是故意的——它们每一条都是以后会再踩的坑。

#### 最终结论（2026-08-03）
- **根因：一个已卸载的软件在 Machine 级环境变量里留下了**
  `WEBVIEW2_USER_DATA_FOLDER = C:\ProgramData\GameViewer\EBWebView`（GameViewer = 向日葵组件，
  安装目录已不存在，仅环境变量与一个 604MB 的残留目录还在）。
  它强制**本机所有 WebView2 应用**把 profile 写进同一目录，而 WebView2 的规则是
  **同一 user-data-folder 共用一个 BROWSER 进程**。于是：
  1. 谁先启动谁创建那个 BROWSER 进程，**整棵共享进程树就记在它名下**（任务管理器按父进程树聚合）；
  2. 两个 Tauri 应用的 origin 都是 `http://tauri.localhost`，共享 profile 就意味着
     **共享同一份 localStorage**（实测那份数据里同时有 `theme` / `onboarding-done` /
     `changelog_last_seen`，无法分辨哪条是谁写的）——这比内存串台严重；
  3. 共享 BROWSER 进程还带来**稳定性耦合**：别的应用把它搞崩会连带我们。
- **用户删除该环境变量 + 那个 604MB 目录后的真实数据**（任务管理器口径 = 专用工作集）：
  | 阶段 | 合计 |
  |---|---|
  | 污染时期（含 cc-switch 的份）| 120.7 MB |
  | 删环境变量 + 重启后（刚启动）| 60.9 MB |
  | 跑 14 分钟后稳定值 | **88.2 MB** |

  稳定值拆分：Rust 后端 **5.5MB**、renderer 29.2、gpu 27.9、BROWSER 14.6、
  utility×2 9.4、crashpad 1.6 → **webview 占 82.7MB（94%），我们自己的代码只占 5.5MB**。
- ✅ **已做的优化**：新增「关窗时释放界面内存」开关（默认开），关窗销毁 webview，
  托盘常驻从 88.2MB 降到约 5.5MB。实现与风险核实见 CHANGELOG。

#### 四次误判，同一个病根：没先确认“我测的到底是谁”
1. 早期记“webview 6 进程 / ~433MB”——按进程树求和时把**自己跑的 bash / powershell /
   conhost（约 98MB）**也算进去了。
2. 改成“305.7MB，webview 六个 233.5MB”——用 `Split-Path -Leaf` 取 user-data-folder 做归属，
   而**两个应用的目录名都叫 `EBWebView`**，归属直接失效。
3. 又改成“那 6 个进程是向日葵的、cc-bridge 只 27.4MB”——**也错**：父进程链一查
   （BROWSER 的 PPID 就是 cc-bridge）就知道那些确实是我们的。错因：正则用了 `ccbridge`
   而命令行里写的是 `--webview-exe-name=cc-bridge-desktop.exe`（**带连字符**），漏匹配。
4. 全程用 `WorkingSet64`，而用户任务管理器看的是**专用工作集**——Chromium 进程间共享
   大量内存映像，两个口径**差 2.4 倍**（288.9MB vs 120.7MB），沟通时一直在量不同的东西。

#### 以后量这类数据的纪律（每条都是上面某次踩坑换来的）
- **归属用父进程链**（`ParentProcessId` 逐级上溯），不要用 user-data-folder 目录名——
  它可能被环境变量改到第三方目录、且多应用同名。
- **排除自己**：诊断时跑的 bash / powershell / conhost 也是 cc-bridge 的子进程，必须排开。
- **口径跟用户对齐**：用 `Win32_PerfRawData_PerfProc_Process.WorkingSetPrivate`（即任务管理器
  的“内存”列），别用 `WorkingSet64`。
- **先确认窗口真的可见**：`MainWindowHandle != 0` 不代表可见，隐藏/最小化窗口的 handle 也非 0；
  用 `IsWindowVisible` + `IsIconic`。
- **横向对比要同条件**：拿 cc-switch 做对照时，两边必须同样的窗口状态 + 同样的口径，
  否则会得出“它只 20MB 我们 120MB”这种完全错位的结论。
- ⚠ **不要用 P/Invoke `ShowWindow` 程序化切窗口状态来做测量**：绕过 Tauri 自己的窗口
  管理，实测还原后 webview 白屏——测量手段破坏了被测对象。需要切窗口请人工点。

#### 还可以做的（优先级低）
- 窗口打开时那 82.7MB 是 Chromium 进程隔离的硬成本。可选裁剪均不推荐：
  禁 crashpad（省 1.6MB，崩了没 dump）、关 GPU 硬件加速（省 27.9MB，但动画/滚动
  退回 CPU 软件合成，会吃掉 L1/L9 刚拿到的 CPU 收益）、`--single-process`
  （Chromium 官方声明仅调试用、不支持生产）。
- 若将来想让开机自启后也保持 5.5MB：现在启动会主动 `window.show()`（main.rs），
  webview 随之创建。可考虑给 autostart 传 `--autostart` 参数、检测到时不显示窗口（真静默
  启动到托盘）。未做——会改变首次安装体验（用户看不到窗口会困惑），需先想好引导。

### ~~M5. 托盘图标重绘缓存确认~~ ✅【已验证，不是问题，已关闭】
- 2026-08-03 逐行核实：`tray_icon()` 用 `OnceLock` 缓存两张图，`build_tray_icon`
  一辈子只跑 2 次；`Box::leak` 总共泄漏 32KB 且仅一次；`refresh_tray` 还带去重
  （仅 running/tooltip 真变才 set_icon）。无需优化。

### ~~M12. `run_command` 11% 的调用撞 30s 默认超时，且超时丢弃已产出输出~~ ✅【已修，详见 L8】
- 默认超时 30s → 120s；超时不再返回空串而是带回已产出的 stdout/stderr + truncated 标记。
- 已有回归测试 `timeout_preserves_partial_output` / `default_timeout_is_not_30s` 锁住行为。
- **未完的那半：**「超时自动转后台而不杀」（对标 Claude Code）仍待做 → 见 P6-1。

---

## N 组 · macOS 支持（2026-08-03 ~ 08-04，已完成部分）

> 未完成的 mac 条目（N5 左键交互待真机定 / N7 IP 监听空实现 / N8 内存收益需重验 /
> N13 人眼审美 / N14 Dock 激活策略 / N15 `Cmd+W`）仍留在「功能优化清单.md」。

### ~~N1. `firewall_diag.rs` 拆分「类型」与「netsh 逻辑」~~ ✅【已完成 2026-08-03】
- mac 上有 **20+ 个 dead_code 警告**，全来自该文件的 netsh 解析函数
  （`parse_netsh_rules` / `split_kv` / `analyze` / `to_info` / `norm_*` 等）无调用方。
- 因此 CI 的 mac job 目前只能跑 `cargo check`，**不能跑 `clippy -D warnings`**，
  与 Windows 侧严格度不对等（Windows 是 `clippy -D warnings`）。
- **不要用 `#[allow(dead_code)]` 草率压掉**。正确修法要区分两类：
  - **类型定义**（`FirewallDiagnosis` / `Issue` / `RuleInfo` / `ProfileInfo`）——
    `commands.rs` 的 `get_firewall_diagnosis` 返回类型用得到，**必须跨平台保留**；
  - **netsh 解析与分析逻辑**——可整体 `cfg(windows)`。
- 做完后把 CI 里 mac 的 `cargo check` 升级为 `clippy -D warnings`（两处都要改）。
- ✅ **实施**：**没有**用 `#[allow(dead_code)]` 压制，按语义切分：
  - 跨平台保留：4 个类型（`RuleInfo` / `ProfileInfo` / `Issue` / `FirewallDiagnosis`）
    与 5 个纯字符串 pub 函数（`is_batch_safe` / `rule_name` / `build_repair_script` /
    `manual_command` / `profile_label`）——`commands.rs` 与 `state.rs` 无条件用得到。
  - 加 `cfg(windows)`：20 个 netsh 专属私有项（3 struct + 17 函数）。
  - 测试同步切分：18 个测试中 15 个引用 Windows 专属项，加 cfg；余 3 个测跨平台
    pub 函数，保持 mac 上也跑。
- ✅ **CI 已升级**：mac job 从 `cargo check` 改为 `cargo clippy --all-targets -- -D warnings`，
  **与 Windows 侧严格度齐平**，现为 green。
- 📌 **教训（值得记）**：这条来回了四轮 CI 才干净。前三轮都是「枚举语法形态」漏项——
  先只扫 `#[test]` 块（漏了无 `#[test]` 标注的测试辅助 fn `rule()` / `profs()`），
  再只扫 `fn`/`struct`（漏了 `const NETSH_EN` / `NETSH_ZH`），还连带暴露出
  `Deserialize` unused import 与 `ip_watch.rs` 的私有常量 `ERROR_BACKOFF`。
  **正确做法是分析引用关系**：扫测试模块全部顶层项，凡不被跨平台测试引用的就加 cfg。
  另注意 `pub` 项不触发 dead_code（同文件的 `MIN_EVENT_INTERVAL` 就无需 cfg），
  这解释了为何只有部分常量报错。

### ~~N2. 命令壳层在 mac 上仍走 `cmd.exe`~~ ✅【已完成 2026-08-03】
- `shell.rs` 的 `ShellType` 只有 `Cmd` / `Bash` 两个值，`build_invocation` 按 Windows
  形态构造（`cmd /C`）。mac 上必须走 `sh -c` / `zsh -c`。
- 连带：`ShellType::Cmd` 这个名字在 mac 上语义不对，考虑改为「系统默认壳层」语义
  或加 `Sh` 变体（涉及配置值 `shell_type` 的兼容，改名要留旧值解析）。
- ✅ **实施**：`build_invocation` 拆为 `build_invocation_windows` / `build_invocation_unix`
  两个平台专属实现（对外签名不变，`run_command.rs` 无需感知）。`ShellType::Cmd` 在 Unix
  映射为 `/bin/sh`；**配置值仍写 `"cmd"`**（不动用户既有配置），语义变为「平台默认壳层」，
  所以枚举没有改名、也没加 `Sh` 变体——避免配置兼容问题。
- ✅ **顺带修掉一个清单里没记的正确性 bug**：`normalize_cwd_from_shell` 的 MSYS 盘符
  转换在 Unix 上会误伤真实路径——mac 的 `pwd` 输出如 `/a/b` 这种**首段单字符**的路径
  恰好命中那条启发式（第 3 个字节是 `/`），被当成盘符转成 `a:`。常见的 `/Users`
  `/tmp` `/opt` 恰好都不命中，所以极难发现。已加 `cfg(windows)` 并补测试锁住回归。
- ⚠ **仍未验证运行时**：mac 上 `sh -c` / `pwd` 捕获 / 会话 cwd 持久化的真实行为
  都没跑过（CI 只 `cargo check`）。见 N13。

### ~~N3. 工具描述硬编码 Windows 语法~~ ✅【已完成 2026-08-03】
- `mcp/http.rs:564` 的 `run_command` 工具描述写死了：「壳层为 cmd（默认，零外部依赖）：
  请使用 Windows cmd 语法与路径（如 `C:\Users\...`）」。
- **这段描述是发给远程 AI 的**，不改的话 mac 上 AI 会持续生成 Windows 命令与路径，
  哪怕后端已经改成 `sh`。必须按平台动态生成（连 `list_allowed_roots` 等提到路径
  形态的描述一并检查）。
- ✅ **实施**：`run_command_description()` 加平台维度（Unix 上 sh 分支还额外提示
  「sh 不保证支持 bash 专属语法（数组 / `[[ ]]` / shopt），需要时切 bash」）。
- ✅ **范围比原估的大**：`server_instructions()`（**连接时发给 AI 的第一段指引**）里也有
  三处硬编码——「本地 Windows 主机」「本地 Windows 环境」「壳层为 cmd 或 Git Bash」。
  已抽成占位符 + `OnceLock` 缓存拼装，**保持 `&'static str` 返回类型**（HTTP / SSE
  两个调用点都不用改）。刻意**不**按平台复制两份字符串：原注释就强调过要避免两份漂移。

### ~~N4. 防火墙功能在 mac 上应整体隐藏~~ ✅【已完成 2026-08-03】
- macOS 的防火墙模型与 Windows 完全不同：**按应用授权**而非按端口开洞，且默认
  **不拦本机监听端口的入站连接**。移植 netsh 那套没有意义。
- 后端：`firewall.rs` 已有 `cfg(not(windows))` 降级分支（返回 unknown），确认其
  语义在 mac 上合理（不要在 UI 上报「未开放」这种误导状态）。
- 前端：`components/tabs/firewall/FirewallPanel.tsx` 与连接页的
  `FirewallAlertBlock` 在 mac 上应隐藏，或换成「系统设置 → 网络 → 防火墙」的引导文案。

### ~~N6. 快捷键与文案~~ ✅【已完成 2026-08-03】
- `Ctrl` → `Cmd`：命令面板（`CommandPalette`）的快捷键与**提示文字**、
  以及 mac 惯例的 `Cmd+Q` 退出 / `Cmd+W` 关窗。
- 注意与「关窗时释放界面内存」的交互：mac 上 `Cmd+W` 关窗、`Cmd+Q` 才退出，
  而我们已经拦了 `ExitRequested{code:None}`，需确认 `Cmd+Q` 仍能真正退出。
- 文案：UI 与 README 里的「Windows」「cmd」「防火墙」「netsh」等按平台条件显示；
  路径展示的反斜杠 / 斜杠差异。

### ~~N9. 打包配置~~ ✅【已完成 2026-08-04】
- `bundle.targets` 现为 `['nsis']`，需加 `app` + `dmg`；新增 `bundle.macOS` 段
  （最低系统版本、entitlements、`signingIdentity: "-"` 走 ad-hoc）。
- 决定是否出 **universal binary**（Intel + Apple Silicon）还是只出 arm64。
- `installer-hooks.nsh` 是 NSIS 专属，mac 不需要（防火墙规则在 mac 上也不适用）。
- ✅ 已完成，三个决策如下：
  - `bundle.targets` **故意保持 `['nsis']` 不动**，mac 侧用 CLI `--bundles app` 覆盖。
    理由：那条是已稳定的 Windows 发版路径，不拿它做实验。
  - 新增 `bundle.macOS.signingIdentity: "-"`（ad-hoc 签名）。该字段在 Windows 上被忽略，
    零风险。实测 `codesign --verify` 通过，flags=`adhoc,runtime`（带 hardened runtime）。
  - **只出 arm64**（用户已定）；**不出 dmg**（烟测只验过 `--bundles app`，dmg 从未构建过，
    内网分发给 `ditto` 打的 zip 就够，多一个未验证环节不划算）。
- `installer-hooks.nsh` 确认无需处理：mac 上 tauri 不会去碰它。

### ~~N10. CI 增加 mac 构建 job~~ ✅【已完成 2026-08-04】
- 已新增 `build-macos`（仅 tag 触发，跑在 `build-windows` **之前**）：
  - 用正式签名 secret（缺失直接 fail——未签名的包会让 updater 验签报 different key，
    用户下载得到包但永远装不上，烟测里已实际撞过）；
  - 跑 `cargo test --release`：tag 发版时 `check-macos` 被跳过、main 的 push 也不在
    触发列表里，不在这里跑 mac 侧在发版时就**完全没有测试覆盖**；
  - `npx tauri build --bundles app`（`bundle.targets` 仍固定 `nsis` 不动，避开影响 Windows）；
  - 用 `ditto` 打 zip（保留 `.app` 的资源叉与符号链接；普通 `zip` 会破坏 bundle 结构）；
  - **不出 dmg**：烟测只验过 `--bundles app`，dmg 从未构建过，内网分发给 zip 就够。
- **不需要** universal target（用户已定只出 arm64）。
- 🔑 **同时解决了一个容易静默出错的点：updater.json 只能有一份**（attach 到同一个
  Release），而两平台在不同 job 构建、各自只看得到自己的产物。若两边都自行生成并
  attach，后跑的会**静默覆盖**先跑那个平台的条目 → 那个平台的用户失去自动更新且不报错。
  做法：mac 只产出平台**片段**（不 attach `updater.json`），`build-windows` 用
  `UPDATER_MERGE_INTO` 合并后统一 attach；GitHub 与 Gitee 两种 URL 形状各出一份片段。
  - `build-windows` 加 `needs: [build-macos]` + `if: always()`：mac 挂了 Windows 仍能发
    （片段缺失时 generate 脚本只 warn 不 fail，降级为只发 Windows）；
  - 新增显式校验：mac 片段存在却没合并进去就**终止发版**——这种失效不报错，
    必须在发版时就看见；
  - Gitee 镜像步骤补推 mac 的 `.app.tar.gz`/`.sig`，否则 Gitee 里的 darwin 条目指向
    不存在的文件。
- 代价：发版变串行（mac → Windows），多花约 10 分钟。发版不是高频操作，可接受。

### ~~N11. mac 自动更新~~ ✅【硬缺失已修 + 完整链路已真跑验证，2026-08-04】

#### ✅ 已修：mac 自动更新之前是**完全不通**的（不是「陷阱」，是硬缺失）
`generate-updater-json.mjs` 只生成 `windows-x86_64`，`findNsisArtifact()` 硬编码扫
`bundle/nsis`。而 mac 客户端要找的 key 是 **`darwin-aarch64`**（tauri-plugin-updater 的
`updater_os()` 返回 `"darwin"` 而非 `macos`，Apple Silicon 的 arch 是 `aarch64`）。
找不到自己的平台 key 就**永远检测不到更新，且不报错**。
已改为双平台探测，并新增 `UPDATER_MERGE_INTO`（Windows 与 mac 在不同 job 构建、
各自只看得到自己的产物，后跑的若直接覆写会静默抹掉先跑那个平台的条目）。
CI 已实测：只有 mac 产物时正确产出 `darwin-aarch64`；带 merge 时同时含两个平台。
**只出 arm64**（用户已定）；将来改 universal 只需让 `darwin-x86_64` 指向同一个包。

#### ✅ 文档里的三条分析都与源码不符，已根据 `tauri-plugin-updater-2.10.1` 源码更正
`docs/Mac版本支持方案.md` 第 2.2 节那三条都是**推理**，逐条核完源码后：
1. 「每次更新后要重新绕 Gatekeeper」——**不成立**。`install_inner` 全程用
   `tar::Archive::unpack` + `std::fs::rename`，**纯 Rust 文件操作、不经 LaunchServices
   也不调 ditto**，所以新 `.app` 不会被打上 quarantine；而 CI 已实证
   **无 quarantine ⇒ 能启动**。
2. 「装到 `/Applications` 需 sudo → 静默失败」——**不准确**。`rename` 返回
   `PermissionDenied` 时 updater 会走 AppleScript `with administrator privileges`
   提权重试，会**弹系统管理员密码框**，输密码就能装。体验差但能用。
3. 🔴 **文档没提的真实风险**：备份用 `tempfile::tempdir()`（在 `/var/folders`），
   而 `rename` 跨文件系统返回的是 `EXDEV` 而**不是** `PermissionDenied`
   → 代码走 `return Err` 分支、**不会尝试提权、直接失败**。
   同卷时没事，但 app 放在外置盘/另一个卷上就必然更新失败。

#### ✅ 完整链路已真跑通过（2026-08-04，第十三轮）——**N11 完成**

`.github/scripts/mac-updater-e2e.sh`，在真实 macOS 上走完整一遍：
检查 → 下载 → **验签** → 替换 `.app` → 重启能否启动。结果：
| 环节 | 结果 |
|---|---|
| 密钥配对前置检查 | ✅ 签名密钥与应用内置公钥配对 |
| UI 两段式点击 | ✅ `更新 v99.9.9` → `下载并更新` |
| 下载 + 验签 | ✅ 通过 |
| **替换 `.app`** | ✅ 第 1s 发现标记文件，`Info.plist` 版本号已变为 99.9.9 |
| 更新后包的签名 | ✅ `codesign --verify` exit=0 |
| **有无 quarantine** | ✅ **无** —— 这就是「更新后不会被 Gatekeeper 拦」的直接证据 |
| **更新后重启** | ✅ `/health` 200（第 2s）—— **本条担心的核心风险不成立** |

关键实现（为何能在 CI 里做到）：
- `CCBRIDGE_UPDATE_ENDPOINT` 可覆盖更新源，不用改产品代码；
- `endpoints()` 在 release 构建下**要求 https**，而本地自签证书行不通
  （updater 默认 feature 是 `rustls-tls` 且未启用 native-roots）→ 用一个带 run id 的
  临时 prerelease 当 https 源，`trap EXIT` 保证无论成败都删掉，仓库不留痕；
- **不构建两次**：把现有 `.app` 复制一份、改 `Info.plist` 版本号并塞一个标记文件，
  `codesign --force --deep` 重签后 `tar czf` + `tauri signer sign` 就是合法的「新版包」；
- **标记文件才是硬证据**：“进程还活着”证明不了替换发生了。

#### ⚠ 这条跑了七轮才通，四个坑都值得记住
1. **tar 结构**：`install_inner` 用 `entry.path().iter().skip(1)`，包里必须带一层顶层
   目录（`cc-bridge.app/Contents/…`），用 `tar -C 父目录` 才是这个形状。
2. **改了包内容必须重签**，否则新包启动不了，会把“更新后打不开”的原因指错地方。
3. **更新按钮名带动态版本号**（`更新 v99.9.9`），且它只是**开弹框**；
   真正触发下载的是弹框里的「下载并**更新**」（我先前写成「下载并**安装**」，
   差一个字就全落空）。现 `mac-ax-click.sh` 先精确后前缀匹配。
4. 🔴 **最坑的一条：我自己埋的错误前提**。workflow 里原有注释写着「公钥对不上
   只影响客户端校验更新，不影响本 job 要验的行为」——在只验打包时成立，
   但加了更新链路验证后就变成错误前提（验签恰恰是核心）。后果是连续三轮报
   `The signature was created with a different key than the one provided`，
   而该错误**只进应用日志**（得开 `RUST_LOG` 才看得到）。
   现已在构建前把配对公钥写进 `tauri.conf.json`，并在 e2e 脚本里加了公钥比对前置检查。
   写 pubkey 时又撞了一次：`.pub` 文件内容**本身就是 base64**，双重编码会让构建报
   `Missing encoded key in public key`。

#### 🔴 仍未验的一个真实风险（源码层发现，见上方第 3 条）
`rename` 跨文件系统返回 `EXDEV` 而不是 `PermissionDenied` → 不尝试提权、直接失败。
CI 里 `.app` 与 `/var/folders` 同卷，所以这条没被覆盖。**app 放在外置盘/另一个卷上
就必然更新失败**。已写进 README 的「装到哪里」表格（N12 已完成）。

### ~~N12. README 加 mac 安装章节~~ ✅【已完成 2026-08-04】
- 未签名应用首次打开会被 Gatekeeper 拦。**macOS 15 (Sequoia) 起「右键打开」这招失效**，
  必须去 **系统设置 → 隐私与安全性** 点「仍要打开」。需要图文说明。
- 若走内网共享 / 直接拷贝 `.app`（不经浏览器下载），通常不带 quarantine、可直接双击——
  这是本项目的主要分发方式，应作为推荐路径写在前面。
- ✅ 已完成：README 新增「macOS 安装与更新说明」，**结论全部来自真机实测而非推测**：
  - 推荐路径（内网直接拷贝，通常不带隔离属性）写在最前；
  - 经浏览器下载的两种解法：`xattr -dr com.apple.quarantine` 或系统设置放行，
    并标注 macOS 15 起「右键 → 打开」已失效；
  - 「装到哪里」表格：`~/Applications` 无感 / `/Applications` 每次弹管理员密码框 /
    外置盘会失败（EXDEV）；
  - 自动更新可用，且**更新后不会再被 Gatekeeper 拦**（附实测证据）；
  - 与 Windows 版的已知差异（`Cmd+W`、防火墙功能、IP 提示、壳层）。
- 额外加了一条容易让用户困惑的说明：**自己用 `spctl` 查到 rejected 不用在意**——
  它判的是公证策略，ad-hoc 永远过不了；Gatekeeper 实际只拦带 quarantine 的包。
- 构建章节也补了 mac：必须带 `--bundles app`，打 zip 用 `ditto` 而非 `zip`。

### ~~N16. mac 上的前端文案残留~~ ✅【已完成 2026-08-04】
- 设置页「命令执行壳层」：选项仍是 `cmd` / `bash`，文案写「默认 cmd（零依赖）；
  选 bash 走 Git Bash…需本机已装 **Git for Windows**」——在 mac 上这整句都不成立
  （后端已按平台切换为 `sh`/`bash`，见 N2，但前端文案没跟上）。
- 设置页「关窗时释放界面内存」：文案写「约 85MB → 6MB」，那是 **Windows** 的数字；
  mac 实测是释放约 **121MB**（在独立 `WebContent` 进程里，见 N8）。
- 属展示层问题，不影响功能；修法是用已有的 `platform.ts`（N6 建的）按平台切文案。
- ✅ 已完成：文案收进 `lib/platform.ts` 的 `shellTypeCopy()` / `releaseWebviewHint()`：
  - 壳层默认项在 mac 显示为 **`sh`**（Unix 下 `cmd` 实际跑 `/bin/sh`，见
    `build_invocation_unix`），说明改为「macOS 自带 bash 3.2，如需新版用 Homebrew」；
  - ⚠ **存储值仍是 `cmd`/`bash` 未动**——后端 `ShellType` 按这两个值反序列化，
    改了会破坏兼容。单测专门锁了这一点（`key` 是存储值、`label` 只是显示名）。
  - 关窗释放内存的数字按平台分：Windows 85MB→6MB / mac 约 120MB（主进程约 22MB 常驻）。
- 为何放 `lib/` 而不是组件里：`SettingsToggles.tsx` 已 **698 行**、远超规则 7 的 300 行
  上限，不能再往里堆（该文件本身的拆分是独立的技术债，未在本次处理）。
- 新增 `platform.test.ts` 10 条单测，重点锁两个不变量：**mac 文案不得出现
  「Git for Windows」**、**两平台的内存数字不许串**。共 37 passed。

### ~~N17. mac 适配全量排查~~ ✅【已修，2026-08-04】
排查方法（下次再排可直接照搬）：
1. Rust 侧 grep 所有 `#[cfg(windows)]`，**逐个核对是否有配对的 `cfg(not(windows))`**——
   没配对的就是 mac 上默默失效或直接报错的候选。⚙ 注意正则要单独写 `cfg\(not\(windows\)\)`，
   我第一遍的正则漏掉了它，差点得出错结论。
2. grep 所有 `Command::new(` ——看有没无条件 spawn `powershell` / `cmd` / `where` 的。
3. 前端 grep `Windows` / 防火墙 / 盘符 / `.exe` / 安装包 等关键词，看文案。
4. 不能忘的：**发给远程 AI 的工具描述**也是“文案”（registry.rs）。

已修（详情见 CHANGELOG 的「修复（mac 适配全量排查）」）：
- 桌面快捷方式（`powershell` 写 `.lnk`）、打开备份目录（`cmd /c start`）——mac 上两个按钮必报错；
- 安装位置指到 `.app/Contents/MacOS` 包内部；
- `bundle.icon` 没列 `icons/icon.icns`，Dock 图标封顶 256px；
- 桌面通知失败被 `let _ =` 完全吞掉（且插件的 `permission_state()` 是硬编码 `Granted` 的空壳，
  查不出授权状态，`show()` 的 Err 是唯一信号）；
- `stop_command` / `push_notification` 的 AI 描述写着 taskkill / Windows toast
  （前者**连 Windows 上都是错的**，实际是 TerminateJobObject）；
- 后台命令通知 / 关于弹框数字 / 危险命令示例 三处 UI 文案。

✅ 顺带确认没问题、**不要重复排**的：
- 数据目录是 `app_data_dir()`（mac 落 `~/Library/Application Support/`），
  **不在 .app 包内** → 自动更新整包替换不会丢配置/审计/备份。这是排查时最担心的一条，结果是好的。
- 开机自启已传 `MacosLauncher::LaunchAgent`；bash 探测、防火墙整块、盘符根目录、
  快捷键监听、Cargo.toml 分 target 段 都已经是对的。

⚠ 仍未真机验证：mac 桌面通知能不能弹（走 `mac-notification-sys` 的 `NSUserNotification`，
需系统通知设置里放行）。现在至少失败会进日志 + 回传 `pushed:false`，不再静默。

⚠ 本轮修动里 **mac 分支的代码本地编不到**（`cfg(target_os = "macos")` 块与
`open_path` 调用），Windows 侧两种 feature 组合的 `clippy -D warnings` 已过，
mac 侧得靠 `check-macos` job。

✅ 补强（由「MCP 接口需不需要适配 mac」顺藤摸瓜）：**Layer 1 破坏性拦截在 mac 上比 Windows 弱**。
`is_catastrophic_target()` 漏了 `/private`（mac 上 `/etc` `/var` `/tmp` 都是它的符号链接，
拦 `/etc` 不拦 `/private/etc` = 真绕过）、`/Users/<名>`（整个家目录，Windows 等价写法一直拦）、
`/Volumes/<卷>`（等价于 Windows 盘根）。已修 + 两组单测（含「深层项目路径不得误拦」的反向测）。
⚙ 教训：排查平台适配时，**安全规则清单也是一种“平台相关代码”**——它没有 `cfg(windows)`，
grep 不到，但内容完全是按某个平台写的。下次直接把这类“硬编码路径/命令清单”当第 5 步查。

⚠ 发现一个**既有**的 flaky test（不是本轮引入，本轮也没修）：
`mcp::tools::run_command::tests::foreground_stderr_separated` 在全量并行跑时偶发失败
（`handle()` 返回 Err），单独跑必过，再跑一遍全量也过（145+1 → 146+0）。
可能是并发 spawn cmd.exe 的资源竞争或共享静态量。下次碰到不要当成新 bug 追。

⚙ 又一条：`.github/workflows/build.yml` 的触发器
**不包含 `push: branches`**，推 main 不会跑 CI，得 `gh workflow run build.yml` 手动触发。

---

## 清单核实关闭组（2026-08-05 逐条对代码核实）

> 这两条一直挂在待办清单里，实际上一个已经做了、一个担心的问题不存在。
> 清单上的状态标记不能当真，只能回去查代码。

### ~~D16. 签名密钥缺失应 fail-fast~~ ✅【已做，2026-08-05 核实】
- 清单里还写着「需你认可」，实际 `build.yml` 已经在做了：mac 发版 job 的「🔑 准备签名密钥」
  步骤把 `TAURI_SIGNING_PRIVATE_KEY` 去空白后判空，为空则直接 `exit 1`，并把理由写在输出里
  （“updater 验签会报 signature was created with a different key，用户下载得到包但永远装不上”，
  并注明“烟测里已实际撞过这个”）。
- 与清单原描述的差别：没有那个 `ALLOW_UNSIGNED_RELEASE` 逆向开关——但目的（密钥没配好就别发包）
  已达到，而且不给逆向开关反而更安全。

### ~~P6-3. 后台输出看门狗（background size watchdog）~~ ✅【已核实不存在，关闭，2026-08-05】
- 清单原文的待办就是一句「后台读取路径（`get_command_output`）是否同样有上限需先核实」，
  并说明“若后台读取已截断则冗余”。
- **核实结果：已截断。** `get_command_output.rs` 回传 `stdout_truncated` / `stderr_truncated`，
  说明截断在读取线程就已完成，后台与前台走的是同一套 `max_output_bytes` 机制。
- 按它自己写的判定条件，这条属于冗余，无需开工。

---

## 已决定不做 / 已关闭（2026-08-05 从待办清单归档，**不是完成项**）

> ⚠️ **本节与上面各组性质不同**：上面是「做完了」，这里是「决定不做 / 不必做」。
> 放在同一个文件里，是为了让 `功能优化清单.md` 只剩真正的待办；读的时候别把这些当成已实现的功能。
>
> 分三类：**A** 原本就有结论只是还挂在待办里；**B** 条目自己写了「不建议做 / 非必需」；
> **C** 2026-08-05 逐条对代码核实后判断收益不足。**每条都带归档理由，判断错了可以搬回待办。**
>
> 之所以归档而不是删除：里面有花代价换来的调研与踩坑记录（P5-4 查过 Claude Code 两个 issue、
> M10 记着两条测量纪律——未受控测量、`ShowWindow` 污染被测对象），删掉下次会重踩。

### A. 已评估决定不做 / 已搁置（原本就有结论，只是还挂在待办里）

#### ~~M6. release profile 是体积优先，不是性能优先~~ ⏭️【已评估，决定不做，用户确认 2026-08-03】
- **归档理由**：已评估决定不做（2026-08-03 用户确认）
- 现状保留：`opt-level = "s"` + `lto = "thin"`，仅 `grep-searcher`/`grep-regex` 提到 3。
- 曾考虑的选项：热点 crate（tokio / axum / serde_json / similar / encoding_rs）单独提 3，
  或整体 `opt-level=3` + `lto="fat"`。
- **为何不做**：代价是 exe 从 16MB 涨、编译变慢，而实测瓶颈并不在编译器优化档位上——
  M8 已用 4484 条审计数据证明 MCP 协议层开销中位数 0ms、最大 4.63ms；M11 的 59s 是
  目录遍历 I/O bound，提 opt-level 一样救不了。收益不明确而代价确定，故不改。
  若未来出现明确的 CPU bound 热点（且有 profile 数据支撑），可重新评估。

#### P7-1. NSIS 安装期防火墙规则在 currentUser 模式下不生效 ⏸【取舍已定，用户确认 2026-07-31】
- **归档理由**：取舍已定：改 perMachine 会让每次自动更新弹 UAC，代价大于收益
- 现状：`installer-hooks.nsh` 的 `NSIS_HOOK_POSTINSTALL` 会跑 netsh 写入站规则，但 Tauri 默认 `installMode = currentUser`（安装器不提权），写规则需管理员权限，因此该钩子实际上大多数安装会失败（失败不中断安装，仅 DetailPrint 提示）。
- 为何不改：改成 `perMachine` / `both` 后，自动更新的**静默安装也需提权**，会让每次自动更新都弹 UAC（或直接失败），代价大于收益。
- 现行兜底：首次启动后「连接」页告警块 / 「设置」页防火墙卡片的「一键修复」（单次 UAC）。若后续确认要做到零操作，需先验证 updater 在 perMachine 下的静默安装行为。

#### P3-2. `run_command` 持久化会话（portable-pty）⏸️【搁置，用户确认】
- **归档理由**：已搁置：有低成本 workaround（每次显式 cwd / && 拼接）
- 结论：痛点有低成本 workaround（每次显式 cwd、`&&` 拼接、后台轮询），真实需持久会话场景少，搁置。

#### P5-4. 补充图片/PDF 可视化读取能力 ⏭️【已评估，决定不做，2026-07-16】
- **归档理由**：已评估决定不做（被 Claude Code 客户端两个 issue 卡死，非本项目能解）
- 背景：对比本地 Claude Code 能力做的一次全面盘点中发现，`read_files`/`analyze_file` 目前对二进制文件只做 `guess_encoding` 判为 `"binary"` 后跳过（见 `analyze_file.rs` 的 `analysisNote: "binary file: line/function/class counts skipped"`），完全没有解码图片/PDF 内容并返回给模型看的能力（经磁盘代码核实，非猜测）。
- **可行性验证（2026-07-16，非猜测，查证 Claude Code 官方 GitHub issue）**：MCP 协议本身支持工具结果带 `image` content block（base64 + mimeType），cc-bridge 服务端（`mcp/http.rs` 的 body limit / gzip 层）没有结构性障碍，服务端能做。**但客户端（Claude Code）对第三方 MCP 工具返回的图片有两个已知限制**：
  - [issue #31208](https://github.com/anthropics/claude-code/issues/31208)：第三方 MCP 工具的 `image` block 会被转成文字描述喂给模型，而不是像内置 Read 工具那样走原生多模态图片块，单张图片多耗 10-20 倍 token。
  - [issue #53256](https://github.com/anthropics/claude-code/issues/53256)：claude.ai 网页/桌面端默认把 MCP 返回的图片折叠进手风琴框，不手动展开看不到。
  - 结论：即使 cc-bridge 服务端改造做对，客户端这边的效果也达不到「像本地 Read 工具一样直接看图」，这是 Claude Code 客户端侧的限制，cc-bridge 单方面解决不了。
- **PDF**：MCP 协议没有定义 `pdf` content type，只能「逐页渲染成图片再返回」（继承上面图片的客户端限制）或「纯文本抽取」（`pdf-extract`/`lopdf` 之类，成本低但对扫描件/复杂版式 PDF 无效）。
- **决定（用户确认）**：不做。若未来 Claude Code 客户端修复了上述两个 issue，或有明确的「只需读文字版 PDF 纯文本」的需求，可重新评估缩小范围的方案。

#### E-P3-1. png crate 仅用于解码一次 icon.png 可移除 ⏭️【已还原】
- **归档理由**：已还原：移除 png 直接依赖收益≈0，风险>收益
- 本 Tauri 版本无 `Image::from_bytes` API；且 png 已被 Tauri 自身的 `image` 依赖传递链接，移除直接依赖的二进制收益≈ 0，风险（图标解码失败）>收益，故保留 `png` crate。

#### E-P3-2. create_dir_all 同步调用（启动期一次性）⬜⏭️【跳过】
- **归档理由**：已评估跳过：启动期一次性，改 tokio::fs 反而有 panic 风险
- 启动期一次性、创建空目录开销可忽略；在同步 `setup` 闭包中改 `tokio::fs` 需 `block_on`，可能触发「无运行时」panic，零收益+启动风险，故不改。

#### E-P3-3. 网络变化检测 15s 固定轮询 ⬜⏭️【跳过】
- **归档理由**：已评估跳过：需平台特定 SDK，收益零
- 审计已认定「非瓶颈」；改为 OS 网卡事件通知需平台特定 SDK（Windows `NotifyAddrChange`/macOS `SystemConfiguration`/Linux netlink），跨平台改动大、收益零，故不改。

#### E-P3-5. db Mutex 单连接（低并发无影响）⬜⏭️【跳过】
- **归档理由**：已评估跳过：单客户端场景 Mutex 够用，零收益
- 单客户端 MCP server，单 `Mutex<Connection>` 已够用；加 r2d2 连接池需改动 4+ 处 `state.db.lock()` 调用点且零收益，故不改。

#### F2. 默认监听 `0.0.0.0` + 明文 HTTP 传输 token ⏸️【文案提醒已做，技术修复用户确认暂不做】
- **归档理由**：用户明确表示不做；文案提醒已落地
- `config.rs` 默认 host 为 `0.0.0.0`，`mcp/http.rs` 无 TLS，Bearer token 全程明文，同网段可嗅探。
- 2026-07-16：已在 README「安全机制」节 + 连接页加传输安全提醒文案（纯告知，不改变任何实际行为）。
- 真正的技术修复（自签名 TLS 选项 / 收紧默认监听到具体网卡）用户已明确表示“这个不做了”，不再排期，除非以后主动提起。

---

### B. 条目自己写了「不建议做 / 非必需」

#### M13. `stop_command` 曾出现 87s —— 样本不足，待观察 ⬜🟢
- **归档理由**：条目原文即「样本太少（n=6），不单独立项修，先观察」。3 次异常全在 7/19 同一天，之后 3 次 ≤2ms，至今未再现
- 6 次调用里 3 次异常（55583 / 8000 / 87252 ms），**全部集中在 7/19 同一天**，且 `success: true`（不是报错，是真的等了 87 秒）。
- 之后 3 次（7/29、7/30）均 ≤ 2ms。process-wrap 改造是 7/10（更早），所以不能简单归因于那次改造。
- 假设（未证）：当天被杀的是 cargo/link.exe 进程树，等 stdout/stderr reader 线程退出时被持有管道的孙进程卡住。
- 样本太少（n=6），不单独立项修，先观察；若再现则按上述假设查 reader 线程 join。

#### P6-2. bash 环境变量快照（env snapshot）⬜🟡
- **归档理由**：条目原文即判定「非必需」：强需求只出现在持久会话场景，而 P3-2 已搁置
- **对标 CC**：执行前对环境变量做快照（`ShellSnapshot`），保证多次执行的 env 一致、可复现，且不受上一条命令 `export` 副作用污染。
- **现状**：pipe 模式直接继承 Rust 父进程环境，每次执行 env 同源（同一父进程派生），天然一致。
- **延后理由**：env 快照的强需求出现在「持久会话」场景（会话间环境漂移）；当前无持久会话（P3-2 已搁置），pipe 模式每次都是新 shell + 同一父 env，非必需。若未来启用持久会话再补。

> P6-3（后台输出看门狗）已于 2026-08-05 核实关闭：后台读取路径本来就有截断
> （`get_command_output` 回传 `stdout_truncated`/`stderr_truncated`），按它自己写的
> 判定条件属于冗余。记录见「已经做完的.md」的核实关闭组。

---

#### O1-b. 网络 RTT 探针（估 `netMs`，带开关）⬜🟢
- **归档理由**：条目原文即建议「不主动排期」：纯诊断价值，客户端 curl -w 零改动可替代，而 Windows SIO_TCP_INFO FFI + hyper 插桩成本不低
- **优先级（2026-07-13 评估）**：🟢 顺手优化、不紧急。纯诊断价值（不提升实际性能，只帮分辨“网络慢还是服务端慢”），受众窄（仅自己排查性能时用得上），而 Windows `SIO_TCP_INFO` FFI + axum/hyper 插桩改造成本不低。客户端自己用 `curl -w '%{time_total}'` 能零改动覆盖大部分排障需求。**建议保持 ⬜ 待做、不主动排期，除非未来多次出现“分不清是网络还是服务端慢”的具体场景。**
- 按 `source_ip` 周期 TCP 探针估 `netMs`；`net_probe_enabled` 默认 false；失败则省略、分析改用 `transportMs`。
- **技术方案（2026-07-13 补充，来源：远程 MCP 测试发现 `netMs` 恒 None 后的方案讨论）**：采用「TCP 层内核 RTT」，不做「客户端配合打点」。
  - **原理**：连接建立后，服务端从底层 socket 读内核维护的 TCP RTT（Linux `getsockopt(fd, IPPROTO_TCP, TCP_INFO, ...)`；Windows 用 `WSAIoctl(SIO_TCP_INFO)`，需 Win10 1703+），**不依赖调用方（Claude Code 等任意 MCP 客户端）配合**，方案更现实。
  - **已排除方案**：让客户端在请求里带发送时间戳、服务端回显估算往返——因 cc-bridge 面对任意第三方 MCP 客户端，无法要求对方实现私有协议扩展，可行性低，不采用。
  - **工程难点**：axum/hyper 把 socket 包一层后不易直接拿到原始 fd/handle，需要在 accept 连接那一层单独抓一次 RTT，存到连接级状态（如以 `source_ip`/连接 id 为 key），再在 `tools/call` 分发时查表关联进 `net_ms`；Windows 版 API 比 Linux 冷门、需在真实 Windows 环境验证。
  - **语义说明**：拿到的是内核平滑 RTT（类似 ping），是“这次请求走了多久网络”的近似代理值，不是逐字节精确计时；对小请求场景够用。

---

#### N14. Dock 图标与激活策略（新增，2026-08-04 由 CI 截图发现）⬜🟡【待产品决策】
- **归档理由**：条目原文即「建议先保持现状（不动），等真实用户反馈」；且与 N6 的 Cmd+Q 直接冲突
- 截图里看到两件 Windows 上不存在的事：应用在 **Dock 里占一个图标**，
  且菜单栏顶部有一整套应用菜单（cc-bridge / File / Edit / View / Window / Help）。
- 定位上我们是「启动即收托盘的常驻服务」（`visible: false`），mac 上这类应用
  通常用 `ActivationPolicy::Accessory` 隐掉 Dock 图标，只留菜单栏图标。
- **但不能直接改**，有真实取舍：
  - 改为 Accessory 后，应用菜单栏也会消失，**`Cmd+Q` / `Cmd+W` 这些快捷键随之失效**
    （它们靠的就是默认菜单）——而 N6 刚把快捷键标签改成了 `Cmd`，且 CI 刚证实
    `Cmd+Q` 能真退出。两者直接冲突。
  - 另一种做法是保留 Regular 策略（Dock 有图标），反正应用本身有主窗口。
- 因此归为**产品决策项**而非 bug：要么接受 Dock 图标、保留快捷键；要么走 Accessory
  并自己注册全局快捷键补回 `Cmd+Q`。建议先保持现状（不动），等真实用户反馈。

### C. 2026-08-05 判断收益不足而归档

#### M3. 前端 CSS 117K（比 JS 还大）⬜🟢
- **归档理由**：116KB CSS 走的是本地 WebView、不过网络，解析成本几毫秒，无用户可感收益；而条目自己说「真要减得先找死类」，需要类名与 tsx 的交集分析，并不顺手
- **先修正一个误判**：本来怀疑“Tailwind purge 没配”。实查后发现**根本没有 tailwind.config**
  （Tailwind v4 是 CSS-first 配置，purge 由 Vite 插件自动扫描）。所以不是没裁干净，
  而是 `index.css` 本身就有 1000+ 行**手写语义类**（.divider-x / .interactive / .card-lift / .hero-* …）。
- 真要减得先找死类：把 index.css 的类名与 tsx 引用做交集，删无人用的
  （已抓到一个实例：`.hero-uptime--live` 已死，已删）。

#### M7. 628 crate / 16MB exe 的 feature 裁剪 ⬜🟢
- **归档理由**：收益是体积与启动 I/O。exe 15MB、安装包 4.48MB 已很小，且启动耗时至今没有任何打点数据（M9 未做）——为一个未被证明存在的问题做裁剪。若 M9 打点显示启动慢，再捡回
- 逐个查可裁 feature（`similar`、`png`、reqwest 默认 feature 等）。收益主要在体积与启动 I/O。

#### M16. `AUDIT_CACHE` 无上限 ⬜🟢
- **归档理由**：5.2MB / 4898 条常驻内存，上限受 30 天保留封顶；v2.4.1 又加了每 24 小时自动清理，增长向量已被压住。改造要动缓存结构，收益不成比例
- `type AuditCache = Option<(PathBuf, u64, u64, Vec<AuditEntry>)>`：把**整份** audit.log
  解析后常驻内存。现 5.2MB / 4898 条尚可，但随使用时长线性增长（上限取决于 30 天保留）。
- 已做的：取页不再克隆全量（`page_slice`，详见 L5）。**剩下的是缓存本身的常驻占用**。
- 方向：缓存字节偏移而非解析结果；或只缓存最新 N 条（total 单独记，越过 N 的页回退重解）。

#### M17. tokio 未限制 worker_threads ⬜🟢
- **归档理由**：后端私有内存仅 10MB，收益有限；且降并行度会直接拖累 search_files 的并行 walker（M11 才是真瓶颈）。条目自己也写了「不要盲调」
- 全工程没有显式 `worker_threads` 配置（只有测试里有），即用 Tauri 默认 runtime = 按核数开。
  16 核机实测后端 **36 线程 / 525 句柄**。
- 但后端私有内存只 10MB，收益有限；且降并行度会影响 `search_files` 的并行 walker（见 M11）。
  **建议待 M9 启动打点就位、能量化线程创建对启动耗时的影响后再动**，不要盲调。

#### M10. 前端性能得先有可重复的测量手法 ⬜🔴【阻塞 M3/M4 的前置】
- **归档理由**：它存在的唯一目的是解锁 M3/M4；M3 已归档、M4 已完成 → 失去被阻塞对象。两条测量纪律（未受控测量、ShowWindow 污染被测对象）已记在附录，教训保留
- 问题：2026-08-03 做变量对比时发现**测量被窗口遮挡状态支配**——同一份 60fps 动画配置
  在两轮实验里 GPU 占用从 6.8% 到 0.4% 波动（WebView2 窗口被遮挡时停止合成）。
  这种噪声下无法判定任何前端优化的真实收益。
- 需要的手法：窗口强制置顶且不被遮挡、关闭其他干扰进程、单次采样 ≥ 30s、
  每个变量重复 3 轮取中位数；或改用 WebView2 devtools 的 Performance 面板看帧时间。
- ~~顺带验证：`data-app-hidden` 没被设上（最小化/收托盘 CPU 都没降，而全局注入
  paused 探针则明显降）~~ ⚠ **2026-08-03 该结论已推翻，不要再当 bug 去查**：
  那次实验是**未受控**的——当时机器上同时跑着两个同名 `cc-bridge-desktop.exe`
  （dev 版 + 安装版），按进程名采 CPU 会同时命中两个，而「最小化/关窗」是在桌面上点的，
  点到哪个窗口、测到哪个进程都没受控。直接证据：安装版 exe（Aug 2 19:09）里
  `data-app-hidden` / `app:visibility` / `pulse-ring-ripple` 三个特征串**一个都没有**，
  它当然不会降 CPU。
- ⬜ 真正要做的：在**只跑一个新版进程**的受控条件下复测（先 taskkill 掉安装版，
  或反之不跑 dev），并**按 PID 而不是按进程名**采样。信号计算本身现已有单测兜底
  （`appVisibility.test.ts`，见 L9），这一层可以从嫌疑名单里划掉。
- ⚠ **又一条测量纪律（代价是白屏一次）：不要用 P/Invoke `ShowWindow` 程序化切窗口状态来做测量**。
  2026-08-03 为了“可重复”直接调 user32 `ShowWindow` 最小化/还原，结果绕过了 Tauri 自己的
  窗口状态管理，还原后 webview 白屏——**测量手段破坏了被测对象**，那组读数
  （可见 0.65% / 最小化 0.00% 单核）因此不可采信。上一次是“未受控测量”，这次是
  “测量污染对象”，同类错误。正确做法：只观察不控制，需要切窗口时请人工点
  （托盘/最小化按钮），或改用不依赖 UI 状态的离散信号（如数探针的 TCP 连接）。

#### P7-2. 反向连接（出站）接入模式，彻底不需入站规则 ⬜【单独立项，待决策】
- **归档理由**：前提是「先观察还有多少用户落在 localPolicyBlocked / 第三方防护」，而实际用户就是这一台机器，观察样本不存在。属于为假设需求做大工程
- 动因：两类场景下入站规则永远解不了——① 域策略 `AllowLocalFirewallRules = False`（本机加规则被系统忽略）；② 企业 EDR / 360 / 火绒等第三方防护（netsh 管不着）。
- 方向：cc-bridge 主动向外建长连接（出站默认全放行），远程通过中继访问。备选：frp / 自建 relay（内网已有 Linux 机，最贴合本场景）、WebSocket 反连（需改 MCP 传输层，改动最大但最干净）。不建议 Cloudflare Tunnel / ngrok（内网代码走公网）。
- 副作用收益：顺手解决 VPN 重连后 IP 变化的痛点（反向连接下远程侧地址固定）。
- 前提：先上线诊断+修复（已完成），观察还有多少用户落在 localPolicyBlocked / 第三方防护，再决定是否投入。

#### P5-1. MCP 协议层改用官方 `rmcp` SDK ⬜【重大重构，先出方案】
- **归档理由**：当初立项的两个理由现已不成立：SSE 已自行实现（/mcp/sse + /mcp/messages 双路由），协议协商无实际需求。而迁移要把 Bearer/限流/审计/只读四层安全全部重挂到 rmcp 宏体系，风险与收益严重不对称。若上游出现必须协商才能用的特性，再捡回
- 补齐 SSE 流式 + 协议协商，手写 dispatch 换官方维护。风险：token 中间件/限流/审计/只读拦截如何挂 rmcp 宏需先单独出迁移方案，不要直接动。

#### P5-2. 目录级复制/移动补 `fs_extra` ⬜（现状：move 靠 `rename` 已支持目录；copy_files 遇目录直接 `return Err`，是否引 fs_extra 待决策）
- **归档理由**：有等价替代：cc-bridge 自带 run_command，`cp -r` / `xcopy` 直接能做目录递归复制，不值得为此引入 fs_extra 依赖
- 需先确认 `copy_files`/`move_files` 是否已支持整目录，仅单文件才补 `fs_extra`（递归复制/移动+进度）。

#### P5-3. `delete_files`/`remove_directory` 改用系统回收站 `trash` ⬜【产品取舍，需决策】
- **归档理由**：重复建设：cc-bridge 已有写前自动备份 + 版本历史 + 单份还原，恢复能力已具备，回收站是第二套恢复机制且会改变容量语义
- 换成系统回收站改变恢复方式/容量，非纯升级，需你接受取舍。

#### E-P3-6. copy_files / move_files 纯顺序处理 ⬜
- **归档理由**：有 path_locks 死锁风险，而没有任何性能数据指向 copy/move 是瓶颈
- 可考虑有限并发（但有 path_locks 死锁风险需小心，建议先讨论方案再动手）。

---

#### N7. IP 变化监听在 mac 上是空实现 ⬜🟢
- **归档理由**：功能可用，只是不实时——5s 轮询对本场景足够。要实时得上 macOS SCDynamicStore FFI
- `ip_watch.rs` 的 `cfg(not(windows))` 分支**不启线程**，地址变化完全靠上层 5s 轮询兜底。
- 功能可用（只是不实时）。要实时需上 macOS 的 `SCDynamicStore`（SystemConfiguration
  framework）。优先级低——5s 轮询对本场景足够。

---

## 附：边界说明

- **D8-续（`run_command` 危险命令拦截）** 标题标记为 🟡【一期已落地】，未标 ✅，本期未纳入。若你希望把它也视为"已做完的一部分"，可单独告诉我。
- **P3-2（run_command 持久化会话）** 为 ⏸️ 搁置，未纳入。
- 本次会话中完成的「IP 变化流程修复（P1/P2/P3/O1–O4）」、「方案 A：scope 持久化」、「默认网卡变化弱提示」等改动，原 `功能优化清单.md` 中未单列条目，故不计入本提取；如需补录，请告知。
